# Conduct checklist — /flow-next:work-rolling (experimental)

A correct run behaves exactly like canonical `/flow-next:work` everywhere except Phase 3, which it replaces with rolling per-task admission: admit at every worker-return event, isolated workspace per task, conductor-owned review, shared notes surface. Canonical work's own checklist (`work.md`) applies in full to the consumed-by-pointer surfaces; the items below cover only the rolling delta.

- [ ] The canonical `flow-next-work` files are consumed by pointer and stay byte-unchanged — the transcript reads `$WORK_SKILL/SKILL.md`, `$WORK_SKILL/phases.md`, and `$WORK_SKILL/references/wave-join.md` rather than restating them, and `git diff` shows no edit under `skills/flow-next-work/`. A run that forked or patched a canonical file has broken this.
- [ ] Every admission event prints the rolling report block — the canonical lines plus `In-flight:`, `Admitted:`, and `Held:` (each held task with its reason) — and admission is recomputed after each event (worker return or review completion), never precomputed for the run and never deferred to the end of a task's review tail. A candidate missing a `**Touches:**` declaration, intersecting an in-flight task, or dep-connected to one is held, and an empty admissible set with a non-empty frontier reports a `Sequential fallback:` reason.
- [ ] With `planSync.enabled=true` the run dispatches strictly serially (at most one in-flight task) and reports `Sequential fallback: planSync.enabled=true` once; canonical 3e then runs after each completed task. A concurrent admission under an enabled plan-sync has broken this.
- [ ] Every dispatched worker — including a one-task admission — carries `PARALLEL_WAVE: true` and returns the parallel handover; the conductor integrates that task (wave-join evidence + SHA normalization), runs its review itself, and calls `flowctl done` only on SHIP. A worker that self-completed, self-reviewed, or a join conflict resolved by hand instead of one serial retry has broken this.
- [ ] The notes directory is created at Phase 3 start under the state-dir sibling, keyed by spec id + run identifier; its path (never its content) rides every worker and scout dispatch prompt; it is deleted only after canonical Phase 5 completes cleanly (never at quiesce - a quality/ship failure still needs the notes). A creation failure degrades to `Notes surface: unavailable (...)` and the run continues.
- [ ] Cross-run claim contention fails closed (verified once, 2026-08-22, in an isolated `FLOW_STATE_DIR` sandbox: `flowctl start` on a task claimed by another actor returns rc 1 `claimed by '<actor>'`; a same-actor re-start is flowctl's own-task resume, so admission additionally skips any task already `in_progress`). The loser drops the task rather than clearing the claim; a run that used `--force`/`--reclaim` to take another run's task has broken this.
- [ ] A `--no-plan` flag or natural-language no-plan intent is refused at the pre-check, before the canonical work files are read, with the stated reason (a single implicit task degenerates the rolling frontier) and a redirect to plain `/flow-next:work <spec-id> --no-plan`. A rolling run that reached the canonical zero-task fork, asked the fork's question, or minted an implicit task has broken this.

## Dogfood record - fn-203.4, 2026-08-22 (initial ship)

Per-item pass/fail from the pre-handoff dogfood. Scope note: fn-203.4's acceptance names exactly this evidence bar - "Admission rule + report lines behave per R4 on a dogfood run; planSync=true degrades to serial" - and the beta's Phase-3 scheduler was executed against the real 5-task fixture spec `fn-174` (flow-swarm `eval/fixture-rolling-frontier`, the fn-203 pre-registration fixture) in an isolated clone with its own `FLOW_STATE_DIR`, producing those report lines live. Full multi-worker end-to-end behavior of the identical architecture (real workers, conductor-owned review, per-task wave-join integration) is evidenced by the fn-203.3 A1 eval draw (study `rolling-frontier-2026-08`, verdict `9635d29`: 52.1% wall saving at quality parity, zero uncontained incidents), whose arm-1 prototype is the prose this beta ships; the beta's own first field runs on real specs are Phase C's pre-declared field window (fn-203 R10) - field receipts from real beta runs are the graduation evidence by design, not a pre-ship gate this task could satisfy from inside a worker context.

- Item 1 (canonical byte-unchanged, consumed by pointer): **pass** - `git diff` over `skills/flow-next-work/` = 0 lines in the fn-203.4 range.
- Item 2 (admission report lines, per-event recomputation, holds): **pass** - dry-run events E0-E3 printed the full block (`In-flight`/`Admitted`/`Held` + canonical lines); cap-holds reported per held task; frontier recomputed per event.
- Item 3 (planSync=true serial degradation): **pass** - the fixture's config carried `planSync.enabled=true`; the gate reported `Sequential fallback: planSync.enabled=true`. Dogfood also caught the initial dry-run printing an admission block before honoring the gate - the scheduler reference orders 3.0 before 3a, which prevents exactly that.
- Item 4 (PARALLEL_WAVE handover, conductor-owned review, join-conflict serial retry): **pass via fn-203.3 A1 draw evidence** (not re-exercised live in this session's dry-run; the beta consumes the identical wave-join mechanics by pointer).
- Item 5 (notes dir lifecycle + advisory degrade): **pass** - created keyed by spec id + run id under the state-dir sibling, note written/read by path, deleted at end of run; unwritable parent degraded to empty `NOTES_DIR` and the run continued.
- Item 6 (cross-run claim contention): **pass** - second `flowctl start` by a different actor returned rc 1 `claimed by 'beta-run'`; same-actor re-start confirmed as own-task resume.

## Dogfood record - blocking-clause provenance edit, 2026-08-27

The edit binds the degradation clause to measured dispatch behavior instead of a host-name list. Its dogfood is the pair of live end-to-end beta runs that produced the measurement, on the two hosts the old list named (blind 3-task fixture, staggered 90/60/30s tasks, `--review=none`; conductors instructed to bind on measurement rather than the parenthetical - exactly the behavior this edit makes default):

- **Cursor (macOS, heimdall)**: probe CONTROL at t0+8s with both agents running, per-completion notify; run: 3 admitted at one event, returns 30/60/90-ordered, each integrated while the slowest ran, `Scheduling: rolling`, `blocked_by_prose=no`, quiesce same-turn. Items 1, 2, 4, 5 re-exercised live: pass.
- **Grok Build 1.0.5 (linux, thor)**: probe CONTROL at t0+0s via `spawn_subagent` background mode, FAST notify at t0+33s; run: simultaneous 3-dispatch, .3 integrated (FF-merge) while .1/.2 in flight, .2 integrated via cherry-pick after the target moved, 3g correctly not-applying under `--review=none`, full-gate quiesce + notes cleanup. Items 1, 2, 4, 5 re-exercised live: pass.
- Items 3 and 6 unchanged by this edit (planSync gate and claim contention untouched); prior records stand.
