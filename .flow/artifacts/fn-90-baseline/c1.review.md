# c1 — cursor:gpt-5.5-high fresh, repo as-is

flowctl_verdict: NEEDS_WORK
true_verdict: NEEDS_WORK
model: gpt-5.5-high

---

I’ll review the full on-disk spec/tasks and current diff, then judge only the plan consistency and implementability against the repo’s actual tracker-sync/workflow shape.I’ve loaded the plan-review guidance; because this is Ask mode I won’t run any receipt/checkpoint-writing review backend. I’m reading the authoritative spec/tasks plus the actual branch diff now.The working-tree diff summary came back empty, so I’ll treat this as a plan review against the current repo state rather than an implementation diff review unless a merge-base diff reveals otherwise. I’m pulling the actual skill/flowctl anchors next to verify the plan’s assumptions.The repo is currently on `main` with no branch diff, so there are no changed files to inspect. The spec is still reviewable: I’m now checking whether the task plan matches existing event names, config defaults, mirror mechanics, and touchpoint control flow.Two likely plan-level hazards are emerging: the “Step 0” proof point is assigned to a normal work task even though workers cannot spawn `Task`, and comment ops are not receipt-only when the spec is unlinked because current tracker-sync auto-links them. I’m checking the remaining touchpoints to see if those are isolated or systemic.Issues found:

**Severity: Major**  
**Location: fn-89.1 Step 0 / Epic Early proof point + R13**  
**Problem:** Step 0 requires spawning a background `Task` plus an intervening foreground `Task`, but fn-89.1 is a normal work task and `worker` agents have `disallowedTools: Task`. This also contradicts R13: tracker-runners are host-context only, never worker-subagent dispatched.  
**Suggestion:** Move Step 0 to a host-run preflight before `/flow-next:work`, or make fn-89.1 explicitly non-worker/host-executed. The worker cannot own this proof.

**Severity: Major**  
**Location: Epic R3 / fn-89.2 fire-and-forget comments**  
**Problem:** The plan says comment-shaped dispatches “write only per-file receipts and never touch spec JSON,” but current tracker-sync auto-links unlinked specs even for `comment <spec-id>`, writing tracker id, merge base, and `lastSyncedAt`. Two comment dispatches on an unlinked spec can race and create duplicate issues or torn sync state.  
**Suggestion:** Treat comment ops as state-shaped until the spec is linked, or preflight `sync get-state` and serialize/await when `tracker.id` is missing. Alternatively change tracker-sync so comment ops never auto-link.

**Severity: Major**  
**Location: Epic R1/R4/R10 + fn-89.2 completionReview**  
**Problem:** `completionReview` config is top-level (`tracker.perEvent.completionReview`), but the work audit/event tag uses `work.completionReview`. `sync check` resolves event leaves by `tracker.perEvent.<event>`, so `work.completionReview` appears disabled and won’t be reported MISSING. That breaks the claimed Phase 5 backstop for fire-and-forget completion review.  
**Suggestion:** Normalize the key everywhere: either use `completionReview` for event tags/checks, or move the config leaf under `tracker.perEvent.work.completionReview` and update discovery/docs/tests.

**Severity: Major**  
**Location: Epic API Contracts / fn-89.1 runner contract**  
**Problem:** Runner outcome enum omits real tracker-sync outcomes. Current receipt states include `pulled` and `diverged`; capture/interview/plan still dispatch `operation: <leaf>` where the leaf may be `pull`, and body conflicts can produce `diverged`. The plan also says receipt semantics stay unchanged.  
**Suggestion:** Make the outcome line status accept the full tracker receipt enum, or explicitly define lossless mappings and update receipt/summary expectations.

<verdict>NEEDS_WORK</verdict>
