# x2 — codex — verdict=NEEDS_WORK

Reviewing against current repo shape first: tracker-sync contract, work phases, agent generation, and Codex mirror guards.
I’ve confirmed the current tracker-sync path is still prose-driven, not executable helper code. Next pass: places the plan wants to hook into, plus existing config/test conventions.
Current flowctl has `sync get-state`, so the linked/unlinked probe is feasible. I’m checking receipt/timeout mechanics now, because that’s where async plans usually become ambiguous.
Receipt writer can support host-side `errored` without a transport value, so that part is mechanically possible. The bigger risk is now the host-agent contract itself: exact async handle lifecycle, not flowctl.
One real semantic gap surfaced: queued conflicts only return a one-line status, but the host-side ask-after-join needs enough detail to ask a useful question. I’m checking deferred-conflict output shape before calling that a blocker.
- **Severity: Major**  
  **Location:** Epic R8, fn-89.1 Approach 7-9, fn-89.4 smoke contracts  
  **Problem:** Claude-only async gate is still too prose-level. Current repo has an executable `host_is_claude_code()` + tests for codex-delegation; this plan only requires “doctrine documented.” That is not enough for the v1 safety invariant, especially because Codex mirror prose will still rewrite `Task flow-next:tracker-runner` into a `tracker_runner` role reference.  
  **Suggestion:** Add a concrete dispatch-mode predicate at the consumption site and test it like `test_codex_delegation_gates.py`: Claude marker passes; Droid/OpenCode/Codex fail; `tracker.dispatch == "inline"` forces inline; booleans/junk do not. Add a mirror guard proving Codex touchpoints resolve inline, not runner/background.

- **Severity: Major**  
  **Location:** Epic API Contracts, fn-89.1 Approach 4, fn-89.2/.3 awaited/fire-and-forget steps  
  **Problem:** The async handle lifecycle is underspecified. Current skills use foreground `Task`; existing background examples are Bash `run_in_background`, which this design explicitly distinguishes. The plan says “record handle,” “join,” “timeout,” and “outcome line,” but does not define exact Claude `Task` launch syntax, handle naming, join operation, timeout behavior, or result parsing.  
  **Suggestion:** In .1, make the tracker-runner contract include a literal canonical block for: background launch, awaited launch, ledger entry shape, join, timeout, output parsing, and summary capture. .2/.3 should reference that block verbatim, and smoke should assert rewritten touchpoints use it.

- **Severity: Major**  
  **Location:** Epic R12, fn-89.2 Approach 5/7  
  **Problem:** Phase 5 “await all outstanding dispatches” can become an unbounded tail in practice. R12 says 10 minutes for an awaited runner, but with many fire-and-forget `work.done` comments the audit join could wait 10 minutes per handle unless the contract defines a shared wall-clock budget or concurrent join semantics. That violates the “off critical path” performance goal.  
  **Suggestion:** Define Phase 5 join as one bounded batch join per spec, with one 10-minute wall-clock ceiling for all outstanding handles, progress lines per handle, and fire-and-forget timeout behavior explicitly receipt-less.

- **Severity: Minor**  
  **Location:** Epic R6/R7, fn-89.1 runner output contract, fn-89.2/.3 ask-after-join  
  **Problem:** A queued conflict returns only `<status> <spec-id> <note>`. Host-side ask-after-join needs enough detail to ask a meaningful question or point the user to the deferred decision. `flowctl sync defer --json` returns `sink_path`, but the runner contract does not require including it.  
  **Suggestion:** For `queued`, require the runner’s one-line note to include the deferred sink path and short conflict title, or require the host to read the latest `.flow/review-deferred/<branch>.md` entry for that spec before surfacing the question.

- **Severity: Minor**  
  **Location:** fn-89.1 Test, fn-89.4 Smoke prose contracts  
  **Problem:** R5 says exact-match `dispatch == "inline"` only, and bool never activates the hatch. The proposed tests cover default plus `inline`/`async` round-trip, but not `true`, `false`, `null`, or junk values after `set_config` coercion.  
  **Suggestion:** Add direct config tests and a prose/structural guard for exact-match semantics at the dispatch gate.

<verdict>NEEDS_WORK</verdict>
