# x2 — codex default fresh (clean parse)

flowctl_verdict: NEEDS_WORK
true_verdict: NEEDS_WORK
model: gpt-5.5

---

**Issues**

- **Severity: Major**  
  **Location: Epic R3/R4; fn-89.2 Approach 4-5; fn-89.3 Approach 2-3**  
  **Problem:** Cross-skill serialization is not actually carried through. The epic says a state-shaped dispatch must await any in-flight dispatch for the same spec, and every pre-audit join awaits all outstanding same-spec dispatches. Tasks only define the ledger in the work conductor; make-pr/capture/interview/plan mostly await their own runner. This misses the stated work.done → makePr/capture straddle.  
  **Suggestion:** Add a shared `tracker-dispatch` reference in `.1` defining ledger fields, launch, join, timeout, and same-spec rules. Require every `.2/.3` touchpoint and every audit site to consult it and join all same-spec outstanding handles.

- **Severity: Major**  
  **Location: Epic Edge Cases/R12; fn-89.2 Approach 7; fn-89.3 Approach 8**  
  **Problem:** Host-side timeout/death writes an `errored` receipt, but current `sync check` semantics treat any-status receipts, including `errored`, as clearing the event. That contradicts the “dead dispatch = unwritten receipt = caught by sync check → retro-fire once” recovery path.  
  **Suggestion:** Distinguish runner-internal errored receipts from host launch/join failure. For host timeout/death, do not write an event-clearing receipt before `sync check`, or explicitly change the recovery contract and summary behavior.

- **Severity: Major**  
  **Location: Epic API Contracts; fn-89.1 Approach 4**  
  **Problem:** Runner outcome status enum omits existing tracker receipt states `pulled` and `diverged`. Lifecycle operations can be `pull` or `reconcile` via capture/interview/plan leaves, so the one-line outcome contract is lossy or impossible for valid existing outcomes.  
  **Suggestion:** Use the existing `TRACKER_RECEIPT_STATES` set for runner status, or define a lossless mapping with detail in `<note>`.

- **Severity: Major**  
  **Location: Epic R8; fn-89.1 Approach 8; fn-89.2/.3 mirror steps**  
  **Problem:** Codex v1 is supposed to run inline, but the planned global mirror rewrite converts `Task flow-next:tracker-runner` into a `tracker_runner` role form. Unless the surrounding prose is also rewritten or gated, Codex mirror docs may still instruct isolated/background runner use despite the “inline only” requirement and the existing no-background convention.  
  **Suggestion:** Add a Codex-specific rewrite/guard that makes tracker dispatch blocks explicitly inline when `host_is_claude_code=false`, or add a structural test proving Codex mirror tracker touchpoints do not instruct fire-and-forget/background runner execution.

- **Severity: Major**  
  **Location: fn-89.1 Approach 0 / Acceptance Step 0**  
  **Problem:** The live proof point is a hard gate but not operationally reproducible. It does not specify the target spec/issue, how to create a tracker-side body change safely, how to force a reconcile writeback, cleanup, or exact success evidence. This leaves the first task blocked on tribal context.  
  **Suggestion:** Turn Step 0 into a concrete recipe: fixture spec id, tracker type/rung, mutation text, expected changed file/receipt, cleanup/reset, and exact “hold” evidence.

- **Severity: Minor**  
  **Location: Test strategy across tasks**  
  **Problem:** The plan relies heavily on prose/smoke checks. It does not require a repeatable check for `tracker.dispatch` exact-match behavior (`inline` only; boolean/string junk never activates), host-capability inline fallback, or Codex mirror inline gating.  
  **Suggestion:** Add focused tests or smoke assertions for those contracts. Keep R4 host timing as prose if needed, but make the config/platform routing mechanically checked.

<verdict>NEEDS_WORK</verdict>
