# c2 — cursor:gpt-5.5-high fresh, repo as-is (independent)

flowctl_verdict: NEEDS_WORK
true_verdict: NEEDS_WORK
model: gpt-5.5-high

---

I’ll review the full on-disk spec/tasks plus the current diff and relevant skill code, then return only plan-blocking issues with the required verdict.I’ve loaded the full spec and task specs. Next I’m checking the actual branch diff and the remaining touchpoint files so the verdict is grounded in the current repo, not just the embedded text.The working tree appears clean, so I’m treating the committed branch changes as the review surface and diffing against the merge base rather than uncommitted files.A couple of consistency risks are emerging around state-shaped serialization and the runner’s status contract. I’m checking `sync check` semantics now because that determines whether failed background receipts are backstopped correctly.The code confirms `sync check` intentionally treats any event receipt, including `errored`/`noop`, as “the touchpoint ran.” That matches the current audit primitive, so I’m separating retry/backstop concerns from the serialization and contract gaps.- **Severity**: Major  
  **Location**: Epic R3 / Task `fn-89.2` Approach 2-4  
  **Problem**: The plan treats comment-shaped ops as receipt-only and freely overlap-safe, but current tracker-sync explicitly auto-links unlinked specs for `comment` ops, writing tracker id, merge base, and `lastSyncedAt`. So `work.done` / `completionReview` can be state-mutating when they are the first tracker touchpoint. Fire-and-forget here can race or duplicate create/link state.  
  **Suggestion**: Add rule: comment ops on unlinked specs are state-shaped, awaited, and serialized until tracker state exists; only linked-spec comments may fire-and-forget.

- **Severity**: Major  
  **Location**: Task `fn-89.3` / Epic R3 coverage table  
  **Problem**: Epic says every state-shaped dispatch must first await any outstanding same-spec dispatch, covering cross-skill straddles. Task `.3` rewires make-pr/capture/interview/plan as awaited but does not require the pre-dispatch same-spec ledger join, and its frontmatter omits R3.  
  **Suggestion**: Add R3 to `.3`; before every state-shaped dispatch in make-pr/capture/interview/plan, await outstanding same-spec runner handles, not just await that dispatch after launching.

- **Severity**: Major  
  **Location**: Epic API Contracts / Task `fn-89.1` Runner Agent  
  **Problem**: Runner outcome enum excludes valid existing tracker-sync receipt states: `pulled` and `diverged`. `capture`/`interview`/`plan` can invoke `<leaf>` and leaves can be `pull`; body conflicts currently receipt as `diverged`. An “exactly one line” closed enum that cannot report valid outcomes will force lossy or false reporting.  
  **Suggestion**: Either make runner status mirror `TRACKER_RECEIPT_STATES` exactly, or explicitly define normalization (`pulled -> updated`, `diverged -> queued`) and update host handling/tests.

- **Severity**: Minor  
  **Location**: Epic R6 / Tasks `.2` and `.3` ask-after-join  
  **Problem**: “Preserves today’s interactive UX” is under-specified. `sync defer` stores a summary/suggested fix in the deferred sink, but the host only receives a one-line outcome and may not have the scoped merge body/options needed to ask the same conflict question inline.  
  **Suggestion**: Define queued-conflict retrieval explicitly, or weaken the promise to “queues and reports; manual reconcile remains the interactive resolution path.”

<verdict>NEEDS_WORK</verdict>
