# x1 — codex default fresh (flowctl verdict SHIP = parse bug; true verdict below)

flowctl_verdict: SHIP
true_verdict: NEEDS_WORK
model: gpt-5.5

---

**Issues**

- **Severity: Critical**  
  **Location: fn-89.1 Approach Step 0 / Epic R13**  
  **Problem:** Step 0 requires dispatching a background `Task` and an intervening foreground `Task`, but normal task implementation runs in `worker`, whose agent contract has `disallowedTools: Task`. The epic also says tracker-runners are host-only and never dispatched from worker subagents. So the required proof point is impossible from the normal fn-89.1 implementation context.  
  **Suggestion:** Move Step 0 to an explicit host-level preflight before starting fn-89.1, or create a tiny host-run ceremony/check outside the worker path. The task should consume recorded evidence, not be responsible for spawning `Task`.

- **Severity: Major**  
  **Location: Epic Edge Cases / R12 and fn-89.2 Approach Steps 5, 7**  
  **Problem:** The failure model contradicts the retro-fire model. The epic says a dead fire-and-forget dispatch leaves no receipt, so `sync check` catches `MISSING` and retro-fires once. But R12 / task .2 say timeout/death writes a host-side `errored` receipt. Current `flowctl sync check` treats **any** event-tagged receipt status as clearing the event, including `errored`, so no retro-fire would happen.  
  **Suggestion:** Pick one invariant. For fire-and-forget deaths, either do not write an event-tagged receipt before `sync check`, or change `sync check` semantics to treat `errored` as missing. If `sync check` must remain unchanged, the host-side errored receipt must not carry the lifecycle `event`.

- **Severity: Major**  
  **Location: Epic Dispatch Shape / fn-89.2 and fn-89.3 touchpoint rewrites**  
  **Problem:** The plan depends on concrete background `Task` handle semantics, joins, and a 10-minute timeout, but does not specify the actual invocation/join syntax or handle naming contract in the skills. Existing repo prose only says “Use the Task tool…” for blocking subagents; there is no reusable dispatch helper, no current ledger pattern, and the proposed tests are mostly grep/prose checks.  
  **Suggestion:** In fn-89.1, add a single reusable dispatch contract section with exact host actions: how to launch background vs awaited runner, what to store in the ledger, how to join, how to timeout, and what outcome line shape updates the summary. Later tasks should reference that exact section.

- **Severity: Major**  
  **Location: fn-89.1 Codex mirror / runner agent generation**  
  **Problem:** `sync-codex.sh` currently maps agent sandbox by name: only `worker|plan-sync` become `workspace-write`; every other generated agent is `read-only`. A new `tracker-runner.toml` would therefore be read-only in Codex even though the canonical runner contract explicitly requires Write/Edit for reconcile writeback. The plan says Codex v1 resolves inline, but it also intentionally rewrites mirror prose to a `tracker_runner` role, creating a dangerous mismatch if followed.  
  **Suggestion:** Either add `tracker-runner` to `sandbox_for()` as `workspace-write`, or explicitly make the Codex mirror rewrite say “inline on Codex; do not invoke tracker_runner in v1.” Prefer adding the sandbox mapping so the generated role matches its contract.

- **Severity: Minor**  
  **Location: fn-89.4 Smoke prose contracts / Acceptance**  
  **Problem:** The stated smoke tests prove presence of prose, not behavior: runner file exists, dispatch text exists, default config exists. They do not catch missing event tags across all touchpoints, missing pre-audit joins, or Codex sandbox mismatch.  
  **Suggestion:** Add cheap structural assertions: all lifecycle dispatch blocks include `event:` and `DISPATCH: forked`; all end-of-run `sync check` sections mention pre-audit join; `tracker-runner.toml` exists with expected sandbox/model if generated.

<verdict>NEEDS_WORK</verdict>
