# n1 — cursor:gpt-5.5-high fresh, worktree without AGENTS.md/CLAUDE.md

flowctl_verdict: NEEDS_WORK
true_verdict: NEEDS_WORK
model: gpt-5.5-high

---

I’ll review the full on-disk spec and task files first, then compare them against the actual repository state and changed files before giving a verdict. I’ll stay read-only and focus on blockers or inconsistencies in the plan, not implementation edits.I’ve loaded the full spec and four task specs from disk. Next I’m checking the branch diff and the concrete code/doc anchors the plan depends on, especially existing tracker gates, sync-codex behavior, and the proposed runner constraints.The branch diff itself is minimal and unrelated to the fn-89 plan, so I’m treating the spec/task files as the review target and using the current codebase as feasibility context. I’m now checking the exact touchpoint files and flowctl config behavior for plan/task mismatches.The current tracker code matches the spec’s anchors: event-tagged receipts are real, `readyState` is top-level today, and `sync-codex.sh` has the structural `Task flow-next:` guard the spec calls out. I’m looking for the kinds of mismatches that would actually stop implementation rather than stylistic concerns.One important consistency check is how `sync check` decides whether an event fired. If an `errored` event receipt counts as present, that affects the plan’s “dead fire-and-forget becomes MISSING and retro-fires” story, so I’m reading that implementation now.I’ve found two related load-bearing issues around the planned “ledger + sync check as backstop” design. I’m doing one last pass for any task ordering or external-docs requirements that would change the verdict.## Issues

**Severity:** Major  
**Location:** Epic R3 / Overview #4; `fn-89.2` Approach #2-5  
**Problem:** The ledger is specified as `spec id -> outstanding agent handle`, but comment-shaped dispatches are explicitly allowed to overlap on the same spec. In a multi-task work run, multiple `work.done` runners can be in flight before Phase 5. A singular handle either loses earlier handles or forces serialization, contradicting R1/R3 and making “await all outstanding dispatches” impossible.  
**Suggestion:** Define the ledger as something like `spec_id -> { state_handle: optional, comment_handles: [] }`, with explicit append/remove/join semantics. State-shaped ops should await all same-spec handles, then install their single state handle; comment ops append freely.

**Severity:** Major  
**Location:** Epic R1/R4/R12; `fn-89.2` Approach #5/#7  
**Problem:** The plan relies on `sync check` as the retry backstop for fire-and-forget failures, but the current primitive is event-level and any status clears it. `test_sync_check.py` explicitly asserts `errored`/`noop` receipts clear the event. So one successful `work.done` receipt can mask another dead `work.done` runner in the same run, and a host-written event-tagged `errored` receipt on timeout would suppress the planned retro-fire.  
**Suggestion:** Don’t make `sync check` the sole per-dispatch retry mechanism. Use the ledger join to detect each failed/timed-out fire-and-forget handle and retro-fire that exact op before writing a clearing receipt, or change the receipt/check contract to be instance/status-aware and update the “no receipt fields change” boundary accordingly.

**Severity:** Minor  
**Location:** Epic “Early proof point”; `fn-89.1` Approach #0  
**Problem:** The proof point requires a live reconcile writeback to `.flow/specs/<id>.md`, but it doesn’t specify a disposable target or restoration procedure. If the interleave or writeback fails, the plan says “land no code,” but the proof itself may already have mutated a real spec/tracker issue.  
**Suggestion:** Pin the proof to a throwaway spec/issue or require a pre-proof snapshot plus explicit restore/cleanup steps before proceeding or escalating.

<verdict>NEEDS_WORK</verdict>
