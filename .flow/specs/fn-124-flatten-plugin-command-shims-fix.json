{
  "branch_name": "fn-124-flatten-plugin-command-shims-fix",
  "completion_review_status": "unknown",
  "completion_reviewed_at": null,
  "created_at": "2026-07-22T14:59:02.762993Z",
  "default_impl": null,
  "default_review": null,
  "default_sync": null,
  "depends_on_epics": [],
  "id": "fn-124-flatten-plugin-command-shims-fix",
  "impl_review_rounds": {},
  "next_task": 1,
  "plan_review_rounds": 0,
  "plan_review_status": "ship",
  "plan_reviewed_at": "2026-07-22T15:17:30.004930Z",
  "spec_path": ".flow/specs/fn-124-flatten-plugin-command-shims-fix.md",
  "status": "open",
  "title": "Flatten plugin command shims: fix tripled command names in Claude Code menu",
  "tracker": {
    "baseHashFlow": "233fa943e1f8f07176db39fd9d72f56992fedf9593034e0a057a78a754e8aba9",
    "baseHashTracker": "5fb36c3df068d62ca9b0b965dfba5058c95e8daca350e50725d0de301933a919",
    "depRelations": [],
    "id": "2ddb1fc6-3b84-454b-880f-c81cbecd6dcc",
    "identifier": "FLOW-57",
    "lastSyncedAt": "2026-07-22T15:09:15.327646Z",
    "mergeBaseFlow": "# Flatten plugin command shims: fix tripled command names in Claude Code menu\n\n## Overview\n\nClaude Code's slash menu renders flow-next command shims as `/flow-next:flow-next:flow-next:qa` (observed 2026-07-22). Three name layers stack:\n\n1. Shims live in a subdirectory `plugins/flow-next/commands/flow-next/*.md`; Claude Code's namespacing is `<plugin-name>:<basename>` and the plugin-name-colliding subdir contributes an extra segment.\n2. Each shim carries pre-plugin-era frontmatter `name: flow-next:qa` (colon inside `name` is non-standard; per current docs, `name` replaces only the LAST segment, plugin prefix stays - v2.1.216+ semantics; before that, `name` replaced the whole command name, which is why the literal names once looked right).\n3. Claude Code prefixes the plugin name.\n\nFix: flatten the shim directory to `plugins/flow-next/commands/*.md` and drop the namespace from frontmatter, updating the Cursor manifest and all path references in lockstep. On Claude Code the skills are the invocable surface; the shims exist for Cursor (manifest `commands` path) and as harmless redirects elsewhere.\n\nVerified namespacing rules (docs-scout, code.claude.com/docs skills + plugins-reference, Jul 2026):\n- Namespace is exactly ONE segment: `<plugin-name>:<file-basename>`; subdirs under `commands/` are not a documented nesting feature - the `flow-next/` subdir naming collides with the plugin prefix.\n- Frontmatter `name:` overrides only the last segment (v2.1.216+); `name: flow-next:qa` renders the colon literally -> doubled prefix.\n- Flat `commands/qa.md` with no `name:` (or `name: qa`) -> `/flow-next:qa`. This satisfies R2.\n\n## Quick commands\n\n```bash\ncd plugins/flow-next/tests && python3 -m unittest test_cursor_plugin_surface test_install_cursor_parity test_cursor_review_commands test_model_routing_scaffold -q\npython3 scripts/ci/verify_cursor_install.py   # from repo root, after install-cursor.sh into a temp HOME if applicable\n./scripts/sync-codex.sh && ./scripts/sync-codex.sh   # twice, idempotent\n```\n\n## Approach\n\n- `git mv plugins/flow-next/commands/flow-next/*.md plugins/flow-next/commands/` (preserve history), leaving `commands/flow-next/` gone (no .gitkeep exists).\n- **Delete `epic-review.md` outright** instead of moving it: its own prose says the alias is removed in 2.0.0, repo is at 3.2.x - dead shim. 24 files -> 23 moved.\n- Frontmatter: strip the `flow-next:` prefix from every shim's `name:` (e.g. `name: qa`), or remove `name:` entirely so the basename governs - implementer verifies which of the two renders cleanest on current Claude Code AND keeps Cursor's command names stable (Cursor may key display names off `name:`); prefer removing the field if both hosts derive from filename. Document the choice in the commit message.\n- Update `.cursor-plugin/plugin.json:19` `\"commands\": \"./commands/flow-next\"` -> `\"./commands\"`.\n- Update `scripts/install-cursor.sh` (comments ~L21-23, count-check glob ~L87) and `scripts/install-cursor.ps1` equivalents.\n- Update `scripts/ci/verify_cursor_install.py:41` (`base = root / \"commands\" / \"flow-next\"` -> `root / \"commands\"`).\n- Update `plugins/flow-next/tests/test_model_routing_scaffold.py:45` (`UNINSTALL` path).\n- Run `./scripts/sync-codex.sh` twice; mirror expected unchanged (only a comment at `scripts/sync-codex.sh:1487` references the old path - update the comment text).\n- Post-change grep sweep: `grep -rn \"commands/flow-next\" --exclude-dir=.git --exclude-dir=.flow` must return only historical CHANGELOG entries (leave those as-is).\n\n## Boundaries / non-goals\n\n- Do NOT change skill names or the skills surface - command shims only.\n- Do NOT rewrite historical CHANGELOG entries (lines ~691/1093/1114 reference the old path in dated prose).\n- Droid/Grok have no test harness here: flat `commands/` is the standard Claude plugin layout both consume, so doc-level verification suffices (platforms.md note).\n- fn-123 already landed on main and is included in this worktree; no rebase pending.\n- flow-next.dev / downstream walk happens at the batched release, not in this spec (maintainer workflow).\n\n## Strategy Alignment\n\nActive tracks served by this plan:\n- **Cross-platform parity** - one shim surface that renders correctly on Claude Code while keeping Cursor's manifest-driven commands working.\n\n## Decision context\n\n- Flatten + de-prefix beats \"keep nested dir and add an explicit `commands` key to `.claude-plugin/plugin.json`\": the docs define no per-plugin commands-path override for Claude Code discovery, and the nested dir exists only as a Cursor accommodation that a one-line manifest edit removes.\n- Delete epic-review shim now: self-declared dead since 2.0.0.\n\n## Acceptance Criteria\n\n- **R1:** On current Claude Code, flow-next commands render without duplicated prefixes (target `/flow-next:qa`); no `flow-next:flow-next` entries remain except the inherent plugin-prefix + `flow-next` management-skill pairing.\n- **R2:** Current Claude Code command-namespacing rules verified against docs/release notes; chosen fix justified against them (documented in Overview above; implementer re-confirms live menu rendering).\n- **R3:** Cursor commands keep working: manifest `commands` path updated in lockstep; `install-cursor.sh`/`.ps1`, `scripts/ci/verify_cursor_install.py`, and tests (`test_install_cursor_parity`, `test_cursor_plugin_surface`, `test_cursor_review_commands`) updated and green.\n- **R4:** `scripts/sync-codex.sh` run twice stays idempotent; Codex mirror unaffected or regenerated cleanly.\n- **R5:** No version bump; CHANGELOG entry staged under `## Unreleased`.\n\n## Early proof point\n\nTask fn-124-flatten-plugin-command-shims-fix.1 validates the core approach (flat dir + de-prefixed frontmatter renders `/flow-next:qa` on current Claude Code). If the menu still doubles, re-verify frontmatter `name` semantics before touching docs.\n\n## Requirement coverage\n\n| Req | Description | Task(s) | Gap justification |\n|-----|-------------|---------|-------------------|\n| R1 | Clean menu names on Claude Code | fn-124-flatten-plugin-command-shims-fix.1 | - |\n| R2 | Namespacing rules verified, fix justified | fn-124-flatten-plugin-command-shims-fix.1 | - |\n| R3 | Cursor path + scripts + tests green | fn-124-flatten-plugin-command-shims-fix.1 | - |\n| R4 | sync-codex idempotent | fn-124-flatten-plugin-command-shims-fix.1 | - |\n| R5 | CHANGELOG Unreleased, no bump | fn-124-flatten-plugin-command-shims-fix.2 | - |\n\n## References\n\n- `plugins/flow-next/commands/flow-next/` - 24 shims, uniform frontmatter (`qa.md:2` shows `name: flow-next:qa`)\n- `plugins/flow-next/.cursor-plugin/plugin.json:19` - Cursor commands path override\n- `plugins/flow-next/.claude-plugin/plugin.json` - no `commands` key (auto-discovery)\n- `scripts/install-cursor.sh:21-23,87`, `scripts/install-cursor.ps1` - path refs\n- `scripts/ci/verify_cursor_install.py:41,79,111` - command-dir assertions\n- `plugins/flow-next/tests/test_model_routing_scaffold.py:45` - UNINSTALL path\n- `scripts/sync-codex.sh:1487` - comment-only reference\n- `plugins/flow-next/docs/platforms.md:208,237,241` - stale path/count prose (\"22 wrappers\" was already stale; 23 after epic-review deletion)\n- `agent_docs/adding-skills.md:7` - authoring instruction for new shims (highest-priority doc fix)\n- Claude Code docs: skills \"How a skill gets its command name\"; plugins-reference; CHANGELOG v2.1.208/v2.1.216 name-semantics fixes\n",
    "mergeBaseTracker": "## Overview\n\nClaude Code's slash menu renders flow-next command shims as `/flow-next:flow-next:flow-next:qa` (observed 2026-07-22). Three name layers stack:\n\n1. Shims live in a subdirectory `plugins/flow-next/commands/flow-next/*.md`; Claude Code's namespacing is `<plugin-name>:<basename>` and the plugin-name-colliding subdir contributes an extra segment.\n2. Each shim carries pre-plugin-era frontmatter `name: flow-next:qa` (colon inside `name` is non-standard; per current docs, `name` replaces only the LAST segment, plugin prefix stays - v2.1.216+ semantics; before that, `name` replaced the whole command name, which is why the literal names once looked right).\n3. Claude Code prefixes the plugin name.\n\nFix: flatten the shim directory to `plugins/flow-next/commands/*.md` and drop the namespace from frontmatter, updating the Cursor manifest and all path references in lockstep. On Claude Code the skills are the invocable surface; the shims exist for Cursor (manifest `commands` path) and as harmless redirects elsewhere.\n\nVerified namespacing rules (docs-scout, [code.claude.com/docs](<http://code.claude.com/docs>) skills + plugins-reference, Jul 2026):\n\n* Namespace is exactly ONE segment: `<plugin-name>:<file-basename>`; subdirs under `commands/` are not a documented nesting feature - the `flow-next/` subdir naming collides with the plugin prefix.\n* Frontmatter `name:` overrides only the last segment (v2.1.216+); `name: flow-next:qa` renders the colon literally -> doubled prefix.\n* Flat `commands/qa.md` with no `name:` (or `name: qa`) -> `/flow-next:qa`. This satisfies R2.\n\n## Quick commands\n\n```bash\ncd plugins/flow-next/tests && python3 -m unittest test_cursor_plugin_surface test_install_cursor_parity test_cursor_review_commands test_model_routing_scaffold -q\npython3 scripts/ci/verify_cursor_install.py   # from repo root, after install-cursor.sh into a temp HOME if applicable\n./scripts/sync-codex.sh && ./scripts/sync-codex.sh   # twice, idempotent\n```\n\n## Approach\n\n* `git mv plugins/flow-next/commands/flow-next/*.md plugins/flow-next/commands/` (preserve history), leaving `commands/flow-next/` gone (no .gitkeep exists).\n* **Delete** `epic-review.md` **outright** instead of moving it: its own prose says the alias is removed in 2.0.0, repo is at 3.2.x - dead shim. 24 files -> 23 moved.\n* Frontmatter: strip the `flow-next:` prefix from every shim's `name:` (e.g. `name: qa`), or remove `name:` entirely so the basename governs - implementer verifies which of the two renders cleanest on current Claude Code AND keeps Cursor's command names stable (Cursor may key display names off `name:`); prefer removing the field if both hosts derive from filename. Document the choice in the commit message.\n* Update `.cursor-plugin/plugin.json:19` `\"commands\": \"./commands/flow-next\"` -> `\"./commands\"`.\n* Update `scripts/install-cursor.sh` (comments ~L21-23, count-check glob ~L87) and `scripts/install-cursor.ps1` equivalents.\n* Update `scripts/ci/verify_cursor_install.py:41` (`base = root / \"commands\" / \"flow-next\"` -> `root / \"commands\"`).\n* Update `plugins/flow-next/tests/test_model_routing_scaffold.py:45` (`UNINSTALL` path).\n* Run `./scripts/sync-codex.sh` twice; mirror expected unchanged (only a comment at `scripts/sync-codex.sh:1487` references the old path - update the comment text).\n* Post-change grep sweep: `grep -rn \"commands/flow-next\" --exclude-dir=.git --exclude-dir=.flow` must return only historical CHANGELOG entries (leave those as-is).\n\n## Boundaries / non-goals\n\n* Do NOT change skill names or the skills surface - command shims only.\n* Do NOT rewrite historical CHANGELOG entries (lines ~691/1093/1114 reference the old path in dated prose).\n* Droid/Grok have no test harness here: flat `commands/` is the standard Claude plugin layout both consume, so doc-level verification suffices (platforms.md note).\n* fn-123 already landed on main and is included in this worktree; no rebase pending.\n* [flow-next.dev](<http://flow-next.dev>) / downstream walk happens at the batched release, not in this spec (maintainer workflow).\n\n## Strategy Alignment\n\nActive tracks served by this plan:\n\n* **Cross-platform parity** - one shim surface that renders correctly on Claude Code while keeping Cursor's manifest-driven commands working.\n\n## Decision context\n\n* Flatten + de-prefix beats \"keep nested dir and add an explicit `commands` key to `.claude-plugin/plugin.json`\": the docs define no per-plugin commands-path override for Claude Code discovery, and the nested dir exists only as a Cursor accommodation that a one-line manifest edit removes.\n* Delete epic-review shim now: self-declared dead since 2.0.0.\n\n## Acceptance Criteria\n\n* **R1:** On current Claude Code, flow-next commands render without duplicated prefixes (target `/flow-next:qa`); no `flow-next:flow-next` entries remain except the inherent plugin-prefix + `flow-next` management-skill pairing.\n* **R2:** Current Claude Code command-namespacing rules verified against docs/release notes; chosen fix justified against them (documented in Overview above; implementer re-confirms live menu rendering).\n* **R3:** Cursor commands keep working: manifest `commands` path updated in lockstep; `install-cursor.sh`/`.ps1`, `scripts/ci/verify_cursor_install.py`, and tests (`test_install_cursor_parity`, `test_cursor_plugin_surface`, `test_cursor_review_commands`) updated and green.\n* **R4:** `scripts/sync-codex.sh` run twice stays idempotent; Codex mirror unaffected or regenerated cleanly.\n* **R5:** No version bump; CHANGELOG entry staged under `## Unreleased`.\n\n## Early proof point\n\nTask fn-124-flatten-plugin-command-shims-fix.1 validates the core approach (flat dir + de-prefixed frontmatter renders `/flow-next:qa` on current Claude Code). If the menu still doubles, re-verify frontmatter `name` semantics before touching docs.\n\n## Requirement coverage\n\n| Req | Description | Task(s) | Gap justification |\n| -- | -- | -- | -- |\n| R1 | Clean menu names on Claude Code | fn-124-flatten-plugin-command-shims-fix.1 | *  |\n| R2 | Namespacing rules verified, fix justified | fn-124-flatten-plugin-command-shims-fix.1 | *  |\n| R3 | Cursor path + scripts + tests green | fn-124-flatten-plugin-command-shims-fix.1 | *  |\n| R4 | sync-codex idempotent | fn-124-flatten-plugin-command-shims-fix.1 | *  |\n| R5 | CHANGELOG Unreleased, no bump | fn-124-flatten-plugin-command-shims-fix.2 | *  |\n\n## References\n\n* `plugins/flow-next/commands/flow-next/` - 24 shims, uniform frontmatter (`qa.md:2` shows `name: flow-next:qa`)\n* `plugins/flow-next/.cursor-plugin/plugin.json:19` - Cursor commands path override\n* `plugins/flow-next/.claude-plugin/plugin.json` - no `commands` key (auto-discovery)\n* `scripts/install-cursor.sh:21-23,87`, `scripts/install-cursor.ps1` - path refs\n* `scripts/ci/verify_cursor_install.py:41,79,111` - command-dir assertions\n* `plugins/flow-next/tests/test_model_routing_scaffold.py:45` - UNINSTALL path\n* `scripts/sync-codex.sh:1487` - comment-only reference\n* `plugins/flow-next/docs/platforms.md:208,237,241` - stale path/count prose (\"22 wrappers\" was already stale; 23 after epic-review deletion)\n* `agent_docs/adding-skills.md:7` - authoring instruction for new shims (highest-priority doc fix)\n* Claude Code docs: skills \"How a skill gets its command name\"; plugins-reference; CHANGELOG v2.1.208/v2.1.216 name-semantics fixes",
    "url": "https://linear.app/gmickel/issue/FLOW-57"
  },
  "updated_at": "2026-07-22T15:17:30.004930Z"
}
