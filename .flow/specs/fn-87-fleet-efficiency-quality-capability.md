# Fleet efficiency + quality (capability-review backlog)

## Goal & context

A 6-reviewer fable capability+efficiency audit of the whole flow-next skill/agent fleet (fn-84 pivot: prose-squeezing was finding nothing because the evals saturated; the real headroom is architectural). This spec captures the ranked findings. **Two items already landed this session** (commits dd67cadf, 67611de5): the verification-spine correctness bugs (three-dot review diff, completion-review status write-back + portable awk, sync CROSS_SPEC) and the 5-scout opus→sonnet downgrade (A/B-verified). The rest are tracked here, ranked by impact÷cost, each grounded in a reviewer finding. Every change must VERIFY quality holds (eval harness re-run for behavior-preserving refactors; tests/reproduction for bugs; fable review for judgment changes) — the fn-84.5 design-review-first + post-run-QA discipline applies.

## Non-goals / boundaries

- NOT reviving the plan-sync skip-gate (fn-83 shelved it with cross-repo evidence) — the plan-sync item here is a MODEL downgrade pending a drift A/B, orthogonal to the skip-gate.
- NOT touching prospect Phase-1 snapshot reuse or land re-probe (the 2026-07-02 survey + reviewers ruled these correctly-bounded).
- NOT a prose-trim pass — these are capability/architecture/correctness changes.
- Each efficiency split must be gated behind LOUD sentinels (fn-81/82 gate-skeleton pattern), never silently dropped.

## Approach

Land in impact÷cost order, smallest-blast-radius first, each its own commit with quality-hold verification.

## Key files / interfaces

Per R-ID below. Central files: `plugins/flow-next/skills/flow-next-interview/SKILL.md`, `.../flow-next-impl-review/{SKILL,workflow-common}.md`, `.../flow-next-make-pr/workflow.md`, `.../flow-next-capture/`, `.../flow-next-prospect/workflow.md`, `.../flow-next-audit/`, `.../flow-next-strategy/`, `.../flow-next-qa/`, `.../flow-next-plan/steps.md`, `.../flow-next-plan-review/`, `.../flow-next-resolve-pr/` + `agents/pr-comment-resolver.md`, `.../flow-next-land/workflow.md`, `.../flow-next-pilot/`, `agents/plan-sync.md`, `plugins/flow-next/scripts/flowctl.py`.

## Acceptance criteria

### Correctness bugs (highest value/risk ratio — quality only improves)
- **R1:** impl-review `REVIEW_ID="${1:-}"` reads a non-existent bash positional in a tool call → the fn-74 per-task `review:` override silently falls back to the project default. Thread the actual `fn-*` token from the invocation into `REVIEW_ID` (workflow-common.md:36). Verify: a per-task override resolves.
- **R2:** impl-review Carmack rubric is hand-synced across 4 places and HAS drifted (rp has criterion #8 glossary gate; the flowctl codex/copilot/cursor prompt lacks it). Single-source the rubric (emit via `flowctl review-prompt --type impl` or a shared block); confirm all backends carry the same criteria.
- **R3:** impl-review fix-loop holes — `MAJOR_RETHINK` is a parsed verdict with no defined action (gets ad-hoc patched), `MAX_REVIEW_ITERATIONS` resets per invocation, and worker.md runs a contradictory unbounded outer loop. Route MAJOR_RETHINK → typed escalation; persist the round count in the receipt; delete worker's redundant outer loop.
- **R4:** completion-review codex/copilot/cursor prompt lacks RP's reverse-coverage (scope-creep) phase — scope-creep detection exists only on the macOS backend. Port the reverse-walk + UNDOCUMENTED_ADDITION/LEGITIMATE_SUPPORT/UNRELATED_CHANGE block into `build_completion_review_prompt` (flowctl.py).
- **R5:** land never reads the QA verdict receipt though pilot promises "the land gate acts on its findings"; a PR with `qa_outcome=NEEDS_WORK` + open P0s auto-merges on green CI. Add a Phase-2 gate: read `.flow/review-receipts/qa-<spec>.json`; NEEDS_WORK + open P0 → NEEDS_HUMAN.
- **R6:** work's standard worker-failure retry (phases.md:283) is unbounded/uncounted — autonomy's "never hang" has no loop-guard for non-delegation failures. Add a 2-strike counter → emit worker's typed BLOCKED escalation. Also: baseline check (worker.md:52) is a placeholder comment — make it run the spec's Quick commands + record red-baseline.
- **R7:** pilot's 2-strike auto-unready is silently reversed by tracker-authoritative readiness projection (backlog 1a re-blesses ready every tick; 1g clears the ledger) → infinite re-dispatch of a failing stage in unattended mode. Record readiness provenance (projection vs human), OR park an `ASKED` anchor on strike-2 so 1d durably skips it.

### Efficiency — always-loaded / per-invocation (verify quality holds via each skill's eval harness)
- **R8:** interview SKILL.md is 847 lines (~15k always-loaded tok); the default path uses ~⅓. Split to a ~300-line core + `references/{scope-passes,doc-aware}.md` + write-back templates, loaded behind loud gates; fold flag parsing into `scope resolve`. ~8k tok/invocation saved. Verify: re-run the `optimization/interview/` eval — quality holds.
- **R9:** impl-review loads ~5.4k tok of `--deep`/`--validate`/`--interactive` machinery + worker.md loads ~2.3k tok of codex-delegation, both unconditionally though default-OFF, EVERY task. Split behind gates (gate-skeleton). ~7.7k tok/task. Verify: default-path review still SHIPs on a fixture.
- **R10:** make-pr workflow.md is ~34k tok loaded whole every run (incl. every pilot tick); ~40% is Done-when restatements + triple-copied guardrails. Per-phase split via the phases.md index (spec-completion-review pattern). Verify: `optimization/make-pr/` E1-E10 hold body-equivalent.
- **R11:** capture pays ~26k mandatory load dominated by conditional paths (HTML lens, strategy-contradiction, tracker) + triple duplication; make phases.md genuinely on-demand, gate the conditional sections. Verify: `optimization/capture/` eval holds.
- **R12:** plan fan-out is depth-invariant (short bug-fix plans still dispatch practice/docs/github/flow-gap). Key the scout matrix on depth (short → repo+spec+memory+docs-gap; standard/deep → full). Needs a deliberate eval (the "run ALL scouts" anti-drift rule is intentional). Also: plan-review + plan backend-block dedup (~1-4k tok, 3-copy drift).
- **R13:** plan-sync model downgrade opus→sonnet — the highest-frequency opus agent (per task), but the only Edit-authority scout (silent stale-spec risk). Run a drift-fixture A/B (seeded rename + decision override + no-drift control), fable-judged, BEFORE flipping. Downgrade only if it holds.

### Quality — capability gaps (verify via eval harness + fable)
- **R14:** prospect's Phase-3 critique claims to run "blind, separate prompt" but the skill is sequential single-chat, so the critique sees the personas + focus hint it claims to exclude — the anti-sycophancy mechanism is theater (which is why it needs the 40% quota compensator). Dispatch Phase 3 as a fresh-context read-only Task subagent (snapshot + candidates only). NOTE: the `optimization/prospect/` eval already ran the critique as an isolated subagent, so it already validates the FIXED behavior.
- **R15:** audit re-investigates every entry every run even when the module hasn't changed; `mark-fresh`/`last_audited` plumbing exists but Keeps never stamp it and Phase 0 never reads it. Stamp Keeps + add a Phase-0 `git log --since=<last_audited> -- <module>` pre-filter → auto-Keep unchanged. O(all)→O(changed). Verify: `optimization/audit/` classification holds on changed entries.
- **R16:** strategy's update run detects zero real drift (staleness asked, never measured). Add a Phase-2.0 evidence scan: `specs --json` + `git log --since=<last_updated>` → map recent spec titles to tracks, surface "track X: 0 specs in 120d / 7 of 10 shipped map to no track / 2 match a Not-working-on item" into the routing question.
- **R17:** qa's "no SHIP without captured evidence" is prose-only; the receipt writer never checks the evidence dir. Make it structural: `QA_OUTCOME=SHIP` requires ≥1 evidence artifact per live-covered scenario, else force BLOCKED. Also: qa writes bug memory but never READS it — add a derive mapping "prior bug entries on touched surfaces → regression scenarios" + an error-path variant per write-path scenario.
- **R18:** plan ignores capture's `[user]/[paraphrase]/[inferred]` source tags entirely (severed pipeline — `[inferred]` acceptance criteria are planned as fact). Route `[inferred]` R-IDs through scout verification; unconfirmed → Open Questions. Also wire the dead `plan/examples.md` (439 lines, referenced by nothing) into Step 5, or delete it. Also: capture's evidence block is user-turns-only (misses agent-proposal+user-assent pairs) — permit paired evidence lines.
- **R19:** resolve-pr starves its resolvers — no spec / R-IDs / `knowledge/decisions/*` reach the pr-comment-resolver, so it reconstructs rationale from code archaeology and can `fixed` something contradicting a deliberate decision. Pass `spec_id` + spec path + decision ids as resolver inputs + a "read Decision Context before judging design-intent feedback" step. Also: orchestrator fetches `gh pr diff` once (Phase 4) instead of each resolver re-fetching.
- **R20:** plan-review feeds codex/copilot/cursor a stale hardcoded `CODE_FILES="src/main.py,src/config.py"` → cross-platform backends get near-zero real code anchors (catch spec-internal but not "contradicts the codebase"). Derive CODE_FILES from the spec's `## Key files / interfaces` section first, fall back to feature-map/git-grep. Also: plan-review never sees `.flow/memory` (pitfalls/decisions) — inject matching-module decision/pitfall titles.
- **R21:** make-pr body freezes at creation; no `--update` path, so after resolve-pr/land fix rounds the R-ID SHAs, Review-plan buckets, and blob links describe a diff that no longer exists — worst on the most-reviewed PRs. Add `--update` re-running Phases 1-3 against the open PR (`gh pr edit --body-file`), invocable by resolve-pr/land.
- **R22:** tracker-sync reconcile loads ~35-45k tok of instructions per dispatch (pilot backlog dispatches it every tick); add an `updatedAt <= lastSyncedAt` short-circuit BEFORE ref load (noop receipt, never open body-merge). Also: `list-relations` is N+1 (one dispatch per candidate) — batch it; extract the cold discovery ceremony (steps.md Phase 1) to `references/discovery.md` gated on `sync active:false`. Also: the promised `renderTaskChecklist` hook is a dangling stub (teams get no task-level board progress).
- **R23:** drive's verify is DOM-only — a flow that logs exceptions / 5xx's passes green (qa_verdict rests on this). Fold "console clean + no failed API requests" into every verify (tooling already on the default rung). Also: mandated full `snapshot -i` before every act — recommend semantic `find` / scoped snapshots for known targets (dominant token cost of a drive pass).

## Test / verification notes

Behavior-preserving refactors (R8-R12, R22) MUST re-run the affected skill's `optimization/<skill>/` eval and show no regression. Bugs (R1-R7) verified by reproduction + the flowctl unittest suite (`python3 -m unittest discover -s plugins/flow-next/tests`). Judgment/capability changes (R13-R21, R23) get a fable review; R13 + any model change gets an A/B quality-hold gate. flowctl.py changes port to the `.flow/bin/flowctl.py` dual-copy + regenerate the Codex mirror (`scripts/sync-codex.sh`). Docs: CHANGELOG `## Unreleased` + flowctl.md/skill references + downstream docs-site per landed item. Batched version bump.
