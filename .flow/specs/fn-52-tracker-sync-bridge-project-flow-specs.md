## Conversation Evidence

> user: "a the linear/jira/github sync stuff right? i already do linear manually in the workunits/sapient project ... some open questions here are would we also sync the tasks after the plan step, do we sync on spec complettion, make-pr, resolve-pr etc, ie. updating the mirror with comments etc"
> user: "how does openai symphony do it, i think they just use linear to start agents? check"
> user: "yes we should capture both but we should do linear first, then github"
> user: "with your spec stuff, although we shouldnt blindly copy what i did in the workunits project, for example we dont have explicit 'merge/sync' there i think"
> user: "the body text sync will be agentic via the agent harness so it can be two-way surely, also play the flows in your head, ie. either someone specs something in flow-next and syncs to linear or a PM/PO puts a vague spec/issue in linear that another PO/Dev syncs into flow-next and does a bunch of steps up to plan and it always gets synced back etc"
> [context: workunits-mvp/CLAUDE.md manual protocol — Linear canonical; Flow local; copy spec body 1:1 both ways; Linear owns backlog metadata/status/comments, Flow owns local task decomposition; attach evidence to Linear on completion. No conflict detection, no lastSyncedAt, no merge.]
> [context: Ray Fernando running-bug-review-board issue-trackers.md (Apache-2.0) — discovery ceremony (detect/surface/ask/never-assume), per-item Tracker/<type> + lastSyncedAt, push/pull/reconcile with field-level who-wins rules, enumerate-only helper scripts, createLocalForUntracked: ask.]
> [context: OpenAI Symphony — Linear board as finite-state-machine / control plane; daemon polls, claims issues, spawns Codex agents, opens PRs linked back. Mirror-image of flow-next (Symphony = Linear canonical + thin WORKFLOW.md; flow-next = spec canonical). Sources: openai/symphony.]

## Goal & Context
<!-- scope: business -->
<!-- Source-tag breakdown: 50% [user] / 35% [paraphrase] / 15% [inferred] -->

flow-next specs (`.flow/specs/<id>.md`) are markdown-of-record but isolated from where teams live (Linear / Jira / GitHub). Gordon already runs this sync by hand in the workunits/Sapient project (Linear canonical; Flow local; copy the spec body 1:1 both ways; attach evidence on completion) — but that manual protocol has **no real reconciliation**: no conflict detection, no `lastSyncedAt`, no merge. This spec automates a bridge as **projection, not coordination**: the spec stays the single source of truth and the quality layer; the tracker is a **co-editable mirror** for teams that must live in it. "Not coordination" means the tracker never drives flow state or spawns agents (Symphony's control-plane model) — it does NOT mean one-way. Body, status, and comments all sync **two-way**, and body reconciliation is **agentic** (the host agent does a semantic merge, which is exactly what flow-next's "host agent IS the intelligence" architecture is for). This unlocks the key flow: a vague PM/PO issue in Linear is pulled into flow, enriched through capture/interview/plan, and the enrichment **syncs back** to the tracker automatically. The design takes the *decisions* from the workunits practice and the *sync engine* (discovery ceremony, per-item `lastSyncedAt`, field-level reconciliation, surface-diffs-never-overwrite) from Ray Fernando's running-bug-review-board `issue-trackers.md` (Apache-2.0 — credited). **Linear-first** — where the lived demand is (the workunits team + the Symphony/Linear ecosystem) — **GitHub next**.

## Architecture & Data Models
<!-- scope: technical -->

A SKILL drives the sync workflow; thin flowctl plumbing provides atomic helpers — the canonical flow-next split (flowctl owns "enumerate / set-field / validate-config / atomic-write"; the skill, running on the host agent, owns "call the tracker API / reconcile / merge bodies / ask the user on genuine conflict"). Per-item sync state lives in spec metadata, written atomically by flowctl: the tracker id (dedupe key), `lastSyncedAt`, and a **merge-base snapshot** of the body at last sync (the 3-way merge ancestor). Three sync operations (push / pull / reconcile) run across three layers: body (two-way, agentic 3-way merge + format translation between flow's structured spec and the tracker's free-form issue), status/metadata (two-way reconciled), comments/evidence (two-way append). Per-tracker adapters are extensible (Linear first, GitHub next; Jira/Notion later). Canonical skill files use Claude-native tool names; `sync-codex.sh` rewrites for the Codex mirror.

**Implementation anchors (from planning research — repo-scout):**
- flowctl is one monolith `plugins/flow-next/scripts/flowctl.py` (~21k LOC). New command group follows the `config`-group pattern at `flowctl.py:21477` (parent parser + nested `add_subparsers` + leaf `.set_defaults(func=cmd_*)`).
- Atomic writes: `atomic_write` (`flowctl.py:1636`) / `atomic_write_json` (`flowctl.py:1658`). Sync-state sidecar = `.flow/specs/<id>.json` (existing); schema authored in `cmd_spec_create` (`flowctl.py:11059-11071`); field-setter template = `cmd_spec_set_branch` (`flowctl.py:11827`).
- Config defaults: `get_default_config` (`flowctl.py:1018`); `deep_merge` (`:1036`) **preserves** unknown keys under the tracker block (forward-compat, R1 — unset keys resolve to defaults, unknown keys round-trip untouched). Env>config>ASK resolution model = `cmd_review_backend` (`flowctl.py:4859`).
- Invocation: a NEW `/flow-next:tracker-sync` command (`plugins/flow-next/commands/flow-next/tracker-sync.md`, mirroring `sync.md`) wraps the new skill. This is DISTINCT from the existing `/flow-next:sync` (= plan-sync, `flow-next-sync` skill) — the two are documented side-by-side so users don't confuse them.
- Adapter payloads are **normalized structs** (transport-blind): `issue {tracker,type,id,identifier,title,body,status,priority,labels,url,updatedAt}`, `comment {id,author,body,createdAt,marker}`, `status {raw,normalized}`. Linear/GitHub adapters map their wire shapes to/from these so reconcile (.4/.5) never sees a transport detail.
- Tracker-key identity (R16, hybrid) is deterministic flowctl id work (fn-52.10): widen `is_spec_id` / `expand_bare_spec_id` to resolve `wor-17` / `wor-17.M`, and branch the `cmd_spec_create` (`flowctl.py:11059-11071`) id generator on origin (tracker-first → `wor-17-slug`; flow-first → `fn-NN` + alias). No rename path.
- Receipt additive-write idiom = `cmd_review_walkthrough_record` (`flowctl.py:18773`). Ralph-safe deferral sink = `append_deferred_findings` (`flowctl.py:18670`) → `.flow/review-deferred/<branch>.md`; give the sync receipt a distinct `type`/path so the review-receipt guard (`hooks/ralph-guard.py`) doesn't reject it.
- Transport ladder template = `skills/flow-next-drive/SKILL.md` + per-rung `references/*.md` (fn-51). Detection lives in the skill (probe `command -v` / MCP list / env), not in Python.
- Lifecycle integration anchors: work `phases.md:88` (first claim → In-Progress) + `:98` (done → status comment + evidence); make-pr `phases.md:132` (PR URL → attach to issue); capture writes via `spec create`+`set-plan`.
- Codex mirror: `scripts/sync-codex.sh` (var rewrites `:177-213`, Task→agent `:220-318`, AskUserQuestion→numbered-prompt `:406-630`, agent-role map `:103`) — needs a new per-skill block for the sync skill.

## Edge Cases & Constraints
<!-- scope: technical -->

- **Body merge is agentic, not deterministic** — relies on the host agent for semantic 3-way merge against the `lastSyncedAt` base. The merge-base snapshot is what keeps it reliable (real 3-way, not a 2-way guess); without a stored base the agent would over-surface false conflicts.
- **Format asymmetry:** the flow spec is structured (sections, R-IDs, source tags); the tracker issue body is free-form. Syncing flow→tracker renders/cleans the structure into a readable issue; syncing tracker→flow folds free-text edits into the right flow sections without inventing R-IDs/tags. The agent translates; it does not byte-copy.
- **Ralph / autonomous mode:** confident merges + conflict-free status/comment ops proceed unattended; genuine body contradictions and unresolvable status conflicts NEVER block — they queue for the human.
- **Transport is detect-best-available, not MCP-dependent:** the Linear adapter uses the MCP when registered, else the GraphQL API via `LINEAR_API_KEY`. So headless / CI / Ralph (no interactive MCP) falls through to the API key; only when BOTH are absent does the bridge no-op + receipt note (never crash). Neither transport is a hard prerequisite — same shape as fn-51's driver ladder.
- **Volume + rate limits:** pull queries batch and stay within tracker rate limits. Linear's binding limit is *complexity* (3M pts/hr/user, 10k/single-query), not request count — set explicit `first:` on every connection and order by `updatedAt` desc for the staleness query; back off on `RATELIMITED` (returned as HTTP 400, not 429). gh is headless via `GH_TOKEN`.
- **MCP tool-name drift:** when the MCP transport is in use, real Linear MCP tool names differ from BRB's illustrative ones — verify/pin at build (this environment's official server uses upsert verbs `save_issue` / `save_comment`, not `create_*`/`update_*`). The GraphQL transport is version-stable and needs no such check. Linear auth header asymmetry: GraphQL personal key = `Authorization: <key>` (NO `Bearer`); MCP = `Authorization: Bearer <token>`. Linear `id` (UUID) vs `identifier` (`WOR-17`) — store the UUID as the durable dedupe key; `commentCreate` needs the UUID, `issueUpdate` accepts either.
- **Echo-loop suppression (planning addition):** after a push, record the resulting tracker-side content hash; on the next pull, a hash match = flow's own echo → no-op, never a phantom conflict. `lastSyncedAt` advances only on a real reconciliation, never on a no-op pull.
- **First-sync / no-base bootstrap (planning addition):** flow-first push with no base ⇒ pure projection (fast-forward, never conflict), snapshot after. Tracker-first link of an existing issue ("grab issue X and spec it") ⇒ seed the base from the current tracker body, first pass is pull-only, then snapshot — so the first sync never surfaces the whole issue as a conflict.
- **`always-ask` × Ralph reconciliation (planning addition — resolves an R1/R11 tension):** the `always-ask` conflict-tiebreak default (R1) means "ask the human." In an autonomous/Ralph loop, "ask the human" resolves to *queue for the human* (the deferred-decisions sink, R11) — it NEVER prompts or blocks. Interactive mode prompts; autonomous mode queues. Same policy, surface-dependent delivery (mirrors fn-51's surface-aware ladder).
- **Unlink / teardown (planning addition):** unlinking a spec from its tracker issue clears the tracker id + `lastSyncedAt` + merge-base atomically and posts a one-line "detached" comment to the issue; a re-link re-seeds the base (does not resurrect stale state).
- **Missing / deleted linked issue (planning addition):** if a linked issue returns 404 / archived / deleted, the adapter does NOT crash and does NOT clear state or advance `lastSyncedAt`; it emits an `errored` (or `queued`) receipt and prompts/queues an unlink decision for the human. State is never mutated on a failed fetch.
- **Partial-batch failure (planning addition):** batch sync is **item-level** — a failure on one spec does not abort the run; the failed item gets its own `errored` per-item receipt and **no** state write (no `lastSyncedAt`/merge-base advance), and the run continues with the rest. State advances only for items that fully reconciled.

## Acceptance Criteria
<!-- scope: both -->

- **R1:** Sync config in `.flow/` records: an explicit **`enabled`** activation flag (default `false`) and `type` (default `null`, then `linear` first / `github` next). Because `get_default_config()` always returns a `tracker` block, **activation MUST be explicit and value-checked** — the bridge is active iff raw `tracker.enabled == true` OR raw `tracker.type ∈ {linear, github}`; absent / `null` / `""` / unknown ⇒ inactive (a default `type: null` persisted by an unrelated `set_config` write must NOT activate). Also: a `provenance` field (default `null`) recording who/what confirmed the config; a **per-event sync policy** whose keys are **nested to stay dot-path-safe** for `flowctl config get/set` — `{capture, interview, plan, work: {firstClaim, done}, makePr, resolvePr, completionReview}`, each leaf an enum `off | pull | push | reconcile | comment`, **all defaulting to `off`** (read as `tracker.perEvent.work.firstClaim`, `tracker.perEvent.capture`, etc.); a per-tracker block (team / project id, label / priority map); a `staleAfterHours` threshold (default documented) that drives `list-stale`; and a conflict-tiebreak default (`flow-wins` | `tracker-wins` | `always-ask`) for the rare unresolvable case. Forward-compatible (version field; unknown keys preserved, not dropped). [paraphrase]
- **R2:** Discovery ceremony — detect tracker signals (Linear MCP registered, `LINEAR_API_KEY`, `gh auth`, `*.atlassian.net`), surface present AND absent, ASK the user, never assume; write config only on confirmation, with provenance. The ceremony configures *which* tracker/team, NOT *where the user must start* — the entry point is flexible: author in flow and push, or link an existing tracker issue ad-hoc (e.g. "grab issue X and spec it") via an already-installed MCP. The bridge attaches sync state on link rather than imposing a fixed starting point. [user]/[paraphrase]
- **R3:** Grain — one flow spec maps to one tracker issue; tasks stay flow-local by default (optionally rendered as a checklist inside the issue body, NOT as sub-issues). [user]/[paraphrase]
- **R4:** Per-item sync state — spec metadata carries the tracker id (dedupe key), `lastSyncedAt`, and a body merge-base snapshot at last sync; flowctl owns the atomic write. [paraphrase]
- **R5:** Enumerate-only flowctl helpers list specs needing push (no tracker id) and stale specs (`lastSyncedAt` old / missing); the SKILL performs the actual tracker API calls, body merge, and reconciliation (flowctl enumerates / validates / atomic-writes; agent acts / merges / judges / asks). [paraphrase]
- **R6:** Body sync is **two-way**, reconciled **agentically** — not a text copy. The host agent reads both bodies + the `lastSyncedAt` merge-base (3-way: base vs flow-side vs tracker-side), folds non-conflicting changes from each side together (e.g. PM clarifies the goal in Linear while a dev adds acceptance criteria in flow → both preserved), and **translates between formats** (flow's structured spec / R-IDs ↔ the tracker's free-form issue body). Enrichment done in flow (capture/interview/plan) flows back to the tracker; tracker edits fold into the right flow sections. [user]/[paraphrase]
- **R7:** Status / priority / metadata sync is two-way + reconciled with explicit who-wins rules (tracker wins `done`/`verified`; flow wins `in-progress`; priority + `deferred`/`wontfix` surface to the user, never auto-changed). [paraphrase]
- **R8:** Comments / evidence sync is two-way append — flow lifecycle events post structured comments to the issue; tracker comments pull into the spec's sync log; appends only (no conflict). [paraphrase]
- **R9:** Only a genuine semantic contradiction the agent can't confidently resolve (both sides rewrote the same content to mean different things) is surfaced as a **focused, scoped** conflict for the human — never the whole body, never a silent overwrite. Interactive: show the merged body for confirmation before write-back. Ralph/autonomous: confident merges proceed; genuine conflicts queue for the human (R11). [user]/[paraphrase]
- **R10:** Lifecycle sync points — capture/interview: spec push/pull + merge; plan: no sub-issues by default; per task done: post a status comment + evidence (tests / PR) and move the issue In-Progress on first claim; spec-completion-review: flip the issue Done/verified + post verdict / R-ID coverage; make-pr: attach the PR link to the issue; resolve-pr: optional resolution comment. [user]/[paraphrase]
- **R11:** Ralph-safe — genuine body contradictions and unresolvable status conflicts NEVER block in an autonomous loop; they queue for the human (deferred-decisions list). Autonomous runs apply confident body merges + conflict-free status/comment ops. [paraphrase]
- **R12:** Each sync run emits a proof-of-work receipt (pushed / pulled / merged / updated / diverged / queued / errored / noop), recording every body merge for auditability + rollback, consistent with the existing receipt model. [paraphrase]
- **R13:** Linear adapter first, via a **transport-agnostic, detect-best-available adapter (mirrors fn-51's driver ladder) — neither transport is a hard dependency**: use the **Linear MCP** when it's registered (interactive convenience, OAuth handled, the dominant interactive mode), else the **Linear GraphQL API via `LINEAR_API_KEY`** (headless / CI / Ralph-safe, version-stable); when both are absent, no-op + receipt note. The agentic reconciliation is identical regardless of transport — the transport is a thin, swappable fetch/write detail. When the MCP transport is used, verify/pin tool names at build (they drift); the GraphQL path is stable. GitHub adapter next (via `gh`, headless-safe). [user]/[paraphrase]
- **R14:** Positioned and documented as projection / co-editable mirror (spec = source of truth; tracker mirrors but never drives the agent loop / spawns agents), NOT coordination; the projection-not-coordination + Linear-first call is recorded as a decision. [paraphrase]
- **R15:** Version bump + docs across THREE surfaces — and because this **changes the documented lifecycle flows** (a tracker dimension now rides on capture / interview / plan / work / make-pr), the flow-next.dev **guide / workflow pages need real edits, not just a changelog line**: (a) **repo** — a new sync reference, `flowctl.md` (sync helpers), `teams.md` (tracker bridge + Symphony contrast), CHANGELOG, root README, CLAUDE.md "where to look", `.flow/usage.md`; (b) **flow-next.dev** (`~/work/flow-next.dev`) — a **broad pass, not a single page** (own task, fn-52.11): a new tracker-sync docs page; a **hybrid id-model / handles explainer** (because R16 introduces a *second user-facing id scheme* — tracker keys as resolvable handles); **edits to every page that teaches spec referencing** (getting-started, command / usage reference showing `/flow-next:work <id>`, the teams / workflow walkthrough, examples that hardcode `fn-NN`) so the docs stop implying `fn-NN` is the only id form; **plus** the optional, clearly-marked opt-in tracker-sync touchpoints on each lifecycle step (the no-tracker path stays the documented default, shown unchanged) + changelog entry, run the `pnpm build` gate; (c) **mickel.tech** (`~/work/mickel.tech`) — the flow-next app page, **maintainer-only (Gordon updates post-merge; contributor PRs skip it)**. [inferred]
- **R16:** Identity / naming alignment — for **tracker-first / synced specs, the flow spec id adopts the tracker key as its handle** (canonical `wor-17-<slug>`; task handles `wor-17.M` resolve to canonical `wor-17-slug.M`) instead of a fresh `fn-NN`, matching the manual workunits convention (`wor-2-…`) so the two schemes don't diverge. For flow-first specs (no tracker, or pushed out), the flow id stays the handle and the tracker issue carries it back (a `flow:<id>` label or `[<id>]` title prefix). Either way each side references the other (flow stores the tracker id per R4) and titles stay 1:1 via body sync. **Resolution model — HYBRID (decided post-review):** the tracker key is a **first-class, resolvable handle**, not just a stored label — `work wor-17`, `plan wor-17`, `show wor-17`, tasks `wor-17.M` all resolve. flowctl widens `is_spec_id` / `expand_bare_spec_id` so every command inherits resolution (fn-52.10 owns this). The two id schemes coexist:
> - **Tracker-first** specs (pulled from a tracker issue) are **natively keyed** by the tracker key — canonical id `wor-17-slug`, tasks `wor-17-slug.M` (the existing `task_id = <spec-id>.N` rule), branch `wor-17-slug` — matching the manual workunits convention (`wor-2-…`). The bare forms `wor-17` / `wor-17.M` resolve as aliases exactly as `fn-52` / `fn-52.M` expand to the full-slug id today. The repo artifact mirrors the board; no second id.
> - **Flow-first** specs **keep `fn-NN`** (the sequential numeric handle is preserved per project convention) and, on push, store the tracker key in the single `tracker.identifier` field (R4) as a **resolvable alias** — so `work wor-99` resolves to the `fn-NN` spec without renaming it.
> - **Case:** `tracker.identifier` stores the display form (`WOR-17`); the canonical id derives from the lowercase key (`wor-17-slug`); alias resolution is case-insensitive. Enumeration sees tracker-key specs, but native `fn-N` allocation counts `fn-*` only (a `wor-9999` never bumps the next `fn`).
>
> **Ids NEVER change** — there is no rename-on-push (the disruptive path is explicitly rejected), and `spec set-title` on a tracker-linked spec updates the title only (it does NOT re-slug / rename the canonical id, branch, or files — that would desync the linkage); unlinked specs keep today's rename behavior. The native `fn-` prefix is reserved for the sequential scheme; tracker-key resolution is tried only after the `fn-` path misses. This is additive — it does NOT require the separate `fn-NN`-deprecation id-scheme change to land first; that change, when it comes, only governs *removing* `fn-NN`, not *also accepting* the tracker key. [user]/[paraphrase]

## Boundaries
<!-- scope: business -->

- **Fully opt-in** — with no tracker configured, the lifecycle skills (capture / interview / plan / work / make-pr) behave exactly as today: zero new steps, zero new prerequisites, project-management software not assumed. The bridge activates ONLY when raw config has `tracker.enabled == true` OR `tracker.type ∈ {linear, github}` (the discovery ceremony writes these) — NOT merely because `get_default_config()` returns a `tracker` block, and NOT for a stray persisted `type: null`. With `perEvent` keys all defaulting to `off`, even a stray `enabled=true` does nothing until events are opted in. It adds nothing to the base / zero-dep flow (preserves the STRATEGY zero-dep base contract). Tracker touchpoints appear in the guides as optional steps, never mandatory.
- NOT a daemon / control-plane like OpenAI Symphony (Linear-as-state-machine that *spawns* agents / drives flow state) — this is a projection bridge; flow-next stays spec-canonical and owns the agent loop. A Symphony-like trigger layer (a poller / webhook listener that watches tracker state and fires flow-next actions) is a **separable future addition on top of this bridge** — explicitly out of scope here, and a separate strategy decision because it would make the tracker a trigger, revisiting the projection-not-coordination line. The sync bridge (linkage + state + headless flowctl actions) is the substrate that makes it cheap to add later.
- NOT auto-creating tracker sub-issues per flow task (tasks stay flow-local).
- NOT auto-importing tracker-only issues into flow without asking (default "ask").
- NOT a deterministic text-merge engine — body reconciliation is agentic; only genuine contradictions surface for a human, never a silent overwrite.
- Jira / Notion adapters are out of scope here (Linear + GitHub only); the adapter shape stays extensible.
- Does NOT copy the workunits manual full-body-overwrite convention — replaced by the agentic 3-way reconciliation + merge base.
- **One tracker team / workspace per repo** — the bridge assumes a single team key so a bare `wor-17` resolves unambiguously; cross-workspace same-key collision (two Linear teams both keyed `WOR`) is out of scope and not disambiguated.
- **No rename-on-push** — existing spec/task ids, branches, and dep edges are never mutated when a spec is synced; the tracker key is added as a resolvable handle/alias, not a replacement of an existing id.

## Strategy Alignment

Active tracks served by this plan:
- **Spec-driven team patterns** — extends `docs/teams.md` with the tracker bridge + Symphony contrast; lets teams who already live in Linear adopt spec-driven flow *without leaving their tracker* (projection, not replacement). Strengthens flow-next's identity-as-methodology.
- **Ralph autonomous mode** — R11 keeps sync conflicts non-blocking (queue to the deferred-decisions sink), preserving the overnight loop; R12 sync receipts extend the proof-of-work receipt model that gates Ralph state transitions.
- **Cross-platform parity** — the new sync skill carries a Codex mirror via `sync-codex.sh`; canonical files stay Claude-native, single source of truth.

Consistent with the strategy's opt-in carve-out ("Opt-in convenience skills … may carry their own runtime prerequisites, but flowctl core never imports or requires them"): the bridge is opt-in and the zero-dep base install is untouched — so the external-service integration does **not** drift from the "everything in the repo / no SaaS" approach. The strategy's "Not working on → replacing Jira/Linear for human-only teams" is respected: this is a bridge *for agentic teams*, never a replacement.

## Decision Context
<!-- scope: both — conditionally substructured -->

### Motivation
Teams that already live in Linear (the workunits team; the Symphony/Linear ecosystem) need flow-next to project status outward AND pull vague issues in to enrich them — without giving up the spec as the quality layer — "Symphony, but with real specs + re-anchoring + receipts." Linear-first because that is where the real, lived demand is. Framed as projection (co-editable mirror, not control plane) so it complements, rather than contradicts, the strategy thesis that the spec carries the weight upstream.

### Implementation Tradeoffs
Body reconciliation is **agentic, not a deterministic merge engine** — a direct application of the CLAUDE.md "Architecture: agentic vs deterministic" rule ("the host agent IS the intelligence; default to skill-based, reach for deterministic flowctl only when there's a real reason"; its anti-pattern list literally names "building a deterministic fallback engine for when the LLM is unavailable"). A dumb 2-way text copy would lose data (forcing the one-way compromise); the agent instead does a semantic 3-way merge against the `lastSyncedAt` base and translates between flow's structured spec and the tracker's free-form issue. The split stays clean: flowctl owns the deterministic parts (enumerate, write `lastSyncedAt`, store the merge-base snapshot, validate config), the agent owns the judgment (the merge). Best practice from planning research tempers the pure-agentic path: reduce trivial cases deterministically first (only one side changed ⇒ auto-apply; both sides byte-identical ⇒ no conflict) so the agent only judges genuine both-sides-diverged cases, and gate the agent's merge with a structural check (no section silently dropped, non-conflicting additions from both sides present) before write-back. This is what makes the tracker-first enrichment flow work end-to-end: vague Linear issue → pulled into flow → fleshed out via capture/interview/plan → synced back. The entry point is intentionally loose — most users already have a tracker MCP installed and can just say "grab issue X and spec it," so the bridge attaches sync state on link rather than gating on a formal start. Only genuine contradictions surface (scoped, not whole-body); the receipt records every merge for audit/rollback; Ralph queues conflicts rather than blocking. The Linear adapter is **transport-agnostic** (MCP when present for interactive OAuth-free convenience, else GraphQL via `LINEAR_API_KEY` for headless/CI/Ralph) — neither is load-bearing, the same detect-best-available shape as fn-51's driver ladder, so the bridge isn't betting on an MCP being wired. GitHub uses `gh`. Linear ships first to match demand. **Identity (R16) is the hybrid model** — tracker-first specs are natively keyed by the tracker key (`wor-17-slug`, matching the workunits convention) so Linear-living teams see one id that mirrors the board; flow-first specs keep the stable `fn-NN` handle and gain a resolvable tracker-key alias on push. Both forms resolve through one widened resolver; ids are never renamed. This was chosen over (a) alias-only-no-resolution (left tracker teams with a two-id seam) and (b) rename-on-push (high-blast-radius id mutation across sidecars / branch / deps / references) — the hybrid is easiest for both cohorts (no-tracker users only ever see `fn-NN`; tracker users get the board id) while avoiding the rename breakage. Boundary: one tracker team per repo.

## Quick commands

```bash
# Smoke (after fn-52.1 — flowctl plumbing; exact verbs finalized at build):
.flow/bin/flowctl sync list-unsynced --json                 # specs with no tracker id
.flow/bin/flowctl sync list-stale --older-than-hours 24 --json   # default from tracker.staleAfterHours
.flow/bin/flowctl sync get-state fn-52-tracker-sync-bridge-project-flow-specs --json

# Invoke the bridge skill (distinct from /flow-next:sync = plan-sync):
#   /flow-next:tracker-sync

# Config + discovery (after fn-52.1/.2):
.flow/bin/flowctl config get tracker --json
.flow/bin/flowctl validate --spec fn-52-tracker-sync-bridge-project-flow-specs --json
```

## Early proof point

Task **fn-52.4 (agentic body reconciliation)** validated over a real **fn-52.3 Linear round-trip** is the core bet: push a flow spec to Linear, edit both sides, reconcile via 3-way merge against the stored base. It proves (a) format translation flow⇄tracker is idempotent for unchanged content, (b) non-conflicting two-sided edits both survive, (c) a genuine contradiction surfaces *scoped to one section*, not the whole body. **De-risk earlier:** fn-52.3 carries a transport round-trip spike (push body → pull back unchanged, no merge) as its FIRST acceptance criterion, so transport bugs (OAuth / MCP tool-name / GraphQL parity / rate limits) surface before the merge engine is built. If the merge over-surfaces false conflicts or loses data, re-evaluate the merge-base format + translation approach before building the GitHub adapter (fn-52.7) and lifecycle touchpoints (fn-52.6).

## Requirement coverage

| Req | Description | Task(s) | Gap justification |
|-----|-------------|---------|-------------------|
| R1  | Sync config block (tracker type, per-event policy, per-tracker map, tiebreak default, version) | fn-52.1 | — |
| R2  | Discovery ceremony (detect/surface/ask/never-assume; flexible entry; attach-on-link) | fn-52.2 | — |
| R3  | Grain: one spec ↔ one issue; tasks flow-local (optional body checklist) | fn-52.2 | — |
| R4  | Per-item sync state: tracker id + lastSyncedAt + merge-base snapshot (atomic) | fn-52.1 | — |
| R5  | Enumerate-only helpers (list-unsynced / list-stale) | fn-52.1 | — |
| R6  | Two-way agentic 3-way body merge + format translation | fn-52.4 | — |
| R7  | Status/priority/metadata two-way reconciled (who-wins) | fn-52.5 | — |
| R8  | Comments/evidence two-way append (dedup markers) | fn-52.5 | — |
| R9  | Scoped genuine-conflict surfacing; interactive confirm; Ralph queue | fn-52.4 | — |
| R10 | Lifecycle sync points across capture/interview/plan/work/make-pr/resolve-pr/spec-completion-review | fn-52.6 | — |
| R11 | Ralph-safe: conflicts queue, never block | fn-52.1 | — |
| R12 | Sync receipt (pushed/pulled/merged/updated/diverged/queued/errored/noop) | fn-52.1 | — |
| R13 | Transport-agnostic Linear adapter (MCP↔GraphQL↔no-op) + GitHub adapter | fn-52.3 (Linear), fn-52.7 (GitHub) | — |
| R14 | Positioning/decision record (projection-not-coordination) | fn-52.8 | — |
| R15 | Docs across 3 surfaces + version bump | fn-52.8 (repo docs), fn-52.9 (Codex mirror + plugin version), fn-52.11 (flow-next.dev broad pass: tracker page + hybrid id-model explainer + cross-page id edits) | mickel.tech maintainer-only (Gordon post-merge), in fn-52.11 |
| R16 | Identity/naming alignment — hybrid: tracker-key resolution + origin-branched generator (fn-52.10); link-ceremony id assignment + back-reference (fn-52.2); skill-side handle recognition so `plan`/`work` resolve not create (fn-52.6) | fn-52.10, fn-52.2, fn-52.6 | — |
