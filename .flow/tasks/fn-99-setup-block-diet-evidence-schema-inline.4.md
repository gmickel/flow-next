# fn-99-setup-block-diet-evidence-schema-inline.4 Post-trim eval gate, restoration loop, changelogs + docs-site staging

## Description
Gate + staging: post-trim eval, restoration loop, changelogs, docs-site. Satisfies R4 (gate half), R6, R7. Depends on .2 (harness + baseline) and .3 (trimmed content).

Scope:
1. EVAL GATE (R4): run the task-2 harness on the trimmed docs - full R13 matrix (both scenarios; minimal + full-block arms; sonnet + terra + haiku; 3 reps on discriminating cells). Gate = no regression vs the task-2 baseline on ANY graded dimension. On regression: restore the implicated content in a follow-up commit to task .3's surface, re-run the gate; never ship a regressed trim. Record post-trim ledger rows.
2. CHANGELOG Unreleased entry with before/after token counts (block + usage.md, chars/4) (R6).
3. Docs-site staging (R7): changelog entry per releasing.md format; review skills/setup.mdx (content description currency), flowctl/cli-reference.mdx lines 19/69 + skills/flow-next.mdx:29 (add the evidence schema example where --evidence-json is shown bare); verify orchestration/index.mdx's usage.md section anchor survived; `pnpm build` green; commit in the docs-site repo (push per maintainer handoff convention).
4. Full repo gate: unit + smoke green.
## Acceptance
- Post-trim ledger complete; no regression vs baseline on any dimension incl. haiku floor; any restoration documented (R4).
- CHANGELOG entry with before/after token counts (R6).
- Docs-site: changelog staged, bare --evidence-json mentions now show the schema, anchors verified, build green (R7).
- Unit + smoke green.
## Done summary
Ran BOTH post-trim gates. Block-only R13 matrix (28 runs): minimal arm 14/14 with the haiku floor intact - no regression on any graded dimension; amended full block 12/14 vs the 9/14 baseline (no cell regressed; sonnet multitask 1/3->3/3, haiku slugify 0/3->1/3). Completion-review P1 (block-only arms never exercised usage.md) closed by two new usage-aware harness arms - full-usage (current post-trim template, live) and full-usage-pretrim (frozen 5392-tok fixture arms/usage-pretrim.md from f564dab4~1) - and a dedicated usage-included gate (28 runs + 6-run extension on the one differing cell): every dimension identical across arms except slugify x haiku (trimmed 3/6 vs pre-trim 5/6), whose failure mode (verification-only micro-task done with empty commits while implementation tasks carry real shas) occurs in BOTH arms and whose teaching content survived the trim verbatim - not content-attributable, no restoration target; multitask deps + ordered reset + evidence 7/7 on both arms across all models. Verdict: no regression attributable to the trim; the trim ships.

Two grader fixes found by the batches, both re-graded from retained run dirs (never re-running cells): merge multi-line shim records (a multi-line --text arg crashed the line-per-record parse), and canonicalize legacy short ids (fn-1.1) in lifecycle events (flowctl resolves them; the raw-string match scored a fully-prescribed reset flow as none). Ledger sections for both gates ("post-usage-trim + block-evidence-amendment" and "usage-included trim gate", 2026-07-16, codex-cli 0.144.1 gpt-5.6-terra med / claude 2.1.210 sonnet+haiku) recorded in the harness README.

CHANGELOG Unreleased entry with before/after token counts (block 575->249 tok-equiv, usage.md 5392->1928) incl. both gate results; docs-site (~/work/flow-next.dev, commits 0fd37c2 + c935364, local only, not pushed): Unreleased changelog entry, evidence-JSON schema at the bare --evidence-json sites, read-on-demand loading-contract fix, pristine-upgrade currency line; pnpm build green. Codex impl-review (gpt-5.5): NEEDS_WORK r1 (dropped 2.14.0 heading - restored) -> SHIP r2 -> SHIP r3 on the usage-arm extension.
## Evidence
- Commits: 9dfecbedce4eca0b3eec3aca0936c8663ccb16ed, b33426d5cb8b32128a6dc2e229b4555ce88e626e, 99902d51346123918a3c8dedd659a3582e94b3e0, f2d331fe1464f61edc7f63cd51ac1cb813a65c40
- Tests: python3 -m unittest discover -s tests (1786 OK, skipped=2; green baseline and post-edit), bash plugins/flow-next/scripts/smoke_test.sh from /private/tmp (144/144, three runs: baseline, post-edit, post-usage-arm work), python3 -m unittest tests.test_token_budgets tests.test_setup_snippet_lockstep tests.test_dogfood_template_parity (5 OK, targeted post-usage-arm), guidance-eval block-only post-trim gate: 28-run R13 matrix - minimal arm 14/14 (no regression, haiku floor holds), amended full block 12/14 vs 9/14 baseline (no cell regressed), guidance-eval usage-included trim gate: 28-run matrix + 6-run extension, full-usage (trimmed 1928 tok) vs full-usage-pretrim (frozen 5392-tok fixture), same block - all dimensions identical across arms except slugify x haiku (3/6 vs 5/6) whose failure mode occurs in BOTH arms and is not content-attributable; verdict: no regression attributable to the trim, pnpm build in ~/work/flow-next.dev (65 pages, green, twice)
- PRs: