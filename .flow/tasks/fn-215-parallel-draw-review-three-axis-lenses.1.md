---
satisfies: [R1, R2, R9, R10, R12, R13, R14, R15]
---
# fn-215-parallel-draw-review-three-axis-lenses.1 flowctl fan-out plumbing: one reservation, three draws, merged receipt

## Description
**Touches:** plugins/flow-next/scripts/flowctl.py, plugins/flow-next/skills/flow-next-impl-review/references/impl-review-prompt.md, plugins/flow-next/skills/flow-next-impl-review/references/standalone-review-prompt.md, plugins/flow-next/tests/**, plugins/flow-next/scripts/flowctl_tracker/MANIFEST.json, plugins/flow-next/codex/**

Two-phase fan-out surfaced as TWO coordinator-visible CLI invocations, gated to the CODEX backend inside the shared pipeline (_backend_impl_review flowctl.py:42085+ is shared with copilot/cursor — gate on backend==codex via the registry; NEVER place fan-out in _dispatch_backend_review, which plan/completion reviews share; negative test pins copilot+cursor still make exactly one dispatch). PHASE ONE (dispatch invocation) reserves exactly one round (enforce_and_increment_review_cap:42196 wraps the whole fan-out; standalone still reserves none), then dispatches three concurrent draws — each the rendered prompt plus one axis line via the template layer (impl-review-prompt.md + standalone-review-prompt.md gain the axis placeholder; parity constants + SHA pins in test_review_prompt_template_parity/test_prompt_text_pinned update in the SAME commit with rationale) — and persists per-draw sidecar outputs WITHOUT finalizing, each draw under its own timeout (a hung draw fails that draw, not the round) with a one-line per-draw progress record on the reservation. Per-draw runners are side-effect-free: no record, no refund, no receipt writes; only the aggregate controller finalizes once (any verdict present) or refunds once (all draws verdict-less = transport failure, today's semantics). Coordinator merge happens between the two invocations; PHASE TWO (finalize invocation) consumes the merged input and atomically records: worst-wins merged verdict from the draws' tags (R9, incl. the all-findings-dropped NEEDS_WORK → NEEDS_HUMAN wedge escalation), a single valid v1 findings container with ordinals re-assigned 1..N across the union (closed allowlist untouched — no axis field on items; axis lives in coordinator prose), the merged receipt at the existing path (top-level = primary draw's session/model + draws[] array: axis, model, session_id, verdict, failed — R12), and the single round consumption. Per-draw raw outputs persist at paths carrying the reservation id (or a secure nonce for standalone) + axis, exclusive creation, atomic publication. Round 2+ (codex): resume the primary correctness session with lean resume DISABLED for the first post-fan-out round — inject the FULL merged container (full ratchet); every merged ordinal must appear in the round-2 dispatch prompt. The draw topology (count, per-draw backend/model specs) arrives as explicit arguments from the coordinator — flowctl never parses prose; cross-family = three explicit dispatch specs. Artifact hash once per merged round; cap counts merged rounds 1:1 (R13). rp/copilot/cursor untouched. Behavioral tests (new test_review_fanout + extend test_review_receipt_schema/test_review_convergence_cap): one-reservation both phases, side-effect-free draw runner, worst-wins, wedge escalation, partial-fail-open, all-fail single refund, draws[] schema, round-2 prompt contains every merged ordinal, path-collision (concurrent same-task + standalone). Optional phases (deep/validate/walkthrough) run once against the merged set post-merge — state the order in the pipeline. gen_tracker_manifest.py; sync-codex twice. Read fn-157 (visibility stub) before building the concurrent dispatch; fn-191/fn-198 sequencing per spec Decision Context.
## Acceptance
R1 (dispatch mechanics), R2, R9, R10, R12, R14, R15 satisfied at the flowctl layer; judge against the parent spec's criteria directly. Quick suites green including updated pins with rationale in the commit message; test_two_axis_audit_contract untouched and green.
## Done summary
Shipped the two-phase codex fan-out plumbing (R1 dispatch mechanics, R2, R9, R10, R12, R13, R14, R15): `flowctl codex impl-review-fanout` (phase one — ONE reservation wraps three concurrent axis draws via ThreadPoolExecutor, per-draw timeouts, side-effect-free runners catching BaseException, sidecar dir keyed by reservation id/nonce with exclusive creation + atomic publication, per-draw progress.log, write-ahead refund-intent journal so a coordinator death between phases replays as a transport failure with observed-sha provenance) and `impl-review-fanout-finalize` (phase two — worst-wins verdict from draw tags via the replay precedence, the all-findings-dropped NEEDS_WORK→NEEDS_HUMAN wedge escalation, milder-merged-tag mismatch stamped on the receipt, merged v1 findings container, merged receipt with additive draws[] array, single round consumption, re-invocable refusals via the mismatched-duplicate guard). Codex-only registry gate (`fanout_draws`); `_dispatch_backend_review` byte-untouched and source-pinned; copilot/cursor negative-tested to exactly one dispatch. Round-2 lean resume disabled after a fan-out receipt (full merged ratchet injected; every merged ordinal in the prompt — pinned). Axis placeholder in both prompt templates with parity constants + SHA pins updated same-commit with rationale; builder-signature pin and CLI-surface leaf list updated with rationale; `.flow/review-fanout/` added to the auto-managed gitignore. New 16-test behavioral suite + receipt-schema pins.

Review trail (conductor-owned host reviews, fresh read-only session-family reviewers): round 1 NEEDS_WORK (9 findings: SystemExit reservation leak P1 — reproduced live; dispatch-window durability; gitignore; verdict-contradiction; 5 P3s), round 2 NEEDS_WORK (2 missed guardrail pins caught by full-suite discovery + 3 fix-delta P3s; all 9 round-1 fixes verified), round 3 SHIP (all fixed, no new findings, full suite 4608 green). Fix cycle 1 also uncovered and fixed a pre-existing double-charge in the journal-replay path (counter read before replays). Implemented via grok-4.6 bridge staged A/B/C with two continuation workers (the first worker's watcher wedged on a pgrep pattern matching an unrelated grok process — pid-scoped watchers hereafter).

stage: impl-review - ran [3 rounds: NEEDS_WORK x2 -> SHIP] (model: claude-fable-5 fresh subagents, conductor-owned)
stage: plan-sync - skipped(config: planSync.enabled != true)
## Evidence
- Commits: 3d2a6bf5, c9a46448, f43cb232, d78a3f13, 805be1f1
- Tests: cd plugins/flow-next/tests && python3 -m unittest test_review_fanout test_backend_spec test_flowctl_surface test_review_receipt_schema test_review_convergence_cap test_review_convergence_journal test_prompt_text_pinned test_review_prompt_template_parity -q, python3 scripts/run_tests_parallel.py (4608 OK), uvx ruff@0.16.0 check ., ./scripts/sync-codex.sh x2 idempotent, python3 scripts/gen_tracker_manifest.py
- PRs: