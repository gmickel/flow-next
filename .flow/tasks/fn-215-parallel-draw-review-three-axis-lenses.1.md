---
satisfies: [R1, R2, R9, R10, R12, R13, R14, R15]
---
# fn-215-parallel-draw-review-three-axis-lenses.1 flowctl fan-out plumbing: one reservation, three draws, merged receipt

## Description
Two-phase fan-out interface in the codex impl-review pipeline (_backend_impl_review flowctl.py:42085+): PHASE ONE reserves exactly one round (enforce_and_increment_review_cap:42196 wraps the whole fan-out; standalone still reserves none), then dispatches three concurrent draws — each the rendered prompt plus one axis line via the template layer (impl-review-prompt.md + standalone-review-prompt.md gain the axis placeholder; parity constants + SHA pins in test_review_prompt_template_parity/test_prompt_text_pinned update in the SAME commit with rationale) — and returns per-draw raw outputs WITHOUT finalizing. Per-draw runners are side-effect-free: no record, no refund, no receipt writes; only the aggregate controller finalizes once (any verdict present) or refunds once (all draws verdict-less = transport failure, today's semantics). Coordinator merge happens between phases; PHASE TWO is one deterministic finalizer atomically recording: worst-wins merged verdict from the draws' tags (R9, incl. the all-findings-dropped NEEDS_WORK → NEEDS_HUMAN wedge escalation), a single valid v1 findings container with ordinals re-assigned 1..N across the union (closed allowlist untouched — no axis field on items; axis lives in coordinator prose), the merged receipt at the existing path (top-level = primary draw's session/model + draws[] array: axis, model, session_id, verdict, failed — R12), and the single round consumption. Per-draw raw outputs persist at paths carrying the reservation id (or a secure nonce for standalone) + axis, exclusive creation, atomic publication. Round 2+ (codex): resume the primary correctness session with lean resume DISABLED for the first post-fan-out round — inject the FULL merged container (full ratchet); every merged ordinal must appear in the round-2 dispatch prompt. The draw topology (count, per-draw backend/model specs) arrives as explicit arguments from the coordinator — flowctl never parses prose; cross-family = three explicit dispatch specs. Artifact hash once per merged round; cap counts merged rounds 1:1 (R13). rp/copilot/cursor untouched. Behavioral tests (new test_review_fanout + extend test_review_receipt_schema/test_review_convergence_cap): one-reservation both phases, side-effect-free draw runner, worst-wins, wedge escalation, partial-fail-open, all-fail single refund, draws[] schema, round-2 prompt contains every merged ordinal, path-collision (concurrent same-task + standalone). Optional phases (deep/validate/walkthrough) run once against the merged set post-merge — state the order in the pipeline. gen_tracker_manifest.py; sync-codex twice. Read fn-157 (visibility stub) before building the concurrent dispatch; fn-191/fn-198 sequencing per spec Decision Context.
## Acceptance
R1 (dispatch mechanics), R2, R9, R10, R12, R14, R15 satisfied at the flowctl layer; judge against the parent spec's criteria directly. Quick suites green including updated pins with rationale in the commit message; test_two_axis_audit_contract untouched and green.
## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
