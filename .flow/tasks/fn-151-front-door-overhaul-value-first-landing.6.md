---
satisfies: [R27, R28, R29, R30, R31, R32, R33, R34, R35, R36]
---
# fn-151-front-door-overhaul-value-first-landing.6 Cross-repo gate, boundary greps, and changelogs

## Description
Final gate across all three repositories: boundary greps, copy-rule sweep over everything the earlier tasks wrote, the docs-site changelog entry, and the visual and build verification that the spec's own criteria depend on.

**Size:** S/M
**Files:** `src/content/docs/releases/changelog.mdx` (flow-next.dev). Otherwise verification and targeted fixes across surfaces the earlier tasks touched.

### Approach

**Boundary greps. Both must produce no output.**

```bash
grep -ri "PSVI\|Velocity Index" ~/work/flow-next.dev/src ~/work/mickel.tech/app/apps/flow-next README.md plugins/flow-next/docs/
grep -riwf ~/.claude/flow-next-client-names.txt ~/work/flow-next.dev/src ~/work/mickel.tech/app/apps/flow-next README.md plugins/flow-next/docs/
```

The client-name pattern file is private and deliberately lives outside this repository. If it is missing, report that rather than skipping the gate.

**Testimonial integrity.** Every published quote must resolve to a public URL and ship verbatim, including the author's own punctuation. Re-verify the three URLs resolve. The curly apostrophe in the raydocs quote stays; it is the author's. The excluded X/Twitter set must not have reappeared anywhere.

**Copy-rule sweep** over every file the earlier tasks wrote. Check for: em dashes, curly quotes outside verbatim quotations, hedging qualifiers in front of capabilities that exist ("aims to", "designed to", "can help you", "we believe"), "not X but Y" constructions beyond the two grandfathered lines, and the banned-phrase list from task 1. The one sanctioned exception is the evidence-band close, where precision about what the paper does and does not show is deliberate and must survive.

**Docs-site changelog.** `src/content/docs/releases/changelog.mdx:8-10` has an `## Unreleased` section holding the placeholder "No unreleased changes yet." Replace it. Register is customer-facing and problem-first per `agent_docs/releasing.md`: lead with what was wrong for the reader and what changes now, upgrade actions first and imperative, numbers as outcomes rather than inventory, machinery in the technical tail. Apply the rejection test: hide the technical tail, and if a reader cannot say what became easier or clearer, rewrite it. No version bump, no `FLOW_NEXT_VERSION` change.

**Verification the spec depends on.**

- flow-next.dev: `pnpm check` and `pnpm build`. Then walk the `DESIGN.md` Visual QA Checklist including the mobile, keyboard-focus, and video-poster steps task 2 added. Open search on both the landing and a docs page; search `ralph` for readable results and `gf` for a clean empty state.
- Landing at desktop and mobile widths: no horizontal overflow, keyboard focus visible, no dead viewport between the harness strip and the following section, video band plays or shows its poster.
- Decay plot: confirm it renders complete with JavaScript disabled, and that the draw-on does not run under `prefers-reduced-motion`.
- mickel.tech: `bun run build` and `bun x biome check .`.
- flow-next repo: every relative link in `README.md` resolves. Run the repository's own offline docs linkcheck if reachable locally.
- Nav parity: the slug-set diff between `src/lib/site.ts` and `astro.config.mjs` shows only `install` and `introduction`.

**Cross-repo consistency check.** The six outcome headings must be byte-identical between the landing and the README. The canonical harness-tiering sentence must read the same in `platforms.md`, the README prose, the README platforms table, `introduction.mdx`, and the mickel.tech platforms section. Diff them rather than eyeballing.

**Evidence.** Record all three commit SHAs in the done summary and in the evidence JSON `commits` array, each prefixed with its repository name, since the schema is shaped for one repository.

### Investigation targets

**Required:**
- `agent_docs/testimonials.md` - the boundary gates and the verified quote pool
- `agent_docs/releasing.md` - the docs-site changelog register and format
- `src/content/docs/releases/changelog.mdx:1-12` - the Unreleased placeholder to replace
- `DESIGN.md` Visual QA Checklist - as updated by task 2

**Optional:**
- `.flow/specs/fn-151-front-door-overhaul-value-first-landing.md` - the full R-ID list to check coverage against

### Key context

This task is the only place the three repositories are checked against each other. Earlier tasks each verified their own repository; nothing before this point catches a heading that drifted between the landing and the README, or a tiering sentence that ended up phrased four different ways.

No version bump anywhere. No `FLOW_NEXT_VERSION` change. Changelog entries stay under `## Unreleased` in both the repository and the docs site.

## Acceptance
- [ ] Both boundary greps exit clean; a missing client-name pattern file is reported, not skipped
- [ ] All published testimonial URLs resolve; quotes are verbatim including the author's punctuation; the excluded set has not reappeared
- [ ] Copy-rule sweep is clean across every file the earlier tasks wrote, with the evidence-band close as the one sanctioned exception
- [ ] Docs-site `## Unreleased` placeholder is replaced with a problem-first customer-register entry that passes the hide-the-tail rejection test
- [ ] `pnpm check` and `pnpm build` pass; the DESIGN.md Visual QA Checklist is walked including the new mobile, keyboard-focus, and video-poster steps
- [ ] Search works on both the landing and a docs page; `ralph` returns readable results and `gf` a clean empty state
- [ ] The decay plot renders complete with JavaScript disabled and does not animate under `prefers-reduced-motion`
- [ ] `bun run build` and `bun x biome check .` pass for mickel.tech
- [ ] Every relative link in the README resolves
- [ ] Nav slug-set diff shows only `install` and `introduction`
- [ ] The six outcome headings are byte-identical between the landing and the README
- [ ] The canonical tiering sentence reads identically across platforms.md, README prose, the README table, introduction.mdx, and mickel.tech
- [ ] No version bump and no `FLOW_NEXT_VERSION` change anywhere
- [ ] All three commit SHAs recorded in the done summary and evidence JSON, each prefixed with its repository


## Done summary
Ran the final cross-repo gate for fn-151: both boundary greps clean, testimonial URLs re-verified against their live sources, copy-rule sweep over every line the earlier tasks wrote (only the two sanctioned raydocs curly apostrophes and one boundary-deferred platforms.md em dash remain), the six outcome headings diffed byte-identical between the landing and the README (md5 268538da), and the canonical harness-tiering sentence diffed identical across all seven statements after removing the one divergent restatement found in the mickel.tech page metadata. Authored the docs-site `## Unreleased` changelog entry in the customer register, added the missing first-class plain-language invocation statement to the landing, restored the author's own sentence-final punctuation on the Michalina quote, and corrected the flow-next.dev nav-check description to name all three DocsRail top-links. Gates green in both buildable repos; the README's 495 lines are recorded as a bad line-count estimate rather than a missed requirement, with the reasoning in the evidence.
## Evidence
- Commits: flow-next.dev: e440b930fdf55356b4331679ed0da625ff54a850, mickel.tech: d3f6426ca4358cb550331fb34d7277f1dff26ecf, flow-next: 1ed72360e007adfda79d9c582b097b39e6a9c608
- Tests: baseline: green (pre-edit, all three workspaces) -> flow-next.dev pnpm check 0 errors/0 warnings/0 hints across 15 astro files + pnpm build 77 pages exit 0; mickel.tech bun run build exit 0 + bun x biome check . exit 0 (206 files), cd /Users/gordon/work/fn151-band && pnpm check && pnpm build -> PASS (exit 0; 0 errors, 0 warnings, 0 hints across 15 astro files; 77 pages built; re-run post-edit and again post-commit, green all three times), cd /Users/gordon/work/fn151-mickel && RESEND_API_KEY=<placeholder> RESEND_FROM=<placeholder> bun run build -> PASS (exit 0; /apps/flow-next prerendered static; compiled successfully), cd /Users/gordon/work/fn151-mickel && bun x biome check . -> PASS (exit 0; 206 files, no fixes applied; lefthook pre-commit ultracite fix also clean), boundary gate 1: grep -ri 'PSVI|Velocity Index' <band src> <mickel app/apps/flow-next> README.md plugins/flow-next/docs/ -> CLEAN (exit 1, no output; re-run post-edit and post-commit), boundary gate 2: grep -riwf ~/.claude/flow-next-client-names.txt <same surfaces> -> CLEAN (exit 1, no output). Pattern file present at ~/.claude/flow-next-client-names.txt (6 patterns), not skipped, client-name repo-wide sweep (tracked files, flow-next): 1 hit at CHANGELOG.md:928, pre-existing (commit c6df8cc0, 2026-07-02), matches three microsite dist filenames in an old downstream-walk line, already public. Zero hits in the added lines of all three fn-151 diffs, R42 outcome-heading byte diff: extracted 6 titles from index.astro outcomeCards vs 6 bold headings from README '## What you get' -> diff empty, md5 268538dae613964d51f4386c8509dc04 on both; also byte-identical to the spec's canonical wording, R26 tiering diff: 7 occurrences of the canonical sentence across README:78 (tenets table), README:443 (prose), platforms.md:3 (canonical home), introduction.mdx:144, page.tsx:55/192/1086 -> all byte-identical; 1 divergent restatement found at page.tsx:31 (Next.js metadata.description) and fixed; 0 divergent restatements remain, R39 cross-repo anchors: /install/#cursor, /install/#team--org-wide-deployment-claude-code-managed-settings, /install/#xai-grok-build, /cookbook/#compose-beyond-the-pipeline, /teams/tracker-sync/#linear-diffs--review-the-pr-inside-the-issue -> all five heading targets present in the band worktree, README relative links: lychee --offline --root-dir . README.md -> 112 total, 79 unique, 56 OK, 0 errors, repo offline docs linkcheck (CI gate command, absolute --exclude-path so the plugins inputs actually resolve): 968 total, 386 unique, 827 OK, 4 errors -> all 4 pre-existing in skills/flow-next-capture/references/html-lens.md and skills/flow-next-interview/references/write-back.md, untouched by fn-151 (last touched 2026-07-04 and 2026-07-30). With CI's relative --exclude-path values lychee 0.24.2 reports 'No files found' for the three plugins inputs, so CI never sees them, nav slug-set diff (spec Quick command): astro-only slugs = first-30-minutes, install, introduction -> all three are hardcoded DocsRail top-links; flow-next.dev CLAUDE.md check description corrected to name all three (was naming two), copy-rule sweep over every added line in all three repos (em dash, en dash, curly quote, hedging, banned-opener/dead-transition/engagement-bait/tech-marketing lists, negative parallelism): 3 findings, all sanctioned -> README.md:97 and index.astro:124 curly apostrophes (raydocs verbatim, author's punctuation) and platforms.md:108 em dash (pre-existing, long-tail file the spec Boundaries defer), R27 em-dash census: README.md 0, index.astro 0, introduction.mdx 0, why-flow-next.mdx 0, install.mdx 0, EvidenceBand.astro 0, proof/evidence.mdx 0, mickel page.tsx 0, mickel lib/apps.ts 0, testimonial integrity: gh api issues/comments/3734228766 + gh pr view 95 + gh issue view 4 -> all three resolve; quotes are contiguous verbatim substrings of the live sources; raydocs curly apostrophe preserved; landing Michalina quote given back its sentence-final period to match agent_docs/testimonials.md and the README; excluded X/Twitter set (7 handles + names) absent from all three repos, R25 counts: 28 skills directories = 'all 28 skills' in the README reference tail; 21 agent files = platforms.md:137 and subagents/overview.mdx:8; zero numeric recipe counts remain anywhere; zero skill/agent/command counts on the landing or the mickel.tech page, R35 no bump: plugin.json 3.9.0 unchanged, FLOW_NEXT_VERSION 3.9.0 unchanged, 0 manifest files in the fn-151 diff; repo CHANGELOG '## Unreleased' above 3.9.0 (task 3) and docs-site '## Unreleased' entry (this task), DOM verification, flow-next.dev preview (pnpm preview 127.0.0.1:4399) via agent-browser 0.27.0 computed styles + geometry; NO screenshots claimed (the screenshot/paint path returns blank frames on this machine, confirmed: requestAnimationFrame never fires in either the MCP browser pane or agent-browser headless/headed, and document.visibilityState is 'hidden' in the pane), R33/R34 desktop 1280x900: documentElement.scrollWidth 1265 <= 1280; scroll-container-aware element scan -> 0 elements overflowing outside a scroll container (body overflow-x is hidden, so the element scan is the meaningful check per DESIGN.md); gap between .teams (harness strip, ends 956) and .evidence (starts 956) = 0px, no dead viewport, R34 mobile 390x844: landing scrollWidth 375, 0 real overflow (4 flagged spans are inside a <pre> scroll container); /introduction/ and /install/ 0 real overflow (all flagged nodes inside expressive-code <pre> scroll containers); /proof/evidence/ 0 flagged nodes at all, keyboard focus: tabbed the first 18 interactive elements plus one element in each of the evidence (dark), features, breadth, paths, notable-updates, quickstart and pipeline bands -> focus ring visible on every one (outline auto 1px on both ink and paper grounds), R33 video band: /videos/pipeline-hero.mp4 HTTP 200 (5,170,530 bytes, video/mp4, range request 206) and /videos/pipeline-hero-poster.jpg HTTP 200 (77,924 bytes); element reports readyState 4, error null, videoWidth 1920x1080, poster set, autoplay+muted+loop+playsinline, plus a static <img> poster fallback -> plays or shows its poster, R43 decay plot drawn by default: served landing HTML contains zero stroke-dash state and home-evidence.css declares no stroke-dasharray/stroke-dashoffset, so the chart is complete with JavaScript disabled; role=img with an aria-label describing both trends; exactly two numeric annotations (0.68, 0.31), R6 reduced motion: agent-browser set media reduced-motion + reload -> matchMedia matches, plot class stays 'decay-plot' (never armed, data-armed absent), no inline dash state, computed stroke-dasharray none, annotations opacity 1, transitions none; document.getAnimations() empty (no motion on the landing beyond the plot and the video), draw-on trigger NOT observable on this machine: requestAnimationFrame never fires and a synthetic IntersectionObserver with the component's own options never delivers, in the MCP browser pane and in agent-browser headless and headed. Armed state verified (data-armed, dasharray set, annotations opacity 0); the intersect->draw transition rests on task 1's recorded observation plus code reading, and is NOT claimed as verified here, search: modal opens on the landing and on /proof/evidence/; 'ralph' -> 42 results with readable titles and excerpts on both; 'gf' -> 5 low-relevance matches from pre-existing untouched pages (troubleshooting, maintenance-factory, visual-aids, map, changelog; Pagefind tokenizing '-g' fragments in code blocks), rendered cleanly with no error state; a guaranteed-absent term returns 'No results for ...' with 0 results and no error, no console messages and no page errors on the landing; 33 resource requests, none failed (the single status-0 entry is the video element's own media fetch, verified 200 by curl), docs-site changelog entry renders: h3 'Front doors that lead with the problem' under h2 'Unreleased', bold one-liner, <details>/<summary>Detail</summary>, 10 paragraphs, MDX markdown inside the disclosure rendered (links to /proof/evidence/, /introduction/, /install/ and a prefers-reduced-motion code span); right-sidebar TOC shows Unreleased > Front doors ... > Latest > 3.9.0 ..., R37 DESIGN.md Visual QA Checklist walked in full: pnpm check/build, preview, homepage 1280, docs page 1280 in the next tab, homepage and docs at 390, element-level overflow, tab focus rings on dark and paper, video plays-or-poster with readyState/paused/error inspected, no console errors, no failed assets, search on both surfaces, ralph and gf queries, rail/version/nav parity, version accurate in site.ts, GitHub and mickel.tech links present, README length judgment (finding 2): 495 source lines, of which 7 collapsed <details> blocks span 106 lines that render as 7 summary lines, so the rendered page is about 396 line-equivalents. Spine is correct and adoption/breadth proof lands at line 83 (16.8%, first-quarter boundary is line 123). '## Where to look' sits at line 402 (81.2%), 33 lines, in the reference tail past the narrative, and was deliberately NOT demoted: collapsing it breaks find-in-page on GitHub for the contributor/agent audience that uses it while saving scroll the 30-second evaluator never performs, and the Commands treatment does not transfer because the catalog link that would replace the table is already cited on the following line. R21's 'roughly 360 lines' is recorded as a bad estimate, not a missed requirement; no content was deleted to chase it
- PRs: