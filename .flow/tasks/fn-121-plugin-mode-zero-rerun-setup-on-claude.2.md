---
satisfies: [R4, R5, R6, R7, R8, R9, R11, R13, R14, R15, R16, R17, R18]
---
# fn-121-plugin-mode-zero-rerun-setup-on-claude.2 setup plugin mode: slim snippet, pre-check rewrite, codex guard, repo docs

## Description
Add plugin mode to setup, ship the slim versioned snippet, rewrite the pre-check across all carrying skills, guard the Codex mirror, and update every in-repo doc. Size: L (prose-heavy but each edit is small and patterned).

**Context**: Read spec Architecture (mode question, snippet content, pre-check rules, consented switching) + Edge Cases + R4-R9/R13-R17 first. **fn-114 landed before this task**: its docs truth-up touched platforms.md / flowctl.md / CLAUDE.md checklist / CHANGELOG / README / local-dev.md and added `test_ralph_docs_truth` prose-pin tests - where your edits move pinned text, UPDATE the pins (never delete a pin to make it pass). Repo-scout anchors: setup workflow.md Step 2 at :56-74 (insert mode question after it; plugin branch skips Step 3 + Step 4 copies; Step 4a SPEC.md offer still runs), snippet engine `flowctl setup-block apply/resolve` (flowctl.py:2296/2363), FLOW_SETUP_ASK variants (12 byte-identical e.g. work SKILL.md:24-54; 3 with mode:autofix - capture:125-155, memory-migrate:87-117, audit:88-118; tracker-sync:30-61 with DISPATCH=forked; tracker-sync ALSO gates in steps.md - update both), notice-only variants found by grep "Pre-check: Local setup version".

**Work**:
1. NEW template `skills/flow-next-setup/templates/claude-md-snippet-plugin.md` per R5: exact EXISTING markers `<!-- BEGIN FLOW-NEXT -->`/`<!-- END FLOW-NEXT -->` (the setup-block engine matches those exact standalone strings - do NOT parameterize the marker); schema version as internal sentinel `<!-- flow-next:snippet:v1 -->` on the first line inside the block; bare flowctl; both pull directives; no-TodoWrite; evidence JSON; spec-create lines; fallback line "flowctl not found: run /flow-next:setup"; under 300-token budget - add to test_token_budgets.
2. NEW flowctl subcommand `setup-mode set plugin|copy --json` (spec API Contracts): the ONLY stamp path. `plugin` refuses (exit 1, listing exact failures) unless CLAUDE.md carries the BEGIN/END FLOW-NEXT block with internal `flow-next:snippet:vN` sentinel AND all copy artifacts (`.flow/bin/` launcher trio, `.flow/templates/spec.md`, `.flow/usage.md`) are absent; success = atomic stamp. `copy` stamps unconditionally. Expected sentinel N = flowctl constant shared with the pre-check text. Dual-copy refresh (.flow/bin/flowctl.py; scripts/ralph/flowctl.py only where ralph-init ran - not this repo post-fn-114) after the flowctl.py change.
2b. Setup workflow.md: mode question (platform==claude only) after Step 2; plugin-mode branch = flowctl init + snippet via the UNMODIFIED setup-block engine into CLAUDE.md (AGENTS.md optional secondary), then `flowctl setup-mode set plugin` as the LAST step - Skip / failed write / unresolved customization simply never reach it (and it would refuse anyway). Mode-switch TRANSITION TABLE (R13): copy-to-plugin enumerates the copy artifacts, asks before removal (`git rm` tracked / listed `rm` untracked; modified tracked files surfaced, never force-removed); decline/partial/failed cleanup = remain copy mode + print exact remaining paths (the stamp command's refusal enforces this even on prose error). Downgrade = copy-mode path writes copies + `flowctl setup-mode set copy`.
3. Pre-check rewrite in ALL carrying skills (all variants, preserving each variant's extra autonomy markers): read `setup_mode`; when "plugin" skip version compare, compare the CLAUDE.md internal sentinel `flow-next:snippet:vN` against the plugin's expected constant (single source: define once in the shared pre-check block text); on drift ask ONE consented marker-bounded refresh (mirror the usage.md Keep/Overwrite ask shape); autonomy markers suppress to stderr note (R14). ralph-init keeps the version ask in BOTH modes (R6 exemption). Copy-mode path byte-unchanged.
4. `model-routing-snippet.md`: volatile pointer switches to `flowctl usage` phrasing (stable policy + user table untouched). fn-115 SHIPPED in 3.0.0: `models.roles` config + 6e refresh ceremony + Step 7's deterministic line transform over this snippet's probe sentinels (workflow.md ~740) - preserve the sentinel grammar; the ceremony and the plugin-mode branch must compose (6e runs in both modes).
5. sync-codex.sh: NEW generation TRANSFORM + paired guards (fn-100 pattern). Transform: exclude `claude-md-snippet-plugin.md` from the mirror; strip/replace the plugin-mode setup-workflow branch with copy-mode-only prose (deterministic sed/awk). Guards: negative (no plugin-mode / bin-PATH-injection phrases, no slim template file in mirror) AND positive (copy-mode branch text retained). A guard without the transform guarantees sync failure - build the transform first. Run twice, commit mirror diff.
6. Uninstall docs (commands/flow-next/uninstall.md + skill if present): plugin-mode branch (R15).
6b. NEW tests (R16): `test_precheck_mode_contract.py` - for every grep-inventoried pre-check carrier assert plugin-mode branch present (setup_mode read + CLAUDE.md sentinel compare), that variant's autonomy markers preserved, copy-mode block unchanged per variant, ralph-init has NO plugin-mode silence; template-shape assertions for the slim snippet (exact engine markers, internal sentinel, no `.flow/bin/` strings). `test_setup_mode_stamp.py` - state machine of `flowctl setup-mode set`: plugin refuses on missing block / missing or stale sentinel / each present copy artifact (failures listed); succeeds only clean + atomic write; copy unconditional; artifact enumeration matches spec.
7. Repo docs (R8, R9), docs-gap anchors: README.md:127-131 "After every update" + :372 setup row + install section; docs/platforms.md:144/:173/:210; docs/flowctl.md (usage entry near `### detect` ~:120); docs/orchestration.md:161/:220-221 mode-qualify `.flow/usage.md` claims; docs/troubleshooting.md:14/:146-151; docs/architecture.md:133; docs/spec-template.md:85; agent_docs/adding-skills.md:92 caveat; agent_docs/local-dev.md note; CHANGELOG `## Unreleased` bold-title entry ending "No version bump (batched)".
8. Verify fn-96 closed w/ superseded banner (R11 - done at capture, verify only).
9b. Two-layers explainer (R18, repo half): `docs/orchestration.md` gains a leading "Two layers of steering" section - session steering (prompts/per-task pins, ephemeral, top precedence; CLAUDE.md pipeline prose steers the agent which feeds explicit values downward) vs machinery steering (config resolved by plumbing that never reads prose; what pilot/Ralph and unattended gates use). Include the precedence chain (prompt > per-task/per-spec pin > env > models.roles > registry baseline) and the "implement via grok-4.5 and review with sol just works, persists nothing" example. Human-readable, no internals dump.
9. Contributor doc (R17): NEW `agent_docs/setup-modes.md` - mode taxonomy (plugin/copy/legacy-absent), per-host resolution chains (flowctl, usage guide, spec template), invariants (no silent tracked-file writes; setup-block markers are EXACT strings with the version sentinel inside; every sync-codex guard pairs with a generation transform; plugin mode Claude-Code-only until probe-verified elsewhere), pre-check contract (variants, autonomy suppression, ralph-init exemption), the `setup-mode set` commit-point state machine, and what canonical skill prose may/may not assume (FLOWCTL preambles never change; bare `flowctl` only in the plugin-mode snippet, never in skills). Reference it from repo CLAUDE.md: one "Where to look" table row + one Cross-platform-checklist line - CLAUDE.md stays lean, the doc carries the detail.

**Files**: plugins/flow-next/skills/flow-next-setup/{workflow.md, templates/claude-md-snippet-plugin.md (new), templates/model-routing-snippet.md}, all pre-check-carrying SKILL.md/steps.md files (grep-inventoried, ~16-21 files), scripts/sync-codex.sh, plugins/flow-next/commands/flow-next/uninstall.md, plugins/flow-next/tests/test_token_budgets.py, README.md, plugins/flow-next/docs/{platforms.md, flowctl.md, orchestration.md, troubleshooting.md, architecture.md, spec-template.md}, agent_docs/{adding-skills.md, local-dev.md}, CHANGELOG.md, plugins/flow-next/codex/** (regenerated).
## Acceptance
- R4: setup mode question exists (platform==claude only); plugin-mode branch writes zero copies; CLAUDE.md slim block is the commit prerequisite (no stamp after Skip / failed write / unresolved customization); snippet-first stamp-last; copy-mode path byte-equivalent (diff the workflow against main for the copy branch).
- R5: `claude-md-snippet-plugin.md` uses the exact existing BEGIN/END FLOW-NEXT markers + internal `<!-- flow-next:snippet:v1 -->` sentinel, both directives, fallback line, no `.flow/bin/` path; test_token_budgets asserts its 300-token budget.
- R6: grep-inventoried pre-check list documented in the commit body; every variant handles setup_mode==plugin (silent when CLAUDE.md sentinel current, consented refresh on drift); ralph-init keeps version ask both modes; tracker-sync updated in BOTH SKILL.md and steps.md.
- R7: sync-codex.sh twice = no diff; transform excludes slim template + strips plugin-mode prose; paired guards green (absence AND copy-mode retention); all existing guards green.
- R16: `test_precheck_mode_contract.py` AND `test_setup_mode_stamp.py` green - per-carrier plugin-branch/autonomy-marker/copy-mode-unchanged assertions + ralph-init exemption + slim-template shape + full setup-mode-set state machine (refusals listed per failure, clean-state success, copy unconditional).
- R8/R9: platforms.md plugin-mode matrix; README, flowctl.md, orchestration.md, troubleshooting.md, architecture.md, spec-template.md, adding-skills.md, local-dev.md updated; CHANGELOG Unreleased entry present, no version bump.
- R13: transition table implemented - ask-first cleanup (git rm tracked / listed rm untracked; modified files surfaced), plugin stamp only after all artifacts confirmed absent, decline/failure leaves copy mode + prints remaining paths.
- R14: drift ask suppressed under FLOW_RALPH / FLOW_AUTONOMOUS / REVIEW_RECEIPT_PATH / per-variant mode tokens (stderr note path present in each variant).
- R15: uninstall docs carry the plugin-mode branch.
- R11 verified (fn-96 status done + superseded banner).
- R18 (repo half): orchestration.md opens with the two-layers section incl. the precedence chain + persists-nothing example.
- R17: `agent_docs/setup-modes.md` exists with all listed sections; CLAUDE.md carries the Where-to-look row + checklist line referencing it (no inline detail dump in CLAUDE.md).
- Focused suite green: `cd plugins/flow-next/tests && python3 -m unittest test_token_budgets test_precheck_mode_contract test_setup_mode_stamp -q`; smoke: `cd plugins/flow-next/scripts && bash smoke_test.sh`.
- Dual copy refreshed after the flowctl.py change (`.flow/bin/flowctl.py`; no `scripts/ralph/` in this repo post-fn-114).
## Done summary
Task .2 complete across two commits (b8218571 mechanism, + docs/hermeticity). setup-mode set plumbing w/ 10-case state machine test; slim snippet template (260 tok, engine markers + v1 sentinel); setup workflow Step 2b mode question + transition table + Step 7c stamp-last; pre-check rewrite 17 carriers (ralph-init exempt) + contract test; sync-codex plugin-strip transform + paired guards (idempotent); full docs sweep incl. setup-modes.md contributor doc + orchestration two-layers section; CHANGELOG Unreleased. Grok-via-cursor quota-dead; native grok authored setup-mode cmd + stamp test (accepted after review); host authored prose/docs/transforms. Fixed two test-isolation gaps exposed by the branch (ralph-docs-truth changelog scan, backend-spec pin leak). Full suite 1957/0/0 + smoke green.
## Evidence
- Commits: b8218571, HEAD
- Tests: python3 scripts/run_tests_parallel.py (1957 ran, 0 failures, 0 errors), cd plugins/flow-next/scripts && bash smoke_test.sh, ./scripts/sync-codex.sh x2 idempotent, all guards green incl. new fn-121 pair, claude plugin validate plugins/flow-next
- PRs: