# fn-92-opinionated-prime-mode-project-type.6 workflow.md Phase 2/3 extensions (ladder + gates + substance execution + AO/DR/TO/HP inline + verdict)

## Description
workflow.md Phase 2/3 extensions: ladder, gates, substance execution, AO/DR/TO/HP inline, verdict.

**Size:** M | **Files:** `plugins/flow-next/skills/flow-next-prime/workflow.md` (Phases 2/3)

### Approach
- Phase 2 (L44-97 seam): tier-1 build execution (bounded, portable timeout per classification.md), per-workspace-member verification, tier-3 boot probe with ready-signal gate + external-SaaS qualifier + not-probed-on-this-host rule; agent-file quoted-command HOST extraction + execution (extraction-failure flag when fenced blocks exist but zero commands resolve); hook-content reads (never execute); .env cross-ref per DE1 spec scoping; stacks.md verify column drives commands.
- Phase 3 (L100-152): five-state scoring keeps its rules; whitelist prose swapped for the pillars.md table reference (resolution 11); hard gates G1-G3 evaluation + Level-2 cap; operability tier computation (per-member, ceilings, "at ceiling" + sideways moves); AO/DR/TO/HP host-inline check blocks (resolution 10) reusing boot/CLI probe output; latency lines per resolution 3 (time only what already executed; else not-measured + CI median); verdict assembly (classification + tier + gates + top-5 from playbooks.md catalog).

### Key context
- Evidence rules are absolute: unverified-counts-as-fail extends to every executed criterion; every substance verdict quotes evidence.
- HP quoting: key names never values; hooks read never executed; suspicious = P0.
## Acceptance
- [ ] Ladder graded from executed evidence: per-member tiers, shape ceilings with at-ceiling reporting + sideways moves, SaaS gate, not-probed rule (R4)
- [ ] Gates G1-G3 cap level at 2 with the failure named (R5); extraction-failure flags instead of vacuous G3 pass
- [ ] Substance executions per R6 wired (build/lint/scripts run; env cross-ref; hook reads); each quoting evidence
- [ ] AO/DR/TO/HP inline blocks present, no new scouts (R16, R17); latency lines per resolution 3 (R18)
- [ ] Whitelist referenced from pillars.md table only; verdict assembly produces headline inputs (R3)
## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
