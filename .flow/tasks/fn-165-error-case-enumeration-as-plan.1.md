---
satisfies: [R1, R5, R6]
---
# fn-165-error-case-enumeration-as-plan.1 Spec template: negative-cases discipline + worked example + docs

## Description
Add the negative-cases discipline to the spec template's Acceptance Criteria scaffold with a compact worked example, regenerate the setup-managed copy, and document the convention in the spec-template guide.

**Size:** S
**Files:** `plugins/flow-next/templates/spec.md` (:97-113 AC section), `.flow/templates/spec.md` (regenerated copy), `plugins/flow-next/docs/spec-template.md`

### Approach
- Under the existing `## Acceptance Criteria` heading (do NOT rename/restructure — parsers depend on it), add guidance prose: each behavioral criterion states its error/invalid-input/boundary handling inside the R-ID bullet, or explicitly records "no error surface beyond X". Keep it to a few lines (fn-99: minimal inline guidance beats prose walls).
- Worked example, 2-3 lines inline: one criterion with its error cases as sub-clauses + one valid no-error-surface line.
- Copy the updated template over `.flow/templates/spec.md` (setup-managed copy, same change).
- `docs/spec-template.md`: longer explanation of the convention next to the existing R-ID rules (what counts as an enumeration, the "considered vs forgot" rationale, G-ID non-restatement interplay).

### Investigation targets
**Required** (read before coding):
- `plugins/flow-next/templates/spec.md:44-113` — discovery-cascade comment + current AC section
- `plugins/flow-next/docs/spec-template.md` — where R-ID rules are documented

**Optional**:
- `plugins/flow-next/tests/test_prompt_text_pinned.py:147-169` — confirm template stays unpinned
- `plugins/flow-next/tests/test_token_budgets.py` — confirm template not budget-pinned

### Key context
- No sub-R-IDs, no new coverage-table columns — error cases fold into the parent bullet (spec Decision context).
- Codex mirror carries `codex/templates/spec.md` with a byte-equality guard (`test_template_canonical.py`, `test_dogfood_template_parity`) — this task runs `./scripts/sync-codex.sh` TWICE and commits the mirror diff with the canonical edit (mirror never deferred across a task boundary). Task .2 syncs again for its own skill edits.

### Acceptance
- [ ] Template AC section carries the discipline + compact worked example; heading/R-ID grammar unchanged (R1)
- [ ] `.flow/templates/spec.md` regenerated byte-equal to plugin template (R1)
- [ ] `docs/spec-template.md` documents the convention (R6 part)
- [ ] `test_prompt_text_pinned.py` + `test_token_budgets.py` pass unchanged (R5)
- [ ] `./scripts/sync-codex.sh` run twice (idempotent), mirror committed with the canonical change; `test_template_canonical` + `test_dogfood_template_parity` green (R1/R5)
## Acceptance
- Template discipline + worked example landed under the existing AC heading; setup-managed copy regenerated (R1)
- spec-template guide documents the convention (R6 part)
- Pinning/budget suites pass unchanged (R5)
## Done summary
Added the negative-cases discipline to the spec template's Acceptance Criteria section: guidance prose (error/invalid/boundary handling inside the R-ID bullet or explicit "no error surface beyond X"; G-IDs referenced never restated), fill-in placeholders retained with an error-clause slot, compact 2-line worked example. `.flow/templates/spec.md` regenerated byte-equal; `docs/spec-template.md` gained an "Error-case enumeration" section (enumeration shape, considered-vs-forgot rationale, G-ID interplay, non-retroactivity). Codex mirror regenerated, sync-codex.sh idempotent. test_template_canonical, test_dogfood_template_parity, test_prompt_text_pinned, test_token_budgets all green. Implemented by grok-4.5 bridge; host restored placeholders and re-verified.
## Evidence
- Commits: f5e59f8fb8d10fd469152f958849aa6fa1139569
- Tests: cd plugins/flow-next/tests && python3 -m unittest test_template_canonical test_dogfood_template_parity test_prompt_text_pinned test_token_budgets -q
- PRs: