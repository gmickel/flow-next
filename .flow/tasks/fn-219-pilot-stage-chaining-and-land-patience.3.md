---
satisfies: [R7, R8, R9, R11]
---
# fn-219-pilot-stage-chaining-and-land-patience.3 Land patience-after-review: review-event anchor for the silence gate (skill prose + conduct + contract tests)

## Description
Read `land.patienceMinutesAfterReview` through the existing single `lcfg` capture, record the head-current review event timestamp in §2.6, re-anchor ONLY the silence gate's window conjunct when the conditions hold (R7, R8), surface the binding anchor in the reason/report lines (R9), amend the conduct checklist, and pin the contract with token-level static tests on the canonical files (R11). Prose only; the codex mirror is regenerated by task .4, never here.

**Size:** M
**Files:** `plugins/flow-next/skills/flow-next-land/workflow.md`, `plugins/flow-next/skills/flow-next-land/SKILL.md`, `agent_docs/conduct/land.md`, new `plugins/flow-next/tests/test_land_patience_after_review.py`
**Touches:** [plugins/flow-next/skills/flow-next-land/workflow.md, plugins/flow-next/skills/flow-next-land/SKILL.md, agent_docs/conduct/land.md, plugins/flow-next/tests/test_land_patience_after_review.py]

### Approach
- Phase 0 (`workflow.md:68-91`): `PATIENCE_AFTER_REVIEW="$(lcfg patienceMinutesAfterReview)"`; anything but a positive integer (`null`, `""`, `0`, non-numeric) → `""` (off). One line of comment: off states + why 0 is off.
- §2.6 reviews loop (`:397-410`): initialise `REVIEW_EVENT_AT=""`; inside the head-current branch keep the running MAX of `submitted` (lexical ISO-8601 compare, the `LAST_PUSH` convention — pagination order is not newest-first and several bots may review). Comment scan (`:415-447`): add `.updated_at` to the jq tsv as the SECOND field (body stays last, tabs already stripped) and on a qualifying match fold that `updated_at` into the same max. Keep every token the existing `CommentScanWorkflowStaticTestCase` pins.
- New block after the comment scan and before the draft-trigger paragraph (`:451`): `WINDOW_ANCHOR=push` initialised where `PR_STATE` is captured alongside `REVIEWERS_STATE` (`:282-292`, the fn-200 initializer discipline); when `PATIENCE_AFTER_REVIEW` non-empty AND `AUTO_REVIEW_CURRENT == 1` AND `UNRESOLVED -eq 0` AND `REVIEW_EVENT_AT` non-empty AND `fromdateiso8601` parses it: compute `REVIEW_AGE_MIN` by the same epoch subtraction §2.3 uses, set `WINDOW_ANCHOR=review`, `SILENCE_WINDOW_ELAPSED=$(( REVIEW_AGE_MIN >= PATIENCE_AFTER_REVIEW ? 1 : 0 ))`; else `SILENCE_WINDOW_ELAPSED=$WINDOW_ELAPSED`. Parse failure → push anchor. `REVIEW_EVENT_AT` is per-tick memory — nothing written to the ledger; `triggerSha` and the review-trigger one-shot are untouched.
- Silence bullet (`:462`): the window conjunct reads `SILENCE_WINDOW_ELAPSED`; the `AWAITING_REVIEW` reason names the binding window (`patience window open (<age>/<limit>m, anchor=review)` when re-anchored). `approve`/`<login>` bullets, §2.4, §2.6b, §2.7 keep `WINDOW_ELAPSED` untouched — state that in one sentence.
- Phase 4 report (`:933`) and the dry-run report (`:583`): when the key is configured append ` anchor=<push|review>` to the `window=` field (review-anchored ticks show the review age/limit); unset keeps the line byte-for-byte.
- SKILL.md workflow summary item 3 (gate bullets) and "Unattended runs": one clause each naming `land.patienceMinutesAfterReview` and that it is a `silence`-only refinement; merge license text untouched.
- `agent_docs/conduct/land.md`: one item — with the key set, only the silence gate's window re-anchors, only on a head-current review with zero unresolved threads, the merge flags are unchanged, and a configured-but-not-due tick reports `anchor=push`.
- Tests (new file, sliced-block token pins in the `CommentScanWorkflowStaticTestCase` style, CANONICAL file only — task .4 extends to the mirror after the regen): `lcfg patienceMinutesAfterReview` present and still exactly one `config get land --json`; the comment-scan jq projection carries `.updated_at`; the anchor block pins `AUTO_REVIEW_CURRENT" == 1`, `"$UNRESOLVED" -eq 0`, `REVIEW_EVENT_AT`, `WINDOW_ANCHOR=review`, `fromdateiso8601`; `WINDOW_ANCHOR=push` initialiser sits in the Phase 2 top slice; the silence bullet references `SILENCE_WINDOW_ELAPSED` while the approve bullet does not; the merge command still carries `--squash --delete-branch --match-head-commit` and no `--auto` (reuse the MergeSeam slice); SKILL + conduct carry the key.

### Investigation targets
**Required** (read before coding):
- `plugins/flow-next/skills/flow-next-land/workflow.md:60-101` — Phase 0 config capture + ledger schema
- `plugins/flow-next/skills/flow-next-land/workflow.md:280-330` — PR_STATE capture, initializers, §2.3 push anchor
- `plugins/flow-next/skills/flow-next-land/workflow.md:385-500` — §2.6 reviews loop, comment scan, signal evaluation, §2.6b
- `plugins/flow-next/skills/flow-next-land/workflow.md:925-945` — Phase 4 report shape
- `plugins/flow-next/tests/test_land_config.py:542-625` — comment-scan pins that must survive

**Optional** (reference as needed):
- `.flow/memory/bug/integration/land-evidence-field-defaulted-to-off-on-2026-08-19.md` — initializer discipline
- `plugins/flow-next/tests/test_land_config.py:836-880` — merge-seam slice
## Acceptance
- [ ] Key read rides the single `lcfg` capture; off states documented inline
- [ ] `REVIEW_EVENT_AT` is the max across head-current reviews and qualifying clean-review comments (comment projection carries `updated_at`)
- [ ] Re-anchor applies only to the silence window conjunct under the four conditions; other window consumers unchanged; no ledger field added
- [ ] `anchor=` reported only when configured; `anchor=push` on configured-but-not-due ticks
- [ ] Merge flags byte-for-byte unchanged; existing land static tests green; no `sync-codex.sh` run in this task
- [ ] Quick: `cd plugins/flow-next/tests && python3 -m unittest test_land_patience_after_review test_land_config test_skill_prose_diet -q`
## Done summary
Land's `silence` gate can now measure its patience window from the head-current automated review event instead of the last push when `land.patienceMinutesAfterReview` is set (R7, R8, R9): Phase 0 reads the key through the single `lcfg` capture (any positive integer is on; null/""/0/non-numeric are off), §2.6 records `REVIEW_EVENT_AT` as the running max across head-current reviews and qualifying clean-review comments (the comment jq projection now carries `.updated_at` as its second field), and a new re-anchor block after the comment scan sets `WINDOW_ANCHOR=review` + `SILENCE_WINDOW_ELAPSED` only under the silence signal AND key-active AND `AUTO_REVIEW_CURRENT == 1` AND `UNRESOLVED -eq 0` AND a `fromdateiso8601`-parseable timestamp, comparing overflow-safely (a limit beyond bash's signed range is never elapsed) — every other case, parse failure included, keeps the push anchor. `WINDOW_ELAPSED` is untouched, so `approve`/`<login>`, §2.4, §2.6b, and §2.7 keep the push window; no ledger field, `triggerSha` and the merge flags are byte-for-byte. `WINDOW_ANCHOR=push` is initialized at the PR_STATE capture (fn-200 initializer discipline); Phase 4 / dry-run append `anchor=<push|review>` to `window=` and the `AWAITING_REVIEW` reason names the binding anchor, both only when the key is configured. SKILL.md (gate bullet + Unattended runs), `agent_docs/conduct/land.md` (one item), and new `plugins/flow-next/tests/test_land_patience_after_review.py` (sliced token pins plus executable fence tests for the Phase 0 read and the re-anchor condition matrix, on the canonical files) — R11. Codex mirror untouched by design (task .4 owns the single regen and extends the pins to the mirror).

Integrated onto the target branch by cherry-pick (worker commit 63eea22c → 9c25ed79), then two conductor-owned review-fix commits (553a327b, 47f25976).

baseline: green via handoff (verified at c21d21ca by fn-219.1); ruff green pre-edit.
Verify: `gate classify` → FULL (skills/ force-full prefix). Integrated-target verify at 47f25976: spec Quick + task Quick + pin suites (9 modules) OK; `gen_flow_config_schema.py --check` current; `uvx ruff@0.16.0 check .` clean. No full-suite receipt: the canonical full-suite command runs at the conductor's final gate.

stage: impl-review - ran (codex; round 1 three-axis fan-out NEEDS_WORK ×3 → merged 3 findings fixed; round 2 NEEDS_WORK 1 finding fixed; round 3 SHIP) (model: codex backend, model as reported in the review receipt)
stage: plan-sync - skipped(config: planSync.enabled != true)
## Evidence
- Commits: 9c25ed79, 553a327b, 47f25976
- Tests: cd plugins/flow-next/tests && python3 -m unittest test_land_patience_after_review test_land_config test_skill_prose_diet -q, cd plugins/flow-next/tests && python3 -m unittest test_pipeline_qa_config test_land_config test_flow_config_schema test_flow_config_schema_drift test_skill_prose_diet test_pilot_chain_stages test_land_patience_after_review test_pilot_strikes_prose test_prompt_text_pinned -q (integrated target 47f25976), python3 scripts/gen_flow_config_schema.py --check, uvx ruff@0.16.0 check .
- PRs: