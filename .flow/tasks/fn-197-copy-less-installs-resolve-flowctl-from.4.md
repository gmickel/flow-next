# fn-197-copy-less-installs-resolve-flowctl-from.4 Stop dogfooding a tracked .flow/bin in this repo

## Description
**What:** flow-next's own repo stops dogfooding tracked copies — `scripts/` becomes the single source; every repo-internal dual-copy ritual, test, and CI trigger retargets.

**Execution checklist:**
1. `git rm -r` the tracked dogfood copies: `.flow/bin/` (launchers + `flowctl_tracker/`, 20+ files), `.flow/templates/spec.md` (+ dir if empty), `.flow/usage.md`. This deletion is explicit and spec-mandated; everything removed must be TRACKED at deletion time (never delete uncommitted `.flow/*`). DO NOT touch `.flow/config.json` (line 106 carries a live tracker UUID) or any `.flow/specs|tasks|artifacts|memory` history. `.flow/meta.json`: leave old `setup_mode`/`setup_version` fields as inert metadata (or drop them — .3's tolerance makes either safe).
2. `tests/test_tracker_distribution.py`: delete `test_flow_bin_copy_matches_the_manifest_too` (L62-75); keep `InstallerVerifier`/`RuntimeSmoke`/`ExecutableInstallerFailClosed` (path-agnostic); `test_every_installer_invokes_the_shared_verifier` (L115-127) — verify .2 already dropped the setup-workflow half.
3. Delete the dogfood parity tests (whole surviving set — .2 kept them green via parity refresh): `tests/test_dogfood_template_parity.py` (whole file), `test_chart_docs_inventory.py::ChartUsageParity::test_template_and_dogfood_byte_identical` (~L612-620, keep `test_usage_has_compact_chart_section`), `test_cursor_host_docs.py::TestUsageTemplateDogfoodParity` (~L112-121), `test_cp1252_robustness.py::DualCopyInvariant` (~L118-132), `test_prime_eval.py` dual-copy tests (~L1256-1267, keep the L418/448 exclusion-category tests and `test_schema_contract_is_pinned_in_classification_md`), `test_pilot_backlog_substrate.py::DualCopyInvariantTestCase` (~L529-535) AND retarget its runtime shell-out at ~L419-424 from `DOGFOOD_FLOWCTL_PY` to the canonical `scripts/flowctl.py`, `test_startup_bootstrap.py::test_dogfood_bootstrap_is_byte_identical` (~L39-49).
4. Repo `CLAUDE.md`: delete the dual-copy propagation gate (~L192: `cp … .flow/bin/flowctl.py` + rsync + gen_tracker_manifest ritual); rewrite L87 (preamble rule → the new 3-rung rule, drop the setup-modes.md link), L142 (`.flow/templates/spec.md` "setup-managed copy" parenthetical), L143 (setup-modes doc-map row); regenerate the FLOW-NEXT block (L166-198) from the new snippet template; L229/239 `.flow/usage.md` recipes → `flowctl usage`.
5. `.github/workflows/test-flow-next.yml`: remove `.flow/usage.md`, `.flow/templates/spec.md`, `.flow/bin/**` from BOTH push and pull_request path filters (~L8-11, 44, 49-52, 75 — the two lists are asserted identical); keep `.flow/assets/**`. Update the test-list step (~L468-476) for the files deleted in .3/.4. `tests/test_ci_trigger_coverage.py` derives its required set with `.flow` in EXEMPT_TOP_LEVEL (L31) — removal stays green; update its stale comment ("partially covered: bin/ + …").
6. Repo-internal stragglers: `agents/worker.md:182` (drop the "`.flow/bin` dual-copies" clause from the orchestrator-owned-artifacts rule; codex-mirror clause stays); `.gitattributes:7` (drop `.flow/bin/flowctl` from the launcher-list comment; the `flowctl text eol=lf` rule stays); `scripts/gen_tracker_manifest.py` — DELETE the dual-write block (~L61-67, writes `.flow/bin/flowctl_tracker/MANIFEST.json`) and its comment; `plugins/flow-next/scripts/ralph_e2e_rp_test.sh` (~L198-207) + `ralph_e2e_short_rp_test.sh` (~L97-104) — both simulate setup by cp-ing into `.flow/bin` (an INCOMPLETE install: no tracker package): retarget their fixture setup to plugin-path resolution (`$PLUGIN_ROOT/scripts/flowctl`); note this changes what the E2Es exercise (real tracker package instead of ImportError-degradation fallbacks) — an improvement, but verify the runs.
7. `makePr.derivedPaths` doc surface: `skills/flow-next-make-pr/workflow.md:665` (dual-copy example dies), `skills/flow-next-prime/stacks.md:92` + `classification.md:134` (dual-copy invariant examples → reword to a generic example), `skills/flow-next-work/phases.md:123` ("`.flow/bin` dual copies" clause), `agents/repo-scout.md:26` (comment naming the setup-installed copy). Mirror regen for these skill edits (sync-codex, twice).
8. `ruff.toml` ~L21-24: the `.flow` exclude's rationale ("setup-managed install copy… desync from MANIFEST.json") is now vestigial — narrow the exclude or rewrite the comment.
9. Full local gate from a clean checkout with no `.flow/bin` — proves the repo's own skills, tests, and gate run copy-less end-to-end. Capture suite exit codes directly. Note: flow-next-tui's `lint:check` (publish-tui.yml) has been red since 2026-01, pre-existing and out of this spec's scope — `bun test` is the honest tui gate; don't treat the pre-existing lint red as a gate regression introduced here.
<!-- Updated by plan-sync: fn-197-copy-less-installs-resolve-flowctl-from.3 confirmed tui lint:check is pre-existing red, unrelated to this spec -->

**Touches:** .flow/bin/** (delete, always-serial), .flow/templates/**, .flow/usage.md, .flow/meta.json, .gitattributes, ruff.toml, CLAUDE.md, .github/workflows/test-flow-next.yml, scripts/gen_tracker_manifest.py, plugins/flow-next/agents/worker.md, plugins/flow-next/agents/repo-scout.md, plugins/flow-next/scripts/ralph_e2e_rp_test.sh, plugins/flow-next/scripts/ralph_e2e_short_rp_test.sh, plugins/flow-next/skills/flow-next-make-pr/**, plugins/flow-next/skills/flow-next-prime/**, plugins/flow-next/skills/flow-next-work/phases.md, plugins/flow-next/codex/** (regenerated), plugins/flow-next/tests/**
## Acceptance
- [ ] `git ls-files .flow/bin .flow/templates .flow/usage.md` returns nothing; `.flow/config.json` and `.flow/` history untouched.
- [ ] All dogfood parity/dual-copy tests deleted (named set in the checklist); `test_pilot_backlog_substrate` shells the canonical flowctl; tracker-distribution guards `scripts/` only.
- [ ] Repo CLAUDE.md has no copy instructions or dual-copy ritual; FLOW-NEXT block regenerated from the new snippet.
- [ ] CI path filters symmetric with no `.flow/bin|usage|templates` entries; test-list step current; `test_ci_trigger_coverage` green with its comment updated.
- [ ] Stragglers fixed: worker.md:182, .gitattributes:7, gen_tracker_manifest dual-write deleted, both ralph E2E scripts set up via plugin path, make-pr/prime/work/repo-scout prose, ruff.toml comment.
- [ ] Full local gate green from a clean checkout with no `.flow/bin` (exit codes captured directly); sync-codex clean.
## Done summary
Retired flow-next's own tracked dogfood copies: `.flow/bin/**` (launchers, `flowctl.py`, bootstrap, help text, the 45-file `flowctl_tracker` package), `.flow/templates/spec.md`, and `.flow/usage.md` are `git rm`'d, and every repo-internal dual-copy ritual, test, CI trigger, and prose mention retargets to `plugins/flow-next/scripts/` as the single source.

What changed, per checklist item:
1. `git rm -r .flow/bin .flow/templates .flow/usage.md` (all tracked at deletion time; leftover `__pycache__` trashed). `.flow/config.json`, `.flow/specs|tasks|artifacts|memory` untouched. `.flow/meta.json` left as-is — `setup_mode`/`setup_version` stay as inert metadata (.3 made both safe; nothing reads them).
2. `test_tracker_distribution.py`: `test_flow_bin_copy_matches_the_manifest_too` deleted, replaced by a single-tree `test_verifier_passes_against_the_shipped_tree`. `InstallerVerifier` / `RuntimeSmoke` / `ExecutableInstallerFailClosed` / `test_every_installer_invokes_the_shared_verifier` kept unchanged (already path-agnostic and already dropped the setup half in .2).
3. Dogfood parity tests deleted: `test_chart_docs_inventory::ChartUsageParity::test_template_and_dogfood_byte_identical` (kept `test_usage_has_compact_chart_section`; also dropped `.flow/usage.md` from the two `PIPELINE_SURFACES` / `no_mandatory_chart_wording` path lists — they would have failed on a missing file), `test_cursor_host_docs::TestUsageTemplateDogfoodParity`, `test_cp1252_robustness::DualCopyInvariant` (byte-identity gone; the #167 content assertions survive as `CanonicalSourceCarriesTheFixes` against `scripts/flowctl.py`), `test_prime_eval::DualCopyInvariantTestCase` (byte-identity gone; emitter-present check survives as `EmitterContractTestCase`, live smokes retargeted to `FLOWCTL_PY`, `test_schema_contract_is_pinned_in_classification_md` and the L418/448 exclusion tests kept), `test_pilot_backlog_substrate::DualCopyInvariantTestCase` + its runtime shell-out retargeted to `FLOWCTL_PY`, `test_startup_bootstrap::test_dogfood_bootstrap_is_byte_identical`. `test_dogfood_template_parity.py` was already gone (removed upstream in .2) — no-op.
4. Repo `CLAUDE.md`: FLOW-NEXT block regenerated from the converged `claude-md-snippet.md` (bare `flowctl`, `flowctl usage` pull directives, `<!-- flow-next:snippet:v1 -->` sentinel) with the repo-specific maintainer notes re-attached below it (receipt-commit rule, bulk task create, criteria G-IDs, Quick-commands policy, config-schema rule, memory dir). The `cp … .flow/bin/flowctl.py` + rsync propagation gate is deleted (only `gen_tracker_manifest.py` + `sync-codex.sh` x2 remain); the cross-platform preamble rule now states the three rungs; the spec-template row drops the "setup-managed copy" parenthetical; the `agent_docs/setup-modes.md` doc-map row is dropped; `.flow/usage.md` recipe pointers → `flowctl usage`.
5. `.github/workflows/test-flow-next.yml`: `.flow/usage.md`, `.flow/templates/spec.md`, `.flow/bin/**` removed from BOTH push and pull_request filters (the two lists stay identical, asserted by `test_ci_trigger_coverage`); `.flow/assets/**` kept; stale "dogfood copies" comments updated in the workflow and in the test's docstring + `EXEMPT_TOP_LEVEL` comment. The test-list step (`test_startup_bootstrap`/`test_bin_launcher_parity`/`test_cmd_usage`/`test_precheck_mode_contract`) needed no change — all four modules still exist.
6. Stragglers: `.gitattributes` launcher-list comment drops `.flow/bin/flowctl` (the `flowctl text eol=lf` rule stays); `scripts/gen_tracker_manifest.py` dual-write block deleted; both ralph E2E scripts (`ralph_e2e_rp_test.sh`, `ralph_e2e_short_rp_test.sh`) now mirror setup by writing only the CLAUDE.md rail and letting skills resolve `$PLUGIN_ROOT/scripts/flowctl` — this changes what they exercise (a real tracker package instead of the ImportError-degradation fallbacks), an improvement; the runs are host-driven (RepoPrompt + claude CLI) and were not executed here. **The `agents/worker.md:182` item was already resolved upstream**: flow-98 removed the packaged-delegation section, so the "`.flow/bin` dual-copies" clause no longer exists in the repo's worker.md (verified by grep + `git log`).
7. Prose: `make-pr/workflow.md` derived-file example → a generic vendored-snapshot pair; `prime/stacks.md` LEG6 example → a generic `vendor/` pair; `prime/classification.md` → "the single shipped source"; `work/phases.md` always-serial set drops the `.flow/bin` clause; `repo-scout.md` comment now names the derived-plugin-root rung and the legacy backstop. Mirror regenerated (`sync-codex.sh` twice, idempotent — 5 mirror files changed on the first run, zero on the second).
8. `ruff.toml`: the `.flow` exclude keeps its scope but the rationale is rewritten (project state / agent scratch, not a setup-managed install copy).
9. Gates: baseline green pre-edit; post-edit green; and a **fresh detached worktree from this HEAD with no `.flow/bin`** ran the full suite green (exit codes captured directly), plus ruff and a clean idempotent `sync-codex.sh` there.

Notes for the reviewer:
- `.flow/templates/spec.md` still appears as tier 3 of the spec-template discovery cascade (`references/spec-template-discovery.md`) and the dual-mode story still lives in `README.md` / `docs/*` — both are task .5's docs sweep, deliberately untouched here.
- Pre-existing reds not caused by this task: flow-next-tui `lint:check` (red since 2026-01; `bun test` is the honest tui gate and is green at 384 pass). The untracked `.flow/artifacts/flow-98-*/` dir was excluded from staging.

baseline: green (`python3 scripts/run_tests_parallel.py` rc=0, 190 files / 4375 tests, pre-edit at 7b6230d6)

stage: impl-review - skipped(policy: host-deferred - conductor owns the gate)
stage: delegation - skipped(config: delegation off)


stage: impl-review - ran (host backend, fresh fable-5 reviewer, SHIP round 1; one cosmetic fixed post-verdict)
## Evidence
- Commits: b25e06039da34ef85efc1d4d9c9c3769050ff039
- Tests: python3 scripts/run_tests_parallel.py (baseline pre-edit, rc=0, 190 files / 4375 tests), python3 scripts/run_tests_parallel.py (post-edit, rc=0, 189 files / 4368 tests), python3 scripts/run_tests_parallel.py (clean bin-less worktree at HEAD, rc=0, 189 files / 4368 tests), uvx ruff@0.16.0 check . (rc=0, main + clean worktree), ./scripts/sync-codex.sh x2 (rc=0, idempotent; clean worktree run produced no diff), cd flow-next-tui && bun test (rc=0, 384 pass), impl-review: host backend SHIP round 1 (reviewer claude-fable-5; receipt /tmp/impl-review-receipt-fn-197-copy-less-installs-resolve-flowctl-from.4.json)
- PRs: