---
satisfies: [R2, R3, R4, R5, R11]
---
# fn-219-pilot-stage-chaining-and-land-patience.2 Pilot in-tick stage chaining under pipeline.chainStages (skill prose + conduct + contract tests)

## Description
Wire the one-row chain table (`qa`→`make-pr`) into pilot's shared dispatch/verify path behind `pipeline.chainStages` (R3–R5), record that `plan`→`plan-review` is not a pair (R2), reconcile every authoritative single-stage-per-tick surface with the gated clause (R4), amend the conduct checklist, and pin the contract with token-level static tests on the canonical files (R11). Prose only — no flowctl changes; the codex mirror is regenerated by task .4, never here.

**Size:** M
**Files:** `plugins/flow-next/skills/flow-next-pilot/SKILL.md`, `plugins/flow-next/skills/flow-next-pilot/workflow.md`, `plugins/flow-next/skills/flow-next-pilot/references/backlog-mode.md`, `plugins/flow-next/commands/pilot.md`, `scripts/sync-codex.sh` (the two hardcoded pilot description strings only), `agent_docs/conduct/pilot.md`, new `plugins/flow-next/tests/test_pilot_chain_stages.py`
**Touches:** [plugins/flow-next/skills/flow-next-pilot/SKILL.md, plugins/flow-next/skills/flow-next-pilot/workflow.md, plugins/flow-next/skills/flow-next-pilot/references/backlog-mode.md, plugins/flow-next/commands/pilot.md, scripts/sync-codex.sh, agent_docs/conduct/pilot.md, plugins/flow-next/tests/test_pilot_chain_stages.py]

### Approach
- Gate read in workflow.md Phase 2, directly after the QA gate (`workflow.md:327-341`): `CHAIN_STAGES="$(jq -r '.value.pipeline.chainStages' "$PILOT_CFG_SNAPSHOT" 2>/dev/null)"`, `CHAIN_ENABLED=1` only on the literal `on`, jq error ⇒ off (fail-closed, unlike the QA probe — say why in one clause). Zero new `config get` (test_skill_prose_diet pins one call in SKILL.md, none in workflow.md).
- Phase 4 (`:424-455`) keeps "dispatch exactly one stage skill" as the primary dispatch. Add a short **Chained stage (`pipeline.chainStages`)** subsection after Phase 5's make-pr block: the one-row table `qa → make-pr`, entered only when `CHAIN_ENABLED=1` AND `QA_ADVANCED=true`; one sentence stating `plan → plan-review` is not a row and why (the plan dispatch embeds its review loop). The chained make-pr reuses the standalone stage's branch-matrix row (already on the spec branch — no second checkout), pre-dispatch evidence (no OPEN PR from the all-done probe), dispatch line, Phase 5 evidence block + `stage:` line, and Phase 6 handling — reference those phases, do not restate them. Backlog mode: `assert_allowed_dispatch "/flow-next:make-pr"` before the chained dispatch; each dispatched stage appends its own `pilot-log` row (rewrite the "exactly one row per tick" sentences at `:717-719` to per-dispatched-stage; a chained tick writes two).
- Ledger ordering (Phase 6): the qa `ADVANCED` clears the entry first, then the chained make-pr records its own clear or strike under `make-pr`; verdict = last dispatched stage's verdict; `stage=qa+make-pr`; reason names the fresh `qa_outcome` and the PR URL (or its absence).
- SKILL.md: description + verdict-contract stage values (`:136-141`) admit `qa+make-pr`; the Forbidden bullet (`:164`), the workflow header (`workflow.md:3`), and the Phase 4 Done-when (`:455`) get the gated clause: with the key on, `make-pr` after a fresh `qa` verdict is the ONLY second dispatch; any other second stage still breaks the contract. Same one-clause treatment on `references/backlog-mode.md:29` and `:326`, `commands/pilot.md:3`, and the two pilot description strings in `scripts/sync-codex.sh:1701` and `:1770` (keep each ≤200 chars — the catalog budget).
- Dry-run report (`:385`): add `chain=<off|on>` and, when on, `would-chain=make-pr` only for a classified `qa` stage (conditional on a fresh terminal verdict), else `would-chain=none (stage <x> heads no pair)`.
- `agent_docs/conduct/pilot.md`: amend items 1–2 (single stage / stage set) with the gated pair exception; add one item: chaining never chains from `plan`, `plan-review`, `work`, or `make-pr`, never into `work`, and never names `plan-review` as a target.
- Tests (new file, G2 tokens only, CANONICAL files only — task .4 extends them to the mirror via the `both_copies` pattern after the regen): `.value.pipeline.chainStages` read appears in workflow.md and no new `config get`; `qa+make-pr` token present in SKILL.md and workflow.md; the chain block (slice from the subsection heading to the next `##`) requires `QA_ADVANCED` and contains no `plan-review` target token and no `→ work`/`-> work`; `would-chain=` appears in the dry-run paragraph; conduct file carries `pipeline.chainStages`; `commands/pilot.md` and both sync-script description strings carry a chain clause (pin the key name `chainStages`, not the sentence).

### Investigation targets
**Required** (read before coding):
- `plugins/flow-next/skills/flow-next-pilot/workflow.md:308-391` — Phase 2 gate reads + classification
- `plugins/flow-next/skills/flow-next-pilot/workflow.md:424-574` — Phase 4 dispatch + Phase 5 evidence blocks
- `plugins/flow-next/skills/flow-next-pilot/workflow.md:607-723` — Phase 6 ledger, verdicts, backlog decision-log
- `plugins/flow-next/skills/flow-next-pilot/SKILL.md:130-175` — verdict contract + Forbidden
- `agent_docs/conduct/pilot.md`

**Optional** (reference as needed):
- `plugins/flow-next/tests/test_skill_prose_diet.py:60-100,200-260` — `both_copies` + single-config-get pins
- `plugins/flow-next/tests/test_land_config.py:542-625` — static-contract test shape (sliced block, token pins)
- `.flow/memory/bug/build-errors/skill-workflow-snippets-must-enforce-2026-06-11.md`
## Acceptance
- [ ] Gate read derives from the root snapshot; `test_skill_prose_diet` stays green
- [ ] Chain table is exactly `qa → make-pr`; no `plan-review` target; make-pr chain only on `QA_ADVANCED=true`
- [ ] Verdict grammar admits `stage=qa+make-pr`; dry-run reports `chain=` and a precondition-checked `would-chain=`
- [ ] Every single-stage surface (SKILL.md, workflow.md, backlog-mode.md, commands/pilot.md, sync-codex.sh description strings) carries the gated clause
- [ ] Conduct checklist updated; new contract tests pass on the canonical files; no `sync-codex.sh` run in this task
- [ ] Quick: `cd plugins/flow-next/tests && python3 -m unittest test_pilot_chain_stages test_skill_prose_diet test_pilot_strikes_prose -q`
## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
