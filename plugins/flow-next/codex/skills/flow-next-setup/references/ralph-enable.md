# Enable or keep Ralph

Read only after a supported host's Ralph answer is `Yes, enable or keep`.

1. Tell the user Ralph is opt-in and the next step is
 `/flow-next:ralph-init` (`$flow-next-ralph-init` on Codex).
2. Prefer following the ralph-init skill workflow now in this same session so
 scaffold plus agent-driven hook merge land before setup ends. If the user
 declines during ralph-init, retain partial state and report how to finish.
3. Never invent a flowctl hook-install command. Registration is ralph-init
 skill prose only.

Set the Ralph summary outcome from the actual ralph-init result.
