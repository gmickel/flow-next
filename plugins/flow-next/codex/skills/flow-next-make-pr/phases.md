# /flow-next:make-pr phase reference

The current sequence is in [workflow.md](workflow.md): preflight and close,
export, aid composition and rendering, optional HTML lens, then creation.
[Create and finalize](create-and-finalize.md) owns push, draft state, stack
linkage and the tracker touchpoint. This stable link is not a runtime read.
