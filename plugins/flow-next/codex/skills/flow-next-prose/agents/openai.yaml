interface:
  display_name: "Flow Prose"
  short_description: "Use while drafting a substantial reply, report, or summary for the user - read the prose contract before writing and draft under its rules"
  brand_color: "#F59E0B"
policy:
  allow_implicit_invocation: true
