# Flow-Next docs

> **Codex install note:** when YOU run a flow-next command on THIS Codex install, invoke it as `$flow-next-<name>` (or pick it from the skills dropdown) wherever this page writes `/flow-next:<name>` — and when the written name itself already starts with `flow-next-` (e.g. `/flow-next:flow-next-drive`), the prefix is not doubled: invoke `$flow-next-drive`. Passages describing OTHER hosts (Claude Code `claude -p` / `/loop` examples, Grok, Cursor, OpenCode sections) document those hosts' own syntax and are quoted verbatim — do not convert them.


The offline-resilient reference for flow-next. Every file here is self-contained and readable without a network, and every cross-link is a relative repo path so a fork keeps working.

## Start here by intent

| You want to know | Read |
|---|---|
| What is this, and how do I install it? | [root README](https://github.com/gmickel/flow-next/blob/main/README.md) |
| Which stages does *this* change need? | [`pipeline-variations.md`](pipeline-variations.md) |
| Do I need all of it? | [`running-lean.md`](running-lean.md) |
| What skills exist? | [`skills.md`](skills.md) |
| How do we adopt this as a team? | [`teams.md`](teams.md) |
| Which model does what, and how do I change that? | [`orchestration.md`](orchestration.md) |
| How does *my* harness reach another model? | [`reach/README.md`](reach/README.md) |
| What's every flag on every command? | [`flowctl.md`](flowctl.md) |
| Something is stuck. | [`troubleshooting.md`](troubleshooting.md) |
| This idea is too big to capture yet. | [`../skills/flow-next-chart/SKILL.md`](../../skills/flow-next-chart/SKILL.md) |
| I don't know which stage to run. | [`../skills/flow-next-guide/SKILL.md`](../../skills/flow-next-guide/SKILL.md) |

The repo's strategic intent is [`STRATEGY.md`](https://github.com/gmickel/flow-next/blob/main/STRATEGY.md). The vocabulary, with the synonyms to avoid, is [`GLOSSARY.md`](https://github.com/gmickel/flow-next/blob/main/GLOSSARY.md); it is a dictionary rather than an encyclopedia, and the concepts are explained on the pages below. The retired long-form version is archived at [`agent_docs/archive/GLOSSARY-full.md`](https://github.com/gmickel/flow-next/blob/main/agent_docs/archive/GLOSSARY-full.md).

## Understand the system

| Doc | Answers |
|---|---|
| [`running-lean.md`](running-lean.md) | Which layers to switch on at all, what each one costs, and how to get the capability without the standing cost |
| [`pipeline-variations.md`](pipeline-variations.md) | Six worked routes from epic to docs chore, and the risk-and-unknowns rule that picks between them |
| [`architecture.md`](architecture.md) | The `.flow/` layout, the spec-first task model, and what a spec's [`## Quick commands`](architecture.md#verification-tiers-the-specs-quick-commands) block is for |
| [`self-improving.md`](self-improving.md) | How memory, glossary, decisions, strategy, and the feature map compound through work you already do |
| [`teams.md`](teams.md) | Handover objects, spec-as-PR, parallel work from one spec, the adoption ladder |
| [`platforms.md`](platforms.md) | Per-harness install and caveats for Claude Code, Codex, Droid, Cursor, Grok Build, and OpenCode |

## Run the pipeline

| Doc | Answers |
|---|---|
| [`skills.md`](skills.md) | All 32 skills in the published catalog, what triggers each one, and what it does |
| [`spec-template.md`](spec-template.md) | What belongs in a spec, the R-ID rules, and how to customize the scaffold from a root `SPEC.md` |
| [`flowctl.md`](flowctl.md) | The full CLI: every command, flag, JSON shape, and exit code |
| [`orchestration.md`](orchestration.md) | The four model tiers, the routing block in your own instruction file, review-backend selection, and the bridge route |
| [`reach/README.md`](reach/README.md) | What each harness can actually reach, and how it degrades when it cannot |
| [`../skills/flow-next-pilot/SKILL.md`](../../skills/flow-next-pilot/SKILL.md) | The build loop: one ready spec, one stage per tick, one verdict line |
| [`../skills/flow-next-land/SKILL.md`](../../skills/flow-next-land/SKILL.md) | The ship loop: CI, review convergence, the gated merge, the release tail |
| [`../skills/flow-next-qa/SKILL.md`](../../skills/flow-next-qa/SKILL.md) | The live-app pass (consumes `.flow/features/` navigation when present), and why it may never mark PASS by reading source |
| [`../skills/flow-next-features/SKILL.md`](../../skills/flow-next-features/SKILL.md) | Seed or maintain the committed user-POV drive map that QA and drive reuse for navigation |
| [`ralph.md`](ralph.md) | The deprecated hardened harness, still supported and still documented |
| [`tracker-sync.md`](tracker-sync.md) | Projecting a spec onto Linear, GitHub, GitLab, or Jira, and reconciling it back |
| [`html-artifacts.md`](html-artifacts.md) | The opt-in HTML render lenses for spec review and PR review |

## Contracts and schemas

| Doc | Answers |
|---|---|
| [`prose.md`](prose.md) | The ten rules every agent-emitted artifact drafts under |
| [`review-findings.md`](review-findings.md) | The portable structured-findings contract: identity, lineage, anchors, [bounds](review-findings.md#bounds), consumer fallback |
| [`pr-cognitive-aid.md`](pr-cognitive-aid.md) | The portable PR cognitive-aid contract, its fixture, and how downstream renderers pin it |
| [`memory-schema.md`](memory-schema.md) | The memory tree, its frontmatter, and the audit lifecycle |
| [`glossary.md`](glossary.md) | How the repo-root `GLOSSARY.md` is shaped and resolved |
| [`strategy.md`](strategy.md) | How the repo-root `STRATEGY.md` is shaped and who reads it |

## Fixing and contributing

| Doc | Answers |
|---|---|
| [`troubleshooting.md`](troubleshooting.md) | Stuck tasks, wedged reviews, `.flow/` cleanup, uninstall, and the [leftover `.flow/bin/`](troubleshooting.md#i-have-flowbin-from-an-old-install) case |
| [`sync-codex.md`](sync-codex.md) | How the Codex mirror is generated and which guards must stay green |
| [`ci-workflow-example.yml`](https://github.com/gmickel/flow-next/blob/main/plugins/flow-next/docs/ci-workflow-example.yml) | A drop-in GitHub Actions job running `flowctl validate --all` |

**Quick jumps into the CLI reference:** [`flowctl brief`](flowctl.md#brief) · [`flowctl chart`](flowctl.md#chart) · [`flowctl review-backend`](flowctl.md#review-backend) · [`flowctl prime classify`](flowctl.md#prime-classify) · [`flowctl repo-map`](flowctl.md#repo-map) · [`flowctl setup-block`](flowctl.md#setup-block) · [`flowctl validate`](flowctl.md#validate) · [`flowctl spec create`](flowctl.md#spec-create) · [`flowctl show`](flowctl.md#show) · [`flowctl pilot strikes`](flowctl.md#pilot-strikes) · [the deterministic review cap](flowctl.md#deterministic-review-cap)

**Deep links worth knowing:** [tiers](orchestration.md#tiers-what-kind-of-model-a-job-wants) · [review backends](orchestration.md#review-backends-cross-model-review) · [the bridge route](orchestration.md#implementation-offload-the-bridge-route) · [turning the review dial down](running-lean.md#turning-the-dial-none-and-host) · [finding identity and lineage](review-findings.md#identity-and-lineage) · [review bookkeeping authority](architecture.md#review-bookkeeping-authority-and-write-ordering) · [chart projection](tracker-sync.md#chart-lifecycle-projection) · [OpenCode install](platforms.md#opencode) · [backlog mode](../../skills/flow-next-pilot/references/backlog-mode.md) · [chart workflow](../../skills/flow-next-chart/workflow.md) · [land workflow](../../skills/flow-next-land/workflow.md) · [make-pr create and finalize](../../skills/flow-next-make-pr/create-and-finalize.md) · [prime](../../skills/flow-next-prime/SKILL.md) · [drive](../../skills/flow-next-drive/SKILL.md) (consumes `.flow/features/` when present) · [chart resolve](flowctl.md#chart-resolve)

## Notable updates

Append-only list of **behavior-affecting changes and new opt-in defaults**. Newest first. Not a changelog - one line each, plus how to enable. Seeded by fn-134; later releases append.

**Format (keep this shape):**

```
- **`config.key` or feature name** — one-line what changed / why it matters. Enable: `command or config`. Details: [link](path).
```

- **Three-axis review fan-out on the first round (unreleased)** — on the codex and host backends, the first review round of a scope dispatches three concurrent axis draws (correctness / contracts / integration) on the resolved backend and the coordinator merges them into one consolidated fix pass, consuming one review round, not three; re-reviews stay single-dispatch with the merged findings carried forward. Steer with prose per invocation — "use 1 reviewer instead of 3" collapses to one draw, "use three different model families for the review fan-out" routes cross-family — no flag, no config key. Always on for the codex and host backends (reviews themselves stay optional; rp, copilot, and cursor keep single dispatch). Details: [orchestration.md](orchestration.md#steering-the-fan-out-worked-recipes), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Spec-level `no_plan` field replaces pilot's `--no-plan` flag (4.11.0)** — mark a zero-task spec "too small to plan" and pilot classifies it straight to work's no-plan route once ready; the pilot invocation flag is removed (a stray `--no-plan` gets the unknown-flag notice; affected specs route through plan). Enable: `flowctl spec set-no-plan fn-N` or `/flow-next:capture ... --no-plan`; undo with `clear-no-plan`. Details: [flowctl.md](flowctl.md).
- **Land recognizes Codex's summary-table clean verdict (4.10.1)** - the silence-signal comment scan's built-in `land.cleanReviewCommentPattern` now also matches the bot's edited-in-place `**Code Review** | **Completed**` row naming the current head, so an unattended land run converges instead of stalling at `NEEDS_HUMAN` when the bot never posts the legacy "Didn't find any issues" comment; repos seeded with the retired default are auto-upgraded at read time (custom patterns and the explicit `""` off-switch untouched). Always on (seeded default). Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Comment-as-alibi finding class + settled-decisions rule in review prompts (4.9.1)** - impl and standalone review now flag a comment that exists to justify a workaround as a finding on the underlying code (severity judged from the workaround; the worker keep-list applies verbatim), and all review prompts state that recorded Decision Context decisions and process-compliance observations are FYI, never blocking - the maintainer decides when a change lands. Always on (prompt rails). Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **`/flow-next:work <spec-id> --no-plan` - skip the planning stage as a recorded choice** - a spec with zero tasks no longer falls through to a green run that implemented nothing: work now forks explicitly (plan first vs work directly, with an agent-judged recommendation per spec), and the direct route mints one minimal implicit task that runs the standard pipeline - receipts, review, and evidence unchanged. `--no-plan` or plain language ("skip planning") pre-answers the ask; the spec-level `no_plan` field (fn-214: `flowctl spec set-no-plan`, or `--no-plan` at capture) is the durable per-spec form pilot classifies on — pilot itself takes no no-plan flag; autonomous runs without the instruction stop with a typed report instead of asking; `flowctl next` surfaces the state as `reason: needs_tasks`; work-rolling refuses (a single task degenerates the rolling frontier). The direct route's worker gets a broad judicious-subagent license, enabled by the same pass's tool-permission audit: the writing agents (worker, pr-comment-resolver, plan-sync) lose an accidental `Task` denial that had blocked subagent dispatch; read-only agents keep theirs deliberately. Enable: nothing - answer the ask, or pass `--no-plan`. Details: [`pipeline-variations.md`](pipeline-variations.md#no-plan-route), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Autonomy hardening pass (4.8.0)** - worker, land, work, review, interview/plan, and audit prose gain the failure-class rules from the fn-208 audit: gate-integrity and evidence honesty for workers (never edit a gate to make it pass; deliberate, acceptance-named baseline updates stay legal), an owner-aware land tick claim with conservative CI triage, run-scoped pause/cleanup commits that never sweep pre-existing user work, a five-label review evidence scale, and probe-before-ask with a non-mutating/disposable safety predicate. Always on (prose contracts). Details: [ralph.md](ralph.md), [orchestration.md](orchestration.md).
- **Artifact prose contract + `/flow-next:prose` (4.7.0)** - agent-emitted artifact prose (PR bodies, tracker comments, specs, memory entries, done summaries, changelogs) drafts against the ten-rule contract in [`prose.md`](prose.md), cited at 23 emission points; the new stable `prose` skill applies the same contract to a substantial chat reply on request. Always on for artifacts (non-blocking asides); the reply skill is invoked or description-triggered. Details: [`prose.md`](prose.md), [`skills.md`](skills.md).
- **Excused completion reviews: `completion_review_status: not_required` (4.6.0)** - work's 3g policy skip now records the excused decision and every gate (projection, `next --require-completion-review`, pilot, Ralph) treats it as satisfied instead of re-requesting the review; surface-changing edits re-arm it. Always on. Details: [flowctl.md](flowctl.md).
- **Reviewer verification budget (4.6.0)** - impl and completion review rounds verify via focused, evidence-named suites; the full suite runs only at the run's final gate. Always on (prompt rail). Details: [orchestration.md](orchestration.md).
- **`planSync.enabled` becomes opt-in (4.5.1)** - fresh installs no longer run the per-task plan-sync pass by default: on most specs it finds nothing to change, and manual `/flow-next:sync` keeps the full capability on demand. Existing configs are untouched; setup still asks. Rolling-frontier's prerequisite is now the default state. Enable (the old behavior): `flowctl config set planSync.enabled true`. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md), [`running-lean.md`](running-lean.md).
- **`/flow-next:work-rolling` - rolling-frontier scheduling beta (4.5.0)** - an experimental, user-invoked variant of work that admits the next eligible task the moment any worker returns instead of waiting at wave barriers (52.1% work-phase wall saving at quality parity in the pre-registered fn-203 eval; the faster shared-checkout arm was rejected for buying speed with thinner tests). Same gates, receipts, and reviews; pilot/land/Ralph stay on canonical work. Enable: `/flow-next:work-rolling <spec-id>` (repos with plan-sync on must first `flowctl config set planSync.enabled false`). Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md), [`troubleshooting.md`](troubleshooting.md).
- **A run stops paying twice for the same test evidence (4.3.1)** - when a review fix loop's post-fix run passes the full-gate command at the committed fix HEAD it writes the green receipt the later Verify honors, so a NEEDS_WORK -> SHIP cycle no longer repeats 2-10 minutes of identical tests; and on a multi-task run a worker can inherit the previous task's proven-green baseline when the Quick commands, HEAD, and Touches all check out. Fail-closed both ways: focused suites never mint a full-gate receipt, the first task's baseline always runs, and lint always runs. Enable: nothing - default behavior. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Capture and plan tell you the smallest sufficient next step (4.4.0)** - each closer prints one advisory `Recommended next:` line above its unchanged menu, judged from the spec or plan just produced against [`pipeline-variations.md`](pipeline-variations.md)'s risk-and-unknowns selector (capture: interview vs plan vs minimal-plan; plan: plan-review vs straight-to-work, review skips reserved for the two documented ceremony shapes; chart never a target, `/flow-next:guide` on conflicting signals; autonomous runs unchanged). The PR's external review also hardened the Codex install: docs ship under an owned `docs/flow-next/` namespace (installer can never touch non-owned files, sentinel-tested), every mirrored docs link resolves or is absolute (closure guard), and each page opens with a Codex invocation banner. Enable: nothing - default behavior. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md), [`pipeline-variations.md`](pipeline-variations.md).
- **Less duplicated review and plan-sync work per run (4.2.2)** - three wall-clock fixes with no change to what reviews check: a single-task spec whose only task already holds a per-task SHIP and covers every spec R-ID skips the completion review (recorded skip line, never silent); plan-sync dispatches once per resolved wave with the full completed-task set (same downstream scope, per-task drift verdicts preserved); and a dep-independent task MISSING its `**Touches:**` line on a multi-task spec is now a plan-review finding (omission silently forces serial dispatch), with pilot echoing the work stage's `Sequential fallback:` reason. Also documented: `.git/flow-state` is shared across worktrees - set `FLOW_STATE_DIR` (outside the tree) per agent for concurrent same-repo pipelines. Enable: nothing - default behavior. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md), [`flowctl.md`](flowctl.md).
- **OpenCode is a first-class harness (4.3.0)** - `./scripts/install-opencode.sh` scatters canonical skills + generated agents/commands into `~/.config/opencode/` with a deterministic ownership manifest; setup detects the host via that manifest and runs like every other host; multi-agent fan-out, codex review, and host review (agent-pinned reviewer, fail-closed degradation) verified live. Slash form is flat `/flow-next-<name>`; Ralph intentionally not built; the flow-next-opencode port is superseded. Enable: run the installer. Details: [`platforms.md`](platforms.md#opencode), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Setup prices each opt-in in its question; `host` on every review menu (4.2.1)** - the review-backend question states where the time goes (each review round is a serial pass the pipeline waits on - usually the largest wall-clock item in a run), the `None` option says what still gates a run and what stops being checked, and the Claude Code / Droid / Codex menu now offers `Host` - the host-native reviewer that keeps every gate with no second CLI, pinned via one `reviewer:` routing-block line. Enable: nothing - next `/flow-next:setup` run on an unset key. Details: [`running-lean.md`](running-lean.md#turning-the-dial-none-and-host), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **land asks the human reviewer when it is their turn (4.2.0)** - `land.requestReviewers` (§2.6b → §3.4b, opt-in): a csv of GitHub logins / `org/team` slugs / the literal `codeowners`. Exactly when a human review is the only missing merge input (CI green, no open threads, signal unsatisfied under `approve`/`<login>` or `REVIEW_REQUIRED` under `silence`), land flips a draft PR to ready and requests the list minus the PR author - once per PR per head SHA, recorded in the land ledger; a failed request records the head and reports `reviewers=failed:<reason>`, never `BLOCKED`. It never gates a merge (`reviewSignal` does); `--dry-run` reports `reviewers=would-request`. Enable: `flowctl config set land.requestReviewers "alice,org/platform"`. Details: [`flowctl.md`](flowctl.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Pilot composes with worktrees; tracker states readable without writes (4.1.0)** - pilot's `plan`/`plan-review` stages no longer demand the default branch (they probe the current branch for an open PR and plan in place - the one-worktree-per-spec setup now runs the full loop), and merged gate PRs on a reused branch classify `make-pr` via head identity instead of dead-ending `NEEDS_HUMAN`. New read-only `flowctl tracker wire list-states` (Linear/Jira) lists workflow states with a trustworthy `complete` flag - detection never writes; `tracker resolve` stays the repair verb. Also: a review round that delivers no verdict can no longer wedge a spec's reviews (`REPLAY_REQUIRED` loop; pre-4.1.0 wedges self-heal on the next dispatch). Enable: nothing. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md), [`tracker-sync.md`](tracker-sync.md), [`troubleshooting.md`](troubleshooting.md).
- **Pipeline variations - the stage axis gets an in-repo owner** - a new [`pipeline-variations.md`](pipeline-variations.md) shows five worked routes through the menu (full epic down to docs-chore) and names the real selector: risk and knowns/unknowns, never size - each stage either converts an unknown into a known or bounds the cost of being wrong, and a stage with neither left is ceremony. Prospect/chart are framed as upstream discovery an organization has often already done under another name (roadmap, brief, groomed backlog). The variants are worked examples, not tiers; every route keeps the evidence/gate/review contracts and records skips as `skipped(reason)`. Enable: nothing - documentation only. Details: [`pipeline-variations.md`](pipeline-variations.md).
- **Copy-less installs: flowctl resolves from the plugin install on every host (4.0.0)** - your repos no longer carry flowctl copies: every skill resolves the bundled `flowctl` through a three-rung chain (plugin-root env var on Claude Code / Droid; the plugin root derived from the loading skill's own path on Cursor / Grok; `$CODEX_HOME` on Codex), so a plugin update never requires re-running setup in any repo again. Setup is one mode and writes zero copies; on a repo with leftovers (`.flow/bin/`, `.flow/templates/spec.md`, `.flow/usage.md`) it offers to delete them - nothing reads them, deleting changes nothing observable (a customized spec template is detected and never deleted; copy it to a repo-root `SPEC.md`). `flowctl setup-mode` is gone; old stamps are inert. Enable: nothing - delete your copies whenever convenient, or let `/flow-next:setup` offer it. Details: [`platforms.md`](platforms.md), [`troubleshooting.md`](troubleshooting.md#i-have-flowbin-from-an-old-install), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Packaged codex delegation removed (4.0.0)** - the `work.delegate*` config subsystem (fn-55) is deleted; implementation offload is the routing block's `implementer` tier plus the bridge recipes (`flowctl usage`), which is what superseded it in practice. The two things worth keeping survive in prose: the bridged child writes code while the host keeps git, judgment, and the verdict; and clear, well-scoped work runs well on a value-tier implementer, escalate for gnarly tasks. A config still carrying the removed keys gets one non-blocking advisory and runs unchanged. Enable: n/a - removal; the migration is one routing-block line. Details: [`orchestration.md`](orchestration.md#implementation-offload-the-bridge-route), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Routing by intent: four tier names, a reach page per harness, zero shipped model ids (4.0.0)** - say what KIND of model a job wants (`reviewer` / `implementer` / `fast scout` / `thinking scout`, or unset - the session model, and the default) and write the model names once, in your own `CLAUDE.md` / `AGENTS.md`, verified against your own account. Removed with the machinery: the setup probe-and-pin ceremony, the `models.roles` role map with its `verifiedAt` / `verifiedWith` staleness stamps, and every shipped model identifier outside the orchestration reference page and the `review.backend` grammar. Resolution is one sentence at each dispatch site - explicit instruction, then your routing block, then the agent default, then the session model - and an unreachable model falls back to the session model with one note, never a failure. Enable: `/flow-next:setup` writes the block commented out; fill in a tier line. Details: [`orchestration.md`](orchestration.md#tiers-what-kind-of-model-a-job-wants), [`reach/README.md`](reach/README.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Locked-down repos: the merge-identity seam, server-side catch-up, and a tail that finishes (3.34.0)** - `FLOW_PR_MERGE_CMD` (env-only, #277's merge-side counterpart, stderr-verbatim contract); the `rebase` action is now server-side `catch-up` via `gh pr update-branch` - land never rebases or force-pushes again (removes the #302 orphaned-evidence cause; repeated refusals escalate bounded); the post-merge tail runs release + tracker before the persist-push, so a PR-only base costs a bookkeeping note, not a stuck board. Also: fresh-clone `validate` is meaningful (durability-aware status findings), `done`/`block` report `modified_paths` + a dirty-receipt advisory, `config_lock` reports what it observed instead of a phantom holder, and review-attempt rows record the model/effort that actually ran. Enable: nothing - `FLOW_PR_MERGE_CMD` opt-in by env. Details: [`skills.md`](skills.md), [`flowctl.md`](flowctl.md), [`architecture.md`](architecture.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Concurrent task waves are reachable by default (3.33.0)** - planning now writes a `**Touches:**` line on every task (declare wider rather than omit when uncertain), which is the declaration wave dispatch is fail-closed on; while it was optional, waves fired far less often than the task graph allowed - and in this repo's own recent specs, not at all. Two preconditions are now stated where the conductor reads them: commit the spec/task files before creating wave workspaces (a workspace is branched from a commit, so an uncommitted spec leaves every parallel worker unable to re-anchor), and normalize each task's evidence to the INTEGRATED commit SHAs at the join (handover SHAs die with the workspace and `flowctl validate` then reports them orphaned). Enable: nothing - default behavior; a wave still requires same spec, size <=3, no dependency path, disjoint declarations, and no always-serial paths. Details: [`teams.md`](teams.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **`/flow-next:visual` - review a plan, task, diff, or the conversation at a glance (3.32.0)** - a compact markdown digest (task tree, planned file-layout diff, R-ID coverage line, diff-fenced sketches) rendered in plain fenced blocks that colorize on every host; capture/plan/interview offer it at read-back, and make-pr's `## Structural changes` may emit a diff-fenced sketch where mermaid is weakest (mermaid-rules §8). Read-only, chat-only; explicit-only on Codex (`$flow-next-visual`, see [`platforms.md`](platforms.md)). Enable: nothing - `/flow-next:visual <spec|task|range|topic>` or plain language ("show me", "too much text"). Details: [`skills.md`](skills.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **land can gate the merge on a repo-local verdict command (3.31.0)** - `land.mergeVerdictCommand` (§2.9, opt-in, fail-closed): once every other gate passes and the planned action is merge, land runs the repo's command once (env-only context incl. `FLOW_HEAD_SHA`); exit 0 merges, anything else - missing, timeout, signal - blocks with `NEEDS_HUMAN`. The verdict binds the (head, base) pair it judged; `--dry-run` reports `would-run`. Built for repos where no required status check can exist (free-plan private). Enable: `flowctl config set land.mergeVerdictCommand '<cmd>'`. Details: [`flowctl.md`](flowctl.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Codex reviewers stop inheriting instructions never meant for them (3.30.0)** - the persona-override preamble rides every codex review (was cursor-only; the codex `False` was inherited from a refactor, never decided), host `AGENTS.md` is suppressed on both `codex exec` dispatch paths (`-c project_doc_max_bytes=0`), the plan-review prompt states "You ARE the reviewer" like the other three, and a healthy run with no verdict journals as `missing_verdict` with an instruction-contamination terminal instead of "repair the backend". Repo review invariants belong in `.flow/criteria.md`, which rides the prompts. Enable: nothing - default behavior. Details: [`orchestration.md`](orchestration.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Worktree kit is scriptable (3.29.0)** - `cleanup [<name>...] [--yes]` (names skip the prompt, `--yes` skips confirmation and is required off a terminal, EOF-guarded reads fail loudly naming the remedy) and `create` passes `--no-track` so an isolated branch never tracks the base (first push under default config needs `-u`). Enable: nothing - default behavior. Details: [`skills.md`](skills.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Pilot strikes get a deterministic human clear (3.28.0)** - `flowctl pilot strikes list` / `clear <spec-id>` / `clear --all` over the existing ledger (`<git-common-dir>/flow-next/pilot-strikes.json`); THE recognized recovery under an armed `tracker.readyState` (a projection-set ready never clears a strike - fn-87 rule, now stated on every surface), named in the `strike 2/2` verdict, readiness-orthogonal. Enable: nothing - default behavior. Details: [`flowctl.md`](flowctl.md#pilot-strikes), [`troubleshooting.md`](troubleshooting.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Tracker bridge: CAS mint claim, honest list-open, per-spec Linear Projects (3.27.0)** - `sync create-first-put --if-absent`/`--expect-spec-id` make the promotion mint claim compare-and-set (race loser exits 10 and adopts the winner; ceremony wired into the tracker-sync skill, defers under autonomy); Linear `wire list-open` with unset `tracker.readyState` refuses explicitly instead of returning a silent empty; optional sidecar `tracker.projectId`/`projectMilestoneId` place the issue in a Linear Project (absent = unmanaged, never cleared); the never-promoted-candidate abandon path is documented remote-first. Enable: nothing - default behavior; Project placement is per-spec opt-in. Details: [`tracker-sync.md`](tracker-sync.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Declared vs evidenced coverage + orphaned-evidence findings (3.26.0)** - `export-cognitive-aid` separates the plan-gate question (`undeclared_r_ids` - no task claims it) from the merge-gate question (`uncovered_r_ids`, unchanged); make-pr renders plan-gate specs with `claimed, not yet evidenced` rows and keys its abort on undeclared coverage; `flowctl validate` warns per evidence commit orphaned by a history rewrite (foreign tokens ignored by design, nothing rewritten, two batched git reads); make-pr marks orphaned SHAs instead of linking them. Enable: nothing - default behavior. Details: [`flowctl.md`](flowctl.md#validate), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Six field-reported fixes: parser drops visible, Claude Code detection, --select completes, start --reclaim (3.25.0)** - the acceptance-criteria parser reads title/parenthetical/wrapped bullet shapes and surfaces unparseable ones as `acceptance_criteria_residue` in the export payload; setup detects Claude Code via `CLAUDECODE` + the plugin manifest (rung below Droid/Cursor/Grok); `tracker resolve --select` completes the whole slot map or reports CONFLICT unstamped; `flowctl start --reclaim` repairs a claimant identity distinctly from `--force` takeover; gate-classify's taxonomy documented as deliberately closed to config. Enable: nothing - default behavior. Details: [`flowctl.md`](flowctl.md), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Review-attempt rows carry work volume and provenance (3.24.0)** - every new `review_attempts[]` row records `output_bytes` (always), `tool_calls` (codex `exec --json` streams only; a measured `0` is the fabricated-verdict signal), `head_sha_observed` (snapshot vs finalize-time fallback), and `base_sha` where the review snapshot ran; `review-rounds attempts --json` surfaces them, absence means unknown, and crash replay preserves the journaled values. Enable: nothing - default ledger behavior. Details: [`architecture.md`](architecture.md#review-bookkeeping-authority-and-write-ordering), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Status reads carry provenance; pre-work commands flag stale checkouts (3.23.0)** - `show`/`list --json` always include `status_source` (`flow-state` = authoritative runtime store, `committed` = snapshot that may be stale) with one plain advisory when runtime state is absent; `ready`/`anchor` warn once when HEAD is behind upstream (`stale_vs_upstream` under `--json`; `list`/`status`/`next` deliberately untouched); both shared review prompts bar judging task lifecycle from committed sidecars. Enable: nothing - default behavior. Details: [`flowctl.md`](flowctl.md#show), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Pointer-shaped handovers + executable Next: (3.22.0)** - worker returns name where the outcome lives (paths, commit range, verdict) instead of restating it; the work skill's final summary ends with a runnable `Next:` line and the chart-to-capture handoff is a paste-ready command; the teams page records the doctrine with its two carve-outs. Enable: nothing - default behavior. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Two-axis quality audit (3.21.0)** - the work skill's Phase 4 audit now dispatches the quality-auditor twice in one message (`AXIS: correctness` / `AXIS: standards`), presents both reports verbatim under two headings, and holds the standards axis to a Should-Fix ceiling with per-axis finding caps - hygiene can no longer bury a behavioral defect or inflate the fix loop. Enable: nothing - default audit behavior. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Declined-scope ledger, parked unknowns, failure-signature prose (3.20.0)** - policy-refused scope gets a file in `.flow/memory/declined/` that plan checks before proposing (only the user reopens); specs gain an optional `## Parked unknowns` section with a fog-or-ticket test and graduate-on-resolution; specs state contracts, never file paths (tasks keep `**Touches:**`); skill prose states rules as checkable failure signatures with ~60 new `Done when:` bounds; the repo glossary is a 12-term vocabulary dictionary. Enable: nothing - prose and doctrine only. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Branch-disclosure wave: leaner skill loads, conduct checklists, two removals (3.19.0)** - ten skills now load a universal spine and read branch-only content from `references/*.md` at the branch point (up to 63% less always-loaded prose on chart, 15-40% on the heavy skills; every safety net and every-run contract stays inline). Every skill gains a conduct checklist in `agent_docs/conduct/` that prose changes are reviewed against. Removed: the rp-explorer skill and the context-scout planning agent (planning standardizes on repo-scout; RepoPrompt stays a full review backend). Enable: nothing - structure only; behavior unchanged. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Running lean, and two deprecations** - a new [`running-lean.md`](running-lean.md) names the human-driven and autonomous operating profiles (neither is the real mode), prices every optional layer in structural terms, and gives the manual invocation for each so a lean run is a deliberate choice rather than a missing setup. In the same pass, **Ralph** and **packaged codex delegation (`work.delegate*`)** are marked deprecated: both predate the orchestration primitives that replace them (a script calling pilot + land; the setup routing scaffold plus bridge recipes). Nothing is removed, no defaults change, and existing installs keep working. *(The delegation half was **superseded by flow-98**, which removed the packaged subsystem outright - see the Unreleased CHANGELOG entry; the deprecation-era claim above no longer holds for it.)* Enable: nothing - documentation only. Details: [`running-lean.md`](running-lean.md), [`ralph.md`](ralph.md), [`orchestration.md`](orchestration.md#implementation-offload-the-bridge-route).
- **The flow-efficiency batch: scope-minimal plans, lean tasks, stage receipts, rule-based waves (3.18.0)** - five replay-measured disciplines: plans bind to scope minimality (overengineering is now a plan-review finding); tasks are lean delegation payloads that reference R-IDs instead of restating the spec, with an optional `**Touches:**` write-surface line; planning documents are authored as files and revised with Edit, never re-emitted heredocs; every orchestrated stage records `ran | skipped(reason) | failed(reason)` in its receipts (read back with `flowctl usage --stages <spec>`); and same-spec worker waves dispatch by an explicit fail-closed disjointness rule with reviewer overlap. Enable: nothing - all prose defaults; anything missing or doubtful behaves exactly as before. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **setup-block manages several blocks per file, and `check` gates on drift (3.17.0)** - `--id <BLOCK-ID>` on `apply`/`resolve`/`check` gives each managed span its own marker pair and its own recorded state (previously the second block in a file clobbered the first's hash); the new read-only `check` classifies a block pristine / drifted / structurally broken and exits non-zero on anything but a clean match, writing nothing in any branch. Stricter input contract: a template must now be exactly its marker-pair block (BEGIN first line, END last line, trailing newline, no embedded marker token) - every shipped template already conforms. Enable: nothing; omit `--id` for unchanged single-block behavior. Details: [`flowctl.md`](flowctl.md#setup-block), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Chart notes carry corrections (3.17.0)** - `chart resolve --sharpen-file` accepts `notes_append`, appending a dated correction bullet to `## Notes` in the same transaction that closes the decision, and it travels into the next briefing; unknown sharpen keys now fail the resolve instead of being silently dropped. Enable: nothing. Details: [`flowctl.md`](flowctl.md#chart-resolve), [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Capture/interview propose the spec count (3.16.2)** - epic-shaped inputs (8+ counted business/technical requirements, or multiple independently shippable outcomes) get a concrete 1..n split proposal at read-back: titles, requirement allocation, dependency edges; `split-as-proposed` writes the linked set. Cohesive-but-large stays one spec; never auto-splits. Enable: nothing - fires only past the threshold. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Plan-sync runs when enabled (3.16.1)** - if `planSync.enabled` is `true`, the work loop's downstream-task extraction previously misread the `tasks --json` envelope and the failure looked like "no downstream tasks", so plan-sync silently never ran. Fixed, with an explicit `EXTRACT_FAILED` sentinel so extraction failure can never look like the empty case. Enable: already on for anyone with `planSync.enabled: true` - update the plugin to get the fix. Details: [CHANGELOG](https://github.com/gmickel/flow-next/blob/main/CHANGELOG.md).
- **Review prompts carry identities, not payloads (3.16.0)** - a review is dispatched with a `base..head` range, `git diff --numstat --no-renames -z` as the exact changed-path list, and repo-relative spec/task paths; the reviewer reads the diff and the specs from your checkout itself. The diff body used to be embedded under a 50 KB cap, so on a 641,784-byte change the reviewer was handed ~10% of the evidence its verdict rested on and fetched the rest anyway. Nothing is shortened to fit: the three content fitters, the 50 KB cap, and the "truncated to fit" markers are gone, and a failed `git` read now aborts before a review round is reserved instead of dispatching an evidence-free review. Re-reviews resume the reviewer's own session rather than re-briefing it, with prior findings injected only when a resume demonstrably failed. Expect better-informed reviews, not cheaper ones - measured input tokens went UP, because a fetching reviewer spends turns on tool calls. Enable: nothing. Tune the dispatch bound with `FLOW_REVIEW_EXEC_TIMEOUT` (default 1800s, raised from 600s because a fetching reviewer is slower on a large diff). Details: [`orchestration.md`](orchestration.md#review-backends-cross-model-review), [`flowctl.md`](flowctl.md#review-backend), [`review-findings.md`](review-findings.md#bounds).
- **Spec authoring is a two-call fast path (3.15.0)** - `spec create --plan-file plan.md` creates spec + plan in one shot; `task create --from-json tasks.json` materializes the whole task set atomically (intra-batch deps by 1-based index; any invalid item rejects the batch with zero writes). Granular verbs unchanged, still the editing path. Enable: nothing - the plan skill, the usage guide, and setup snippets now teach it as the default. Details: [`flowctl.md`](flowctl.md#spec-create).
- **Cold sessions re-anchor with one bounded call (3.15.0)** - `flowctl brief` renders the workspace picture (open specs, actionable tasks with claim state, recent completions, memory index) deterministically capped at ~2k tokens; `--full` lifts, `--json` for machines. Setup snippets teach brief-first; task-scope `anchor` unchanged. Enable: nothing. Details: [`flowctl.md`](flowctl.md#brief).
- **Acceptance criteria enumerate their error cases (3.15.0)** - the spec template, plan skill, and interview now put error/invalid-input/boundary handling inside each R-ID bullet (or an explicit "no error surface beyond X"); workers inherit enumerated cases as required tests. Not retroactive. Enable: nothing for new specs. Details: [`spec-template.md`](spec-template.md).
- **Review-loop stall detection reads resolution instead of inferring it (3.16.0)** - the re-review prompt now states the machine grammar for reporting prior findings (`Prior finding #2: not-fixed`, or one `Prior findings: all fixed` line), the parser accepts every token it advertises, and the two heuristic stall rules that guessed convergence from finding-count/severity trends are **removed**. They escalated three healthy converging loops in a row at round 2 of 8. `same-not-fixed-lineage` - the reviewer explicitly calling the same finding unfixed in two consecutive rounds - and the round cap are the only terminals. Enable: nothing. Tune with `review.maxIterations` (new config key; env `MAX_REVIEW_ITERATIONS` still wins) if a loop costs more than it is worth. Details: [`review-findings.md`](review-findings.md#identity-and-lineage), [`flowctl.md`](flowctl.md#deterministic-review-cap).
- **Review loops end on convergence evidence, not just a counter (3.14.0)** - an unchanged artifact is refused before dispatch (`NOT_RETRYABLE`, nothing consumed), a loop whose findings stopped improving escalated early (`ESCALATE: review loop stalled` - that trend-based rule was **superseded by fn-168**, which replaced it with an explicitly-stated repeated `not-fixed`), and reviewers can return `NEEDS_HUMAN` as a first-class verdict that persists its full evidence before exiting. Plan reviews now block only on findings that name a concrete bad outcome; spec-prose findings on code PRs are fix-or-record, never merge-gating. Enable: nothing - deterministic flowctl machinery on every review. Details: [`flowctl.md`](flowctl.md#deterministic-review-cap), [`ralph.md`](ralph.md), [`review-findings.md`](review-findings.md).
- **A reopened chart keeps its capture door** - re-running `flowctl chart briefing` after a `chart reopen` with the same proposal over an unchanged ledger used to hand back the briefing the reopen had staled and call it a no-op, leaving a briefable chart with no capture-ready package and no obvious way to get one. The reopen now counts as a new epoch, so the same proposal mints the next package and names what it supersedes (`supersedes_stale` under `--json`; `(supersedes stale B1)` on the terminal line). Enable: nothing - it is what `chart briefing` now does after a reopen. Details: [`flowctl.md`](flowctl.md#chart), [`../skills/flow-next-chart/workflow.md`](../../skills/flow-next-chart/workflow.md).
- **Review-round cap default raised 4 -> 8** - fix+re-review loops get twice the room before refusing and escalating to a human. Behavior at the cap is unchanged (refuse to dispatch, exit `4` + `ESCALATE:`, reset only on SHIP or `flowctl spec reset-review-rounds`). Enable: nothing; override with `MAX_REVIEW_ITERATIONS=<n>` (never zero, never disabled). Details: [`ralph.md`](ralph.md), [`flowctl.md`](flowctl.md).
- **`chart create --initial-map-file` refuses an ambiguous alias namespace (3.13.2)** - an explicit `id` colliding with another decision's generated alias (`<n>`, `d<n>`, or the full decision id) is now rejected with a `validation` / `alias_collision` error naming both claimants, instead of the last writer silently winning and every edge on that alias pointing at the wrong decision. Enable: nothing - it is the command's own validation. Details: [`flowctl.md`](flowctl.md#chart).
- **Chart's entry test - destination known, route unknown (3.13.1)** - a theme or direction ("make X more Y") is now refused before any grounding spend, because with no nameable end state there is no Outcome to state, no boundary that rules anything out of scope, and a map that never closes; chart offers a narrowing or `/flow-next:prospect` instead. Enable: nothing - it is the skill's own refusal path. Details: [`../skills/flow-next-chart/SKILL.md`](../../skills/flow-next-chart/SKILL.md), [`../skills/flow-next-guide/SKILL.md`](../../skills/flow-next-guide/SKILL.md).
- **`/flow-next:chart` + `/flow-next:guide` (fn-135)** - optional pre-capture decision-map discovery for oversized/unclear ideas, plus a smallest-sufficient router so chart never becomes a mandatory stage. Enable: nothing required for local chart work; optional tracker projection with `flowctl config set tracker.charts on` when the bridge is active. Details: [`../skills/flow-next-chart/SKILL.md`](../../skills/flow-next-chart/SKILL.md), [`flowctl.md`](flowctl.md#chart), [`tracker-sync.md`](tracker-sync.md#chart-lifecycle-projection).
- **`FLOW_PR_CREATE_CMD` - make-pr's PR-create call is interposable** - repos that require App/bot-authored PRs (single-maintainer repos with a required approval, where GitHub forbids self-approving) point the env var at a wrapper that supplies its own identity; the wrapper receives make-pr's stable argument contract (`--title/--body-file/[--draft]/--base/--head`) and must print the PR URL. Unset = `gh pr create`, unchanged. Enable: `export FLOW_PR_CREATE_CMD=/path/to/wrapper`. Details: [`create-and-finalize.md`](../../skills/flow-next-make-pr/create-and-finalize.md) § 4.6 (inline contract).
- **`FLOW_PR_MERGE_CMD` - land's merge call is interposable** - repos whose branch protection lets only an App/bot perform the merge point the env var at a wrapper that supplies its own identity; the wrapper receives land's fixed argument contract (`<pr> --squash --delete-branch --match-head-commit <sha>`), must exit 0 only on a real merge, and must proxy `gh`'s stderr verbatim - land classifies a head-race (retry next tick) apart from a policy refusal (blocked) by reading that text. Never `--auto` or merge-queue enrollment; scope is the merge call only, every other `gh` call in the tick stays on the session identity. Env only, never a config key. Unset = `gh pr merge`, unchanged. Enable: `export FLOW_PR_MERGE_CMD=/path/to/wrapper`. Details: [`workflow.md`](../../skills/flow-next-land/workflow.md) § 3.5 (inline contract).
- **Standing project-wide acceptance criteria are now judged on every spec** - put team-wide rules in `.flow/criteria.md` (`- **G1:** ...`, the R-ID grammar at project scope) and the existing spec completion review judges each one, recording met/violated/n-a compliance in the ordinary review receipt. Absent file = zero effect anywhere. Enable: answer the `/flow-next:setup` "Global criteria" question, or create `.flow/criteria.md` yourself. Details: [`spec-template.md`](spec-template.md) § Global criteria, [`review-findings.md`](review-findings.md) § Global-criteria compliance.
- **Pull requests now guide the human review from intent to evidence** - logical steps explain why the change exists, group the files that belong together, name deliberate non-changes, and attach proof, while the separate review plan keeps attention on the decisions that still need human judgment. Enable: nothing to configure. Details: [`pr-cognitive-aid.md`](pr-cognitive-aid.md).
- **Review findings keep their identity across fix rounds** - current, resolved, and superseded findings stay distinguishable and traceable to the review that raised them, while existing prose receipts remain valid. Enable: nothing to configure. Details: [`review-findings.md`](review-findings.md).
- **Interview-authored criteria now say where they came from** - reviewers can distinguish what a person said from what the agent inferred, so the grounded-versus-guessed tally and targeted follow-up work on interview-authored specs. Enable: nothing to configure. Details: [`spec-template.md`](spec-template.md) § Source tags.
- **Interview now fills the spec sections your project adds** - mark a project-specific section with `<!-- scope: ... -->` and the matching interview pass owns it; other or unmarked sections stay preserved. Enable: add a scope marker to the section; no config. Details: [`spec-template.md`](spec-template.md) § Customizing the scaffold for your project.
- **Tracker status conflicts now follow the team's chosen policy** - `flow-wins` and `tracker-wins` take their documented paths, while `always-ask` keeps the conflict visible for human resolution. Existing default remains `always-ask`; configure with `flowctl config set tracker.conflictTiebreak flow-wins|tracker-wins|always-ask`. Details: [`tracker-sync.md`](tracker-sync.md) § Reconciliation - who-wins.
- **Reviewer sandbox is never widened, and a delivered verdict is never a transport failure** - the codex sandbox-failure message used to suggest `--sandbox danger-full-access` / `CODEX_SANDBOX`, which agents pattern-matched onto unrelated review failures and used to re-frame a `NEEDS_WORK` as retryable transport. Reviewers are read-only by contract, so a blocked reviewer is a prompt/scope bug (Windows still resolves via `auto`). No action needed; the review skills now carry the rule explicitly.
- **Tracker mutations now use one deterministic facade across GitHub, GitLab, Jira, and Linear** - lifecycle skills still decide semantic content and recovery choices, while `flowctl tracker` owns provider requests, pagination, idempotency, status policy, relation projection, and one aggregate receipt. No action needed for existing tracker configuration. Details: [`tracker-sync.md`](tracker-sync.md) § Lifecycle facade.
- **`.flow/` writes refuse symlinked components** - `flowctl` no longer writes through a symlink anywhere between `.flow` and the file it is writing, so an untrusted checkout cannot redirect a write outside the workspace or onto another managed file. A legitimately symlinked `.flow` **directory** is still supported. No action needed; if a run now reports a refused path, replace that symlink with a real file or directory.
- **`tracker.specIds`** - team default id scheme for new specs when a tracker is configured. Parallel agents collide on bare `fn-N`; tracker-keyed ids (`WOR-17` → `wor-17-slug`; GitHub `#123` → `gh-123-slug`; GitLab iid → `gl-N-slug`) use the tracker as the distributed allocator. Enable: `flowctl config set tracker.specIds tracker` (or answer the setup question when a tracker is configured and the key is still unset). Details: [`tracker-sync.md`](tracker-sync.md) § Hybrid id model / `tracker.specIds`.

## Conventions

- **R17 cross-link discipline.** Each doc here is a self-contained reference. Canonical sources (`templates/spec.md`, `scripts/sync-codex.sh`, `STRATEGY.md`, `GLOSSARY.md`) are linked, never re-embedded.
- **Relative paths only.** No absolute `github.com/...` URLs anywhere in this tree - fork-survivable + offline-readable.
- **Length discipline.** Reference shape (tables, lists, schemas first; narrative second). Brevity beats completeness.

## See also

- [`../README.md`](https://github.com/gmickel/flow-next/blob/main/plugins/flow-next/README.md) - plugin overview, install, workflow narrative.
- [`../../../STRATEGY.md`](https://github.com/gmickel/flow-next/blob/main/STRATEGY.md) - flow-next's strategic intent + active tracks.
- [`../../../GLOSSARY.md`](https://github.com/gmickel/flow-next/blob/main/GLOSSARY.md) - canonical vocabulary (Spec, Task, R-ID, ...).
- [`../../../CONTRIBUTING.md`](https://github.com/gmickel/flow-next/blob/main/CONTRIBUTING.md) - contributor entry point (local dev, adding skills, releasing).
- [`../../../CLAUDE.md`](https://github.com/gmickel/flow-next/blob/main/CLAUDE.md) - repo-level guide for working in this codebase.
